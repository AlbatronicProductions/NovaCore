# NovaCore handoff

## Start here

Before editing, read `README.md`, `ENGINEERING_RULES.md`, `docs/NOVACORE_CURRENT_STATE.md`, `docs/architecture.md`, and `docs/planetary-rendering.md`. Inspect `git status --short`, then inspect the exact implementation and tests in scope. Repository contents and tests win over this handoff if they differ. Preserve all existing unstaged work; do not reset, restore, stage, commit, tag, or push unless explicitly directed.

## Architectural invariants

- Simulation owns celestial truth. Graphics receives immutable evaluated presentation data only.
- Do not change authoritative body centers, physical radii, hierarchy, ephemerides, simulation time, or deterministic identity to solve rendering issues.
- Subtract root-resolved camera and body positions in doubles before GPU float transport.
- NCPE is storage. `CelestialSystemDefinition` plus `CelestialSystemEvaluator` is runtime celestial authority.
- CSPICE, NAIF kernels, source manifests, and builders are offline-only; no runtime dependency is allowed.
- GPU LOD, culling, labels, markers, and representation handoff are presentation-only. Keep CPU reference implementations as correctness oracles where equivalent GPU logic exists.
- Native/managed ABI changes require explicit fixed-width layout validation and focused native/managed tests. Native Vulkan resources have explicit native ownership.

## Renderer architecture

`CelestialSystemDefinition` → `CelestialSystemEvaluator` → immutable `PlanetaryPresentationSnapshot` → managed camera-relative submission → narrow native ABI → Vulkan renderer.

The renderer shares one normalized 16×16 cube-sphere grid for global/regional work. Deep quadtree levels are no longer the production surface path: a renderer-lifetime 128-ring × 256-segment camera-centric spherical topology takes over through a 2,000–1,000 km overlap and is exclusive at and below 1,000 km. One compute dispatch performs body-fixed FP64 terrain evaluation/displacement and writes one indexed-indirect command; CPU leaves, near-ground stitch balancing, and cache traversal are absent from that range. Far bodies use a shared whole-body mesh and deterministic distant/detail handoff. All representations consume immutable planet material, environment, evaluated-Sun, physical-radius, authoritative body-fixed orientation, and camera-relative presentation authority. Local scenes use finite reversed-Z; Solar uses infinite-far reversed-Z.

## Current milestone

Milestone 9A-4F-2 remains complete; Milestone 11B-3A owns the checked production-v3 Earth channel format; Milestone 11B-3B adds optional lawful bounded regional ingestion; Milestone 11A-4 adds deterministic body orientation; Milestone 11A-4B adds current-epoch Solar startup and extended warp; Milestone 11A-4D unifies orientation through the render handoff; and the final 11A lunar checkpoint adds compact DE440 orientation. The global Earth pack retains five logical levels and 682 page identities with a 256×256 interior and two geographic gutter texels per side. `SolCompact-DE440Validated-v3` preserves the DE440-grounded ET0 seeds and lunar translation corrections. Definition hash is `0x493FE8B1E867110F`; residual hash is `0xD5E2E00FF5F1C2C2`. DE440/CSPICE remains offline-only.

The body-orientation evaluator is a separate pure exact-epoch authority. Mercury, Venus, Earth, Mars, Jupiter, Saturn, Uranus, and Neptune evaluate the official `pck00010.tpc` models directly. The Moon uses a 3,506,540-byte embedded residual pack extracted every six hours over 1900–2100 from official `moon_pa_de440_200625.bpc` through `moon_de440_250416.tf` frame `MOON_ME_DE440_ME421`; pack hash is `0x3BCE78D924EA3532`. The complete 13-term `IAU_MOON` model remains an explicit deterministic fallback outside coverage or on invalid pack provenance. Offline `pxform` comparisons cover J2000, current epoch, ±1/30/365.25 days, arbitrary history/future, fractional knots, and maximum-warp destinations. ET0 orientation hash is `0xD5767D2C2BABE9AA`; all-nine warmed evaluation allocates zero bytes.

The true-distance Solar scene retains focus navigation, moving bodies, deterministic orbit paths/labels/markers, label collision rejection, and extreme-range infinite-far projection correction. Fresh default construction queries UTC exactly once and converts it with the pure pinned-NAIF0012 `SolarUtcTime` boundary; deterministic scenes call the explicit `SimulationInstant` constructor. Thereafter only `SimulationClock.CurrentTime` drives translations, orientations, paths, and rendering. One immutable 15-entry speed catalog spans 0.1×, realtime, and the ordered accelerated presets through 7,776,000×. Native rising-edge comma/period input steps exactly once. Speed feedback remains wall-time driven for two seconds with the final 0.75 seconds fading smoothly, now through a dedicated 210-vertex 5×7 sans bitmap-glyph HUD draw that uses derivative antialiasing, adds no descriptor or per-frame allocation, and leaves ordinary 3×5 body labels unchanged. Orbit paths are sampled from the current simulation instant through the same compact evaluator; this replaces the visibly stale ET0 path policy without a Moon renderer branch. The overview remains capped at 100 AU. Startup and `R` select a deterministic 58 AU Solar Map home pose near the ecliptic normal without changing the current instant; mouse drag orbits in a root-inertial body-centered frame, and time advancement translates that rig without inheriting body spin.

The renderer writes the scene to one persistent FP16 target and tone-maps it in a second subpass using fixed exposure. Solar frames add a deterministic procedural infinite-far background, an evaluated-center/physical-radius stellar Sun with analytic corona, and one generic evaluated-Sun lighting record shared by distant and detailed planets. A 160-byte fixed-width presentation record carries the same material identity, body-fixed-to-root quaternion, and response to both planet representations; the complete frame submission is 784 bytes. The nine non-stellar bodies use repository-authored body-local materials; Saturn's generic annulus is relative to the authoritative equatorial frame. Earth near field retains the fixed 11A workload and checked SVT data.

10B-2B adds one 128-byte body-associated environment record and extends the frame ABI to 640 bytes. Earth uses a bounded 100 km atmosphere, 2–11 km procedural cloud shell, and 180 m presentation sea level. The persistent fullscreen environment pipeline draws after the infinite background and before opaque geometry into the existing HDR target; it adds no depth attachment or per-frame image. Distant and detailed materials share body-direction cloud density, evaluated-Sun brightness, bounded cloud shadows, and raw-terrain sea-level classification. Below sea level the existing grid clamps to a spherical water surface with radial normals, bounded wave perturbation, roughness/specular response, and Fresnel color.

The Earth camera still exposes `Orbital`, `Transition`, and `SurfaceLocal` modes and preserves the ten-metre shared CPU/GPU terrain floor. `PlanetaryEyeballTopology.Shared` is immutable: 32,769 vertices, 195,840 indices, hash `0x4A46E29A7D6E90A7`. Its 1,572,912-byte output vertex buffer, 783,360-byte index buffer, 128-byte constants buffer, and 20-byte indirect buffer are persistent. 11A-2 adds body-metre triplanar regional/landscape/local/surface/grain material bands, smooth slope blending, derivative-filtered micro normals/roughness, atmosphere-authority aerial perspective, coherent ocean detail, and bounded `W/S/A/D` body-tangent translation. A 2 m lateral proof and both directions of the 2,000–1,000 km handoff retain CPU/GPU parity and fixed topology.

11A-4D makes the distant convention match those regional and eyeball contracts: the whole-body mesh is body-local, its geometry is rotated by the authoritative quaternion exactly once, and its normal/material/light directions remain body fixed. A live handoff audit corrected the detailed shader's mixed-frame Sun subtraction and established a non-rotating focused-camera frame. Focused tests drive every non-stellar Solar body through detailed-only, transition, and distant-only states while paused; camera position changes around the center at fixed distance while body position/quaternion remain exact. Earth, Mars, Jupiter, Saturn, and Moon additionally pass 1×, 30×, 120×, 600×, 14,400×, and 7,776,000× translation-follow/rotation-independence proofs.

11B-2A makes the camera/reference-frame boundary explicit. `FocusTarget` separates evaluated target position from root-inertial camera orientation. `CameraRelativeRenderPosition` requires `objectRootDouble - cameraRootDouble` before any narrowing or high/low split. The distant, regional, eyeball, stellar, ring, orbit, marker, label, environment, atmosphere, and lighting paths already followed that ordering. The generic object/triangle path was corrected from shader-side subtraction of independently encoded absolute roots to CPU-relative encoding without changing the native ABI.

11B-2B activates deterministic `SurfaceAnchorFocus` in the Solar camera without requiring a vessel. At 2,000 km the actual camera view ray may acquire a body-fixed physical/elevation-surface point; a miss preserves the prior valid focus, and full anchor focus is reached at 1,000 km. Receding retains the anchor through a separate 3,000 km release threshold. Earth refinement queries the existing loaded CPU elevation oracle, other bodies retain physical-radius intersection, and the anchor reevaluates through current body translation/orientation at all warp presets. Local east/north/up transforms are pole-safe and right-handed. Wheel zoom is logarithmic relative to the physical anchor, a bounded terrain query enforces ten metres of clearance, and camera orientation remains root-inertial throughout. `SceneObject` remains an explicit alternative target, not an acquisition prerequisite.

11B-3A replaces the monolithic production-v2 Earth records with an explicit version-3 four-channel pack. Albedo is BC7 sRGB L0–L4, elevation is bit-identical R16 UNORM L0–L4, land/ocean mask is BC4 UNORM L0–L4, and clouds are BC4 UNORM L0–L2. Each channel has independent request, residency, parent fallback, promotion, and eviction state while retaining the same body-fixed page identities and 30-frame blend. The native renderer queries sampled-image and transfer-destination support for BC1/BC3/BC4/BC5/BC7/R16, performs direct compressed copies with no runtime decode, and selects a labeled uncompressed procedural-root fallback for absent/incompatible data or format support. Pack identity is `664ff32c3a57043960f246d5d97397214cedc4b976e48e867e9803c414d796b5`; pack hash is `a16aebd834f01bdd430790de499a095d55f895655ce037fe25b6e13106674dc5`.

11B-3B adds `tools/planetary_data/ingest_region.py` and a checked optional Mount St. Helens pack derived from bounded official USGS/USDA NAIP and USGS 3DEP exports. The 343 km² proof retains exact provenance and hard defaults of 128 MiB raw input, 64 MiB output, 256 pages, and 1,000 km². It reuses the global body-fixed page identity, emits BC7/R16/BC4, and contains 48 sparse L5–L12 pages in 11,359,360 bytes; pack SHA-256 is `9f66aa63963ce503fc871eed03d5626b42548dec24c8222221f8c273b6c27b00`. Startup-only checked discovery and three fixed 512-entry per-channel tables provide bounded lookup without forcing equal channel LODs or page counts. Reserved existing slots 80–127 hold regional pages; source validity, regional-parent search, and global parent/root fallback preserve coverage when optional content is missing or rejected. Full source, datum, quality, budget, and extrapolation details are in `docs/planetary-data-ingestion.md`.

## Recommended next milestone

**11B-3B completed: lawful bounded regional ingestion.** Keep the global-v3 format and fallback authority, checked regional provenance/budgets, shared body-fixed page identity, fixed runtime lookup, and optional-content behavior. The exact next milestone is 11B-3C: local ENU procedural material detail plus BC5 micro normals plus bounded GPU scatter. Begin the material/detail transition only below the measured regional scientific frequency (approximately 13 m/texel at Mount St. Helens), and never move coastlines, terrain peaks, source elevation, or geographic identity.

## Recommended models

Use **Terra Medium** for documentation, focused tests, managed wiring, and simple rendering changes. Use **Sol High** for Vulkan, shaders, GPU compute, synchronization, ABI-sensitive native work, and renderer architecture changes.
