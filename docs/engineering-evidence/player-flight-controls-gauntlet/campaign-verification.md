# Player flight controls qualification

**Stage6 engineering PASS — STOP FOR PROJECT CONTROL.** Stages1–5 passed and promoted automatically. Final manual acceptance is **PASS - explicitly reported by Project Control**. The accepted controls accomplishment is **UNBANKED and ready for Project Control banking consideration**. No milestone or banking is authorized.

## Native recovery and cause

Repeated original Sky W/Q taps set and cleared real Windows held state before any production snapshot: W tick13838328/frame3261, Q tick13838421/frame3280. Each had zero intervening snapshots and zero managed held observations. This proves **HARNESS / EVENT-DURATION MISMATCH for the reproduced original mechanism**, not a retrospective trace of earlier uninstrumented trials.

The authorized external diagnostic helper used actual separate Windows SendInput down/350ms hold/up through unchanged handlers. Six individual axes, three opposed pairs, four simultaneous unions, two real focus-loss/return cases and Earth/Moon focus/F passed:17 actual episodes and24,111 independent physical-oracle checks. Native snapshots match managed input. Release admits zero/remaining demand while momentum continues. Loss clears attitude but preserves engine latch; old holds do not resurrect on return.

**No production correction occurred during recovery or Stage6.** No artificial production path, field mutation, sampling delay or managed-demand substitution supplied keyboard proof. Observer overlays and driver are diagnostic only. Stage5's earlier inactive-frame cleanup remains its one historical production correction. Driver foreground completion and first-source-held-gimbal test preconditions were corrected without production edits; invalid trials were excluded.

## Integrated qualification

Five predeclared native repetitions cover READY, Z, WAQ with real orbit, wheel zoom, X, SDE, Earth focus with D controlling SRV, F and refused terminal Z. All2,601 native/managed snapshots match. Each uses vessel201/generation1, one ingress, one eight-admission journal and128 intervals. Mouse input changes orientation without a command admission; zoom24→19.2 is restored by F. View changes never retarget controls.

The Stage4 independent named-nozzle/quaternion oracle passes6,703 additional checks: selected physical nozzles, source-held finite gimbal slew, full force/moment, exact unique-nozzle debit, coast momentum, save restoration and bit-exact history/state/debt under changed host-credit fragmentation. Reversal preserves dynamics; angular velocity need not immediately match a reversed request. captured-session.json retains one actual save.

Permanent integrated tests pass3,007 checks per configuration. They retain run1 admitted frontiers, exercise depletion during overlapping engine/gimbal/RCS demand, prove empty feeds cannot power later requests, verify center-of-mass momentum after cutoff and exact replay. Two test-authoring mistakes were repaired: spinning material-origin velocity need not remain constant, and fresh scene lifetimes need fresh camera bindings. Neither was a production failure.

## Authority and convergence

One capability/vessel/generation and bounded256-entry journal own engine and signed pilot intent. Stale/foreign/outstanding/order errors refuse. Same-frontier OFF dominates. The54-row allocator preserves existing force/flow/COM torque bounds: main-ON gimbal pitch/yaw plus roll RCS; main-OFF deduplicated nozzle unions. Exact resource owners determine powered duration/depletion. Input never writes transforms, angular velocity, resources or actuators. Save v5 carries physical execution; v3/v4 retain prior demand-only behavior. UI holds are not serialized.

Installed KSA remains2026.9.10.5438, SHA256 A03E98153C6F353AF6F280CD3BDC1C71C718008E2049F508DC6FBE54457A0AA8. Accepted source/history held/reset and view/control separation findings are reused. No broad research restart, KSA code/assets or runtime dependency. NovaCore retains deterministic admission, exact feed, bounded replay and finite gimbal policy.

## Performance and storage

campaign-performance.json records final normal-runtime median/P95/P99/max, five repetition maxima, allocations and GC for request resolution, adapter/admission, allocation lookup, physical service, follow/presentation and their combined path. Pure changing admission is separately measured by pilot-demand. The integrated adapter has8 changes per128 intervals. Combined includes diagnostic request/lookup probes; phase byte/GC totals cover the whole route, not each phase separately.

Equivalent fresh warmed combined work measures exact0B and zero collections in both configurations; positive control152B. Cold scene/assets/focus selection, full Solar publication and native GPU/present are outside this contracted managed path. Assembly submission uses a default camera snapshot plus actual follow-camera origin. Existing celestial/vessel switching costs48B/operation and is not renamed zero allocation. Actual native timing separately covers real display/present scheduling with observer overhead.

Fresh-process service peaks repeat in all five runs at4.71–5.33ms, P99.2183–.2543ms; cause remains unestablished. Earlier Stage2 peak6.0184ms is context, not an equivalent-workload improvement claim. Final warmed Release combined maximum1.1675ms occurs in one of five windows; other maxima.359–.450ms. No invented budget or demonstrated new material repeating regression. Tiny request/lookup times can round to timer resolution.

Named retained bounds: journal14,336B; cold attachment14,448B; allocator17,280B; full pilot save76,578B; engine/max-credit save162,428B under1MiB. Input100B, host event128B, physical horizon128 intervals/2,000,000 ticks. These are payload bounds, not process-memory census. Prior Stage3 raw allocation diagnostic retains an unproven cause; exact gates pass under the approved measurement split.

## Regression, provenance and handoff

Debug/Release builds pass with zero warnings/errors.58/58 final gates protect authority, allocation, recorded Plan, resources, free flight, Florida support, static/warp/moving/cost camera, ABI, launcher, Simulation, ReferenceFrames, Precision, Camera and BEPU. Independent review passes all24 attacks. Existing departure tests do not authorize Florida powered departure. HOME is unbound; FREE deferred. Generic Solar console help still mentions surface WASD/E; proving-scene help/routing describe actual controls. No assistance systems or flight warp entered scope.

HEAD/local/public main and M15.4 tag remain banked; all55 entry-sealed unrelated files unchanged and index empty.88/88 accepted deployment hashes match the later manual warp-correction manifest. Earlier Stage2 launcher refresh and mistaken older restoration were reconciled by restoring two files from the accepted later candidate; launcher-provenance-reconciliation.json preserves that history. No controls candidate was deployed. Project Control's prior camera manual PASS remains intact.

Project Control exercised the supplied bounded Release route across repeated fresh sessions and accepted all listed player-facing controls and camera behavior. The canonical endpoint hold after 128 intervals / 2 seconds is accepted qualification-harness behavior, not loss of momentum caused by X. manual-acceptance.json records this external acceptance and its association with the unchanged candidate. The route remains reproducible in manual-route.md and launch-player-controls.ps1. Final closure made no production change or deployment. Ready for Project Control banking consideration; no milestone, staging, commit, tag, push, banking or next front. STOP FOR PROJECT CONTROL.