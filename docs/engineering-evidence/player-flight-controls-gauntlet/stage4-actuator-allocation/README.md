# Stage 4 — physical actuator allocation

**PASS — PROMOTE to Stage 5.** No production correction. Independent read-only architecture review PASS against `source.json`; 54/54 regression gates and 36,768 focused checks in each configuration pass.

One cold-qualified 54-row table converts admitted signed demand into actual gimbal targets and unique physical jet masks. With main requested ON, pitch/yaw use gimbal and roll uses RCS. With main OFF, all axes use the union of signed jet pairs. Shared nozzles fire and debit once: combined commands saturate naturally at that union, without ideal torque or axis priority. Exact shared-feed depletion remains authoritative. Gimbal slew and source-held interval realization remain unchanged.

Current KSA findings carried from the accepted source/history trace: adapt manual axis requests and deduplicated RCS union; intentionally retain NovaCore's finite gimbal slew, deterministic interval admission and exact shared-resource owner. No rate assist or KSA tuning is copied. The current admitted main request selects allocation; using previous realized engine state on first ignition would exceed the qualified force bound.

All 54 combinations are cold-proved against unchanged force, flow and dry/wet COM torque limits before capability installation. The reduced-main numeric negative witness and legacy geometry refuse atomically. Independent literal nozzle/quaternion tests cover signs, combined saturation, reversal, cutoff, resources, depletion and fragmentation. Physical execution uses save v5; v3/v4 preserve demand-only semantics. Recorded arbitrary jet masks remain refused.

Measured complete admission/allocation/resource/dynamics/publication path: exact 0 B on equivalent warmed normal and isolated fixtures, positive control 152 B. Release median/P95/P99/max: 0.1000/0.1185/0.1459/0.1673 ms. Table payload 17,280 B; journal payload 14,336 B. See measurement, regression, geometry and source records. No physical envelope, camera authority or bank changed. Native attitude input is Stage 5 work.
