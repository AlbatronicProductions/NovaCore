# Reproduce the engineering candidate

From E:\NovaCore, run build-candidate.ps1 here with -Configuration Debug and -Configuration Release, then validate-stage6.ps1. It runs58 final gates using isolated campaign outputs. Referenced launcher test executables were built from unchanged current launcher source at Stage2; launcher provenance/restoration are documented separately. Do not refresh standard deployment paths.

Permanent gates include Simulation.Tests --assembly-control, --assembly-control-measure, --pilot-demand, --pilot-allocation; Graphics.Tests --player-engine, --player-attitude, --player-integrated and --case=Transport layout. The matrix includes recorded Plan, resources, free flight, existing contact/departure baselines, Florida support, static/warp/moving/cost camera tests, launcher, ReferenceFrames, Precision, Camera and BEPU.

Actual native diagnostic instructions are in stage5-player-wasdqe/reproduce.md. Distinguish real Windows proof from synthetic permanent tests. stage6-integrated-controls/captured-session.json preserves an actual integrated sequence and exact save; restore and fragmented-credit replay are verified by the independent diagnostic oracle. The permanent integrated fixture retains run1 frontiers0,9,31,35,41,63,72,94 and tests shared depletion, momentum and deterministic history. New wall-timed keyboard runs may admit at different frontiers.

Normal production candidate: build/player-flight-controls-gauntlet/candidate/bin/NovaCore.Triangle/release/NovaCore.Triangle.exe, arguments --scene=sol --player-flight-controls --solar-epoch=j2000 --physical-surface=m12d-natural-candidate. launch-player-controls.ps1 launches this isolated candidate. NOVACORE_CONTROL_TRACE=1 adds allocating diagnostics excluded from ordinary cost gates; the launch helper removes it for the child only.

Normal timing and checked no-GC allocation use equivalent fresh warmed fixtures under ENGINEERING_RULES.md. No threshold is invented. Phase distributions and five repetition maxima are retained; tiny request/lookup measurements can round to timer resolution. Pure changing canonical admission is separately measured by --pilot-demand. Integrated adapter samples contain8 changing admissions plus held snapshots per128 intervals. Phase byte/GC fields are whole-route totals. Managed presentation includes vessel follow and assembly render submission with default camera snapshot; it excludes full Solar publication, native GPU/present and camera selection edges. Actual native episode timing covers the real callback/present route separately with diagnostic observer overhead.

identity.json seals current source/runtime. Earlier checkpoint hashes remain historical. Reproduce overlays only in an isolated checkout; do not overwrite user changes or banked outputs. Final manual acceptance is PASS, reported by Project Control. The accepted candidate remains UNBANKED.