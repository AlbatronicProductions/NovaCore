# Stage 5 — actual player WASD/QE qualification

Historical checkpoint: Stage 5 recovery and Stage 6 qualification subsequently completed. Project Control has now reported final manual PASS; current authority is root campaign-verification.md and manual-acceptance.json. Original checkpoint wording below preserves its timing.

**STOP FOR PROJECT CONTROL. Stage 5 is not qualified or promoted.** Implementation and managed/native build checks exist; actual held/all-axis native input proof remains unestablished. Stage 6 is unopened.

Native input carries six fixed-width held bits, separate from existing camera movement polling. Fresh non-repeat focused keydown sets a bit; ordinary/system keyup clears it. Focus/capture/destruction and native modal entry/cancellation clear transient state. Inactive frames clear only player engine edges and pilot bits. Regaining input eligibility cannot reconstruct a hold from asynchronous keyboard polling. ABI is explicit: input 100 B, pilot offset 96, host submission offset 120, host event 128 B.

The application converts the snapshot to W/S pitch, A/D yaw, Q/E roll and uses the same canonical capability, ordering and journal as Z/X. Simultaneous engine/axis changes share one admission. READY X plus attitude remains pilot-only; held/repeat is a no-op, opposing keys cancel, releasing one opposing key reveals the remaining hold. Loss of eligibility admits neutral attitude without changing the engine latch. Restore persists admitted state, never UI-held bits. Both camera entry paths remove the six movement fields only inside the explicit proving context; F, focus selection, orbit, zoom and independent Solar presentation controls retain their authority.

Current KSA command/held/reset and viewport/control findings are carried from the accepted Stage 1/3/4 current-source and authenticated-history trace. The installed assembly hash was rechecked unchanged. Adapt request/reset boundaries; preserve NovaCore's deterministic bounded admission and physical owner. Body convention remains right-hand roll X, pitch Y, yaw Z: W/A/Q positive and S/D/E negative. These are body axes, not screen directions.

Permanent managed proof covers all 64 key combinations, repeat, opposing release, focus/capture-neutral snapshots, combined ignition, engine-off RCS, release resource cessation, inertial angular momentum, restore, terminal/disposed refusal and exact camera-input filtering. Final corrected Debug/Release builds have zero warnings/errors; 56/56 gates and 8,264 focused checks per configuration pass. Timing uses normal runtime; exact allocation uses equivalent fresh warmed fixtures with checked no-GC isolation. Both measured paths report 0 B, with a 152 B positive control. Final results and source hashes are sealed separately.

Independent review found **one causal production defect**: initially the per-frame inactive fallback called broad ClearRawInput, resetting camera edge histories every inactive frame. The one permitted correction clears only engineActions/pilotKeys there. Final source review PASS, no second production defect found. Native modal/event resets remain event-driven. Test-authoring fixes (enum name/struct equality and replacing a false constant-body-omega assertion with world angular momentum) changed no production behavior.

Actual engineering attempts used the supported Windows tool's press_key API. A Z+W tap followed by X established ON at frontier 0 and OFF at frontier 34, with actual cutoff at 35 and continued momentum, but **no attitude admission**. Six individual W/S/A/D/Q/E taps in a fresh READY session also yielded no attitude admissions. The logs prove the missing admission, not its precise native event timing. Press/release draining before a sampled held snapshot is a plausible explanation, not a proven causal diagnosis. The documented tool exposes a tap/chord and no hold-duration or separate keydown/keyup operation. No alternate input semantics or enlarged physical envelope was introduced to manufacture proof.

Both trials preceded the narrow cleanup correction; runtime fingerprints in keyboard.json identify them. They are not evidence of corrected-candidate held behavior. Actual sustained all-axis, opposition, combined input and focus/capture lifecycle remain unproven. Managed coverage and source review cannot satisfy that gate. Project Control decides how to recover the missing engineering proof. Final campaign manual acceptance remains required and PENDING; it has not been reached or requested early.

Residual presentation issue: the generic Solar console help still describes surface WASD/E bindings. The proving-scene-specific help and actual context routing use vessel attitude bindings. This is disclosed, not silently fixed after the production stop.
