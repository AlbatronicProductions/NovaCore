# Surface-point query cost proof

**Causal winner: evaluate expensive qualification stencils only when their prerequisite checks pass.** No regional cache, approximation, altered H, changed normal tolerance, new batch API or hidden reuse is needed. This workstream changed only external diagnostic files; production remains the lead's responsibility.

## Method and identity

Actual baseline is the existing Release query in the copied test-deployment Graphics DLL SHA256 `7F0E6592057AFB4F5243DF91E3529E900A9CB65DB6DA19D99E2AB16BF2F6568E`; Core SHA256 `51647A9464E160A97616A128DE6A9D4C63D23521F3FC03869E7D03DC8D714FC3`. Exact paths and diagnostic-source hashes are in provenance.json. These are the uncorrected surface-point-query candidate.

Performance compares actual baseline Query against an uninstrumented external StagedQuery. Its constructor takes the already validated production authority identity from actual TryAcquire; the proposed production correction adds no constructor, cache, loading or identity bypass. CountedQuery is used separately for H-call counts and instrumented responsibility attribution.

Two fresh processes use opposite pattern order and alternate AB/BA every measured iteration,128 samples per pattern. Coordinates are precomputed, then deterministically shuffled. Full result vectors compare equal before timing; returned results feed a checksum outside timing; warmed allocation count is zero. Default .NET tiering remains enabled. Each instrumented path receives300ms warmup before attribution, avoiding an initially misleading Tier0 Florida result. VERIFY B's actual-production-assembly controlled-JIT ABBA measurement supersedes these prototype timings for final acceptance.

The staged prototype matches **all564 permanent correctness points bit-for-bit as full PhysicalSurfacePointResult records**,527 Ready /37 NormalUnqualified, in both fresh processes. Equality includes status, normal, identity, height, position, direction and fine stencil distance. The new production source must separately pass its fingerprint/correctness suite.

## Causal responsibility

The old loop computes current(cardinal), cross45 and fine45/h3 stencils before testing its Boolean qualification. Each costs8 full H samples. If previous/current convergence or current one-sided qualification fails, cross/fine cannot cause acceptance. If current passes but cross fails, fine cannot cause acceptance. H is pure over immutable admitted data: skipping unused reads changes neither result nor lifetime. Every accepted comparison, final sample position and refinement update remains identical.

Measured eager H count is9+24*levels. Regional273→113(11 cardinal refinements); inland129→65(5); Florida33→33(1); unresolved south pole489→169(20,still unresolved). These counts explain the result across distinct contact locations, rather than relying on identical-input cache effects.

## Nonoverlapping attribution

These are **instrumented bucket times**, not uninstrumented recovery. Stopwatch boundaries and diagnostic decomposition perturb execution. No inclusive normal time is added atop H buckets. The remainder includes input/frame/normal arithmetic, comparisons, result construction, timer boundaries and loop overhead. Instrumented H reconstruction was checked against the original query result.

Regional eager273H:306.56us instrumented total =17.21us global sampling +151.53us regional sampling +110.32us natural physical composition +4.85us support/recomposition +22.65us remainder.

Staged113H:130.36us =7.31 +64.43 +46.04 +2.14 +10.45us, respectively. Actual uninstrumented medians appear in performance-table.md. Do not substitute the instrumented total for ordinary runtime cost.

Regional sampling includes relaxed-cube addressing, record selection, interpolation and coverage. This proof does not separate those internal parts. Four immutable neighbor-membership checks in Coverage are source-proven repeated work but **not measured dominant work**. Outside-regional inland has similar regional per-H cost despite no Coverage hit. Do not claim a neighbor cache as the winner or combine it with this correction. Source: PlanetaryLocalTerrain.cs374–396,487–494. Admission/readiness/manifest work already occurs in TryAcquire, not per point; warmed Query performs no I/O or locking.

## Real contact patterns and payoff

performance-table.md and final-forward.json/final-reverse.json contain median/P95/P99, H counts and allocations. Patterns: single, repeated-identical16, nearby4/8/16, four legs with1/2/4 contacts each, source-boundary footprint, Florida graded footprint, inland footprint and unresolved-pole16. Four leg centers are±2m east/north, subcontacts±0.15m; nearby spacing is0.6m. Coordinates are body-fixed physical offsets, independent of camera. Repeated-identical16 is retained for comparison with the historical blocker, not presented as16 distinct contacts.

Typical four-leg×4 regional footprint16 drops from2.822–2.987ms to1.272–1.306ms, recovering1.550–1.681ms across the two fresh processes. The repeated-identical blocker drops from4.215–4.474ms to1.743–1.792ms, recovering2.472–2.682ms. Florida16 remains unchanged within measurement noise (0.522–0.537ms versus0.523–0.539ms) because its first stencil already qualifies. Unresolved16 drops from7.162–7.723ms to2.438–2.643ms and remains explicitly unqualified.

Four regional leg points drop from0.683–0.700ms to0.311–0.321ms. That is material room for an initial bounded craft; it is not proof of arbitrary craft count or sampling cadence. Sixteen regional samples still cost approximately1.3ms per reevaluation, and unresolved cases cost more. No solver, callback/full-frame measurement or total future-game budget is claimed.

## Lifetime and setup

No context/cache is added. Both sides acquire the same production authority once from existing immutable CPU data. Fresh-process data loading measured approximately0.28–0.30s and acquisition26–30ms, including cold JIT/manifest path effects. External diagnostic object construction is recorded separately; the proposed branch correction adds none. Both variants pay the same existing loading/admission cost. Coordinate construction/permutation is outside query timing and is not proposed production machinery.

## Acceptance and adversarial boundaries

1. Preserve the complete564-output fingerprint and527/37 classification, not merely angular tolerance.
2. Preserve every acceptance test: one-sided, cross-basis, non-dyadic and outward/finite guards. Only avoid work already made irrelevant by failed prerequisites.
3. No memoization or mutable cache. Distinct and shuffled nearby queries must retain equality.
4. Actual corrected-assembly measurements and tests are required; external source-equivalent prototypes alone do not prove implementation acceptance.
5. This is unchanged normal qualification with cheaper rejection ordering, not a cheaper physical normal formula.

## Reproduction and storage

Proof.csproj references the current test-deployment assemblies. Rerunning after that deployment changes uses a different baseline unless the recorded old DLLs are deliberately supplied. provenance.json prevents silently claiming changed inputs are the old candidate. Program.cs, StagedQuery.cs and CountedQuery.cs retain a compact reproduction recipe. Retained final JSON is aggregate evidence, not raw timing arrays.

disposable-build-files.json classifies34 generated external files totaling1,455,607 bytes: diagnostic bin/obj plus2 superseded early summary files. None is a production runtime asset or permanent fixture. Automatic approval review rejected the command containing removal of those2 exact early JSON files with reason `blocked by policy`; no deletion workaround or retry was attempted. All34 remain for the lead's bounded manual-cleanup handoff. No repository production source was changed by this workstream.
