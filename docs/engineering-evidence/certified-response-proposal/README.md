# Certified isolated-contact response proposals — unbanked candidate

2026-09-10. **PASS — return UNBANKED for Project Control acceptance.**
Accomplishment: **Qualify certified isolated-contact response proposals at
provider-owned roots.** No milestone number is assigned.

[Current contract](../../certified-response-proposals.md) owns the equations,
applicability, lifetime and future application boundary. This package retains
decisive results and reproduction instructions. Budget: 32 KiB for this report,
[identity.json](identity.json) and [validation.json](validation.json).

## Baseline and production scope

Started clean on `main`, HEAD/main/origin/main/M14.10 at
`fce80a520240293a0373857986c607caf187eacd`. Working branch:
`codex/certified-response-proposal`. M14.1–M14.10 tag targets remain unchanged.

Two new internal production files own response arithmetic and private receipt
issuance/reading. Of 225 existing production C# files, 224 remain byte-identical;
the sole existing edit adds `partial` to the nested M14.10 Kinematics declaration.
M14.4/M14.5 admission, equations, transactions and all timeline/clock methods are
unchanged. No intent, executable vector, replacement state or mutation is emitted.

The scope is the existing complete single-feature natural-terrain specialization,
prescribed Earth, strict approach, frictionless restitution-zero response. It does
not extend source date/spin coverage or certify universal collision isolation.

## Independent physical proof

Permanent exact-rational fixtures cover centered contact (`k=1/2`, `j=6`),
off-center contact (`k=3`, `j=1/3`), asymmetric inertia and nonidentity attitude
(`k=17/6`, `j=9/17`), and exact normalization of the stored quaternion bits.
An independent rational rotation matrix supplies the oracle. Rational and
irrational analytical alpha fixtures are inclusion proofs, not invented provider
certificates. A correlated unit-normal family demonstrates safe box widening.

The authored body lever is exact source data, not a recovered uncertain root
lever. Large-lever arithmetic overflow, mass/inertia reciprocal floors, uncertain
approach, wide normal and unmet widths refuse. Small/large finite mass controls
qualify where representable. No numerical nominal value is substituted.

Independent before/after kinetic-energy arithmetic proves linked exact normal
closure and work-adjusted dissipation for stationary and moving surfaces,
including positive spacecraft inertial energy gain. The exact law is normal-only;
Earth is prescribed, so simulated two-body momentum conservation is not claimed.
The permanent binary64 `1/3` fixture retains residuals `-2^-54` / `+2^-53` and a
generic-normal direction-rounding counterexample. No applied-state guarantee is
inferred from an enclosure containing zero.

## Decisive real Florida witness

The actual acquired Earth/Florida terrain query and existing M14.9 root/M14.10
witness qualify. Fixture: 8 kg, principal inertia (2,3,4) kg m², authored lever
(1,-2,3) m, stored attitude (0,0,0.6,0.8), initial gap 0.5 m, speed -1 m/s and
radial acceleration -0.2 m/s². The independent root bracket in seed-relative
seconds is `[0.48001532256603240966796875, 0.480015337467193603515625]`.

The test uses the banked independent decimal Earth ODE/orientation oracle and
its guarded scale/derivative budget, then an exact-rational interval rotation
matrix and response over the ENTIRE independent bracket. It does not compare
only one sampled time, or use production interval arithmetic as its sole oracle.

| Quantity | Qualified enclosure |
|---|---|
| k, kg^-1 | [6.294770917159207, 6.294771118411056] |
| j, N s | [0.17208846935722585, 0.1720890762406888] |
| J root X | [-0.14237404218033572, -0.14237353915395642] |
| J root Y | [-0.05054262637812698, -0.050542447197718186] |
| J root Z | [0.08239949930193914, 0.08239978989022677] |
| Delta L body X | [-0.5323810179177274, -0.5323791329056656] |
| Delta L body Y | [-0.3475567513101953, -0.34755551810121726] |
| Delta L body Z | [-0.054244164079340566, -0.054243965253812386] |

Debug/Release produce identical interval bits. All components contain the
independent response. Full-width request `(1e-6,1e-6,1e-6,3e-6)` qualifies;
`(1e-30,1e-30,1e-30,1e-30)` returns Unresolved / RequestedWidth with no usable
receipt. Angular impulse is retained in all components.

## Authority, replay and red team

Private issuance consumes the actual checked M14.10 witness. Typed comparisons
bind the exact root, supplied qualification and response-width request without
boxed value equality. Checked consumption revalidates current source authority.
Same-owner immutable refinement is not globally revoked; tuple substitution
requires explicit reissuance. Independent replay reproduces bits but cannot
borrow capabilities. Twenty-four seeded request permutations remain deterministic.

Both issuance/read refuse changed engine, geometry, feature, graph, Earth/model,
query provenance, state, timeline-only changes, clock-only changes, force, torque,
mass and inertia. Test-only frozen-property fault injection uses disposable
fixtures and guaranteed restoration. Before/after checks cover state, revisions,
clock, events, all relevant histories, terrain identity and root data.

Three independent read-only reviewers closed PASS. Decisive challenges and fixes:

- **A, containment:** replace a stored nonunit normal singleton in one test with
  a rational unit-normal enclosure; restore the independent oracle's +10/+10000
  derivative guard margins. Production math required no correction.
- **B, physical law:** independently calculate kinetic energies and moving-surface
  work; paired angular impulse and exact normalized transpose survive.
- **C, application:** no midpoint, executable command or replacement state exists;
  rounding counterexamples keep application separate.
- **D, authority:** unwrap by-reference reflection parameter types in the factory
  guard and require the substituted coarse witness to qualify before testing its
  rejection. This prevents vacuous authority tests. Stale/refined checks pass.
- **E, banked boundaries:** M14.4/M14.5 source identity and focused gates pass.
- **F, future consumer:** root, actual source, widths and angular impulse survive;
  numerical realization, private execution and rotating reacquisition stay deferred.

All final validation below occurred after these test corrections.

## Allocation and bounded performance

Fixed plan: 32 warmups, 101 normal-runtime timing samples, eight separate checked
allocation calls per operation. The existing 1 MiB no-GC test helper isolates
allocation only; entry/exit failure remains fatal. Six operations each measured
exactly zero in both configurations. The shared byte[128] control detected 152
bytes in both. No profiler, production GC/tiering/PGO or runtime-policy change.

Release microseconds (Debug distributions retained in JSON):

| Operation | Median | P95 | P99 | Maximum |
|---|---:|---:|---:|---:|
| Request setup | 0.0 | 0.1 | 0.1 | 0.1 |
| Qualified proposal | 2.9 | 3.0 | 3.0 | 3.1 |
| Checked consumption | 0.7 | 0.7 | 0.7 | 0.7 |
| Width refusal | 2.5 | 2.7 | 19.1 | 20.4 |
| Stale refusal | 0.2 | 0.2 | 0.2 | 0.2 |
| Unsupported refusal | 0.2 | 0.2 | 0.2 | 0.2 |

Timer quantization is 0.1 microseconds; zero setup samples do not mean free work.
Request setup excludes upstream provider/root/M14.10 witness construction. The
width-refusal tail is retained without speculative attribution. Costs are per
certified root, not whole-frame or future operational-loop promises.

Receipt: 640 bytes; values: 128; request: 32 in tested x64 builds. Additional heap
workspace: zero. One checked source read and one response arithmetic pass per
qualification; one checked read per consumption; zero refinement calls/loops.

## Final validation

| Gate | Debug | Release |
|---|---|---|
| Full solution build | PASS, 0 warnings/errors | PASS, 0 warnings/errors |
| Full Simulation | 42/42 | 42/42 |
| Focused new response and M14.2–M14.10 Simulation gates | PASS | PASS |
| ReferenceFrames | 11/11 | 11/11 |
| Precision | PASS | PASS |
| Selected headless terrain/response tests | 7/7, 0 skips | 7/7, 0 skips |
| Six response allocation paths | 0 bytes each | 0 bytes each |
| Real-allocation positive control | 152 bytes | 152 bytes |

The seven headless tests cover surface query/stale snapshot (2), contact
generation, Earth-relative observation, Florida certification, root kinematics
and the new response proposal. JSON records every executed gate and filter.
No permanent gate was waived. No unrelated GPU/window campaign was run.
Manual visual acceptance: **NOT REQUIRED**.

Final `git diff --check`: PASS. Index empty; all baseline refs unchanged.

## KSA / BRUTAL boundary

Current installed KSA/BRUTAL/Bepu identities match the prior assessment; the six
retained matching source fingerprints were rechecked. The current path separates
`NarrowPhaseCallbacks` contact policy, `ConstraintSim` solving/readback,
`PhysicsBubble` nonfailed result publication and `Vehicle`/`VehicleUpdateTask`
authoritative application before rendering. **ADOPT** calculation/staging versus
application ownership. **ADAPT** transient evidence lifetime to checked root/source
applicability. **INTENTIONALLY DIFFER:** exact unknown-alpha isolated analytical
qualification cannot take iterative float solver values as physical authority.

Previously authenticated official history is reused narrowly:
[4866 prepass/contact state](https://discord.com/channels/1260011486735241329/1260112103134724146/1524556268151374037),
[4867 angular frames](https://discord.com/channels/1260011486735241329/1260112103134724146/1524584112093007943),
[4874 inertia](https://discord.com/channels/1260011486735241329/1260112103134724146/1524639263658999972),
[4878 contact measurement](https://discord.com/channels/1260011486735241329/1260112103134724146/1524677633143476276).
See [matching source provenance](../certified-root-contact-kinematics/identity.json).
No new broad historical reconstruction or private message was needed.

## Reproduction and retention

From `E:\NovaCore`, run `dotnet build NovaCore.sln -c Debug --nologo -v quiet`
and the Release equivalent. Run matching Simulation test DLLs once without
arguments and with each focused switch in validation.json; the new switch is
`--certified-response-only`. Run matching ReferenceFrames and Precision DLLs.

Run the matching Graphics test DLL with `--category=headless` and each retained
filter; the new one is `--test=Certified Florida isolated response proposal`.
The exact DLL layout is `tests/<project>/bin/<configuration>/net10.0/<project>.dll`.
Existing acquired Earth runtime assets/native headless dependencies must be
present. JSON records their deployed identity. The permanent test emits compact
oracle, response, cost, workspace and checked-allocation records. No observer,
profiler, special runtime environment or disposable patch is required.

Retain this report, identity/results JSON, current contract and permanent tests.
No task-specific scratch/comparison tree, raw stdout file, capture, temporary
reporter or copied observer was created. Disposable created/disposed/remaining:
**0 files / 0 bytes**. Normal build outputs remain in their usual locations.
No cleanup of unrelated evidence or assets was performed.

Return UNBANKED for Project Control; no next responsibility is started.
