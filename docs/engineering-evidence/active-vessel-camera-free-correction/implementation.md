# Correction contract

FREE is explicitly deferred because local detached inspection requires broader camera/navigation-frame ownership; see `architecture-decision.md`. HOME is unbound at native input. Managed action mask retains the same 88-byte ABI with F at bit 1 and the former value 2 reserved/ignored. Foreground-gated rising F remains camera-only. Existing celestial selection then host reset retain precedence. No KSA keybinding or speed constant is imported.

Removed only the front's newly added, incoherent Solar free bridge: private `_freeCamera`, `EnterFreeCamera`, its orbit/near-clip bypasses, host dispatch and acceptance expectations. Existing Core FreeCameraController, DebugCameraInput, SurfaceCameraAuthority and prior Solar surface navigation remain. No world-motion, speed, teleport, physics, package, launch control or renderer transport changes.

Core ownership remains:

| View | View target | Active vehicle | Environment |
|---|---|---|---|
| Florida vessel | SceneObject 201 / canonical material O | SRV-01 201 | Earth 6 |
| Earth | Earth | SRV-01 201 | Earth 6 |
| Other celestial | Selected celestial | SRV-01 201 | Selected celestial |
| F refocus | Same bound SceneObject 201 | Same SRV-01 | Earth 6 |

There is no player FREE state in this front, hence no false BodyCenter label standing in for NONE/FREE. Future FREE must define that target and its environmental frame explicitly.

F restores the existing fixed-size vessel orbit tuple. Orbit/zoom, copied publication generation/epoch checks, retirement fallback and FP64 camera-relative transport are unchanged. `CameraState.Mode.Free` and Solar `Free3D` are older low-level/presentation labels, not evidence that the deferred detached route remains available.

The permanent test replaces the incorrect HOME-free contract with reserved-action refusal plus 600 complete display steps: input, Solar advance, copied observation refresh, final terrain invariant and Solar update. It checks bounded camera-to-vessel distance while Earth travels over 300 km, correct target/active/environment identities, and canonical bytes unchanged. Earth/Moon/Sun/Mars, orbit, zoom, F, retirement, physical moving publication/COM changes, 100 m/1 km/100 km translation, pause/held updates and roots through 7e12 m remain covered.

Warmed follow/orbit/zoom/display and F refocus retain the exact-zero allocation gate. Discrete existing celestial Focus still allocates its known 48-byte handoff; this is reported separately, not hidden inside a universal zero claim. A positive control protects the measurement.
