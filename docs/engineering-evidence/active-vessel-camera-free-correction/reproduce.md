# Reproduction and evidence boundaries

Run from `E:\NovaCore` in PowerShell. No staging or banking is part of these scripts.

1. The failing source seal and request identity are in `entry-identity.json`. Original source copies are retained under `build/active-vessel-camera-free-correction/entry-source`. `reproduce-home.ps1` verifies and overlays the sealed failing Solar source into a disposable Graphics.Tests build, using the previous camera candidate's dependency/native assemblies. It does not reintroduce HOME in production. The scripted input/full callback trace is `home-probe.cs.txt`; numerical results are `home-failure.json`. Retain the entry-source copies and previous `build/active-vessel-camera/candidate` until Project Control releases them.
2. Build current correction with `./docs/engineering-evidence/active-vessel-camera-free-correction/build-candidate.ps1 -Configuration Debug`, then Release. Output is isolated under `build/active-vessel-camera-free-correction`. `validate.ps1` runs the 94-gate matrix. The final combined-action test refinement was rebuilt in both configurations and its static gate rerun; no production change followed the full matrix.
3. `build-observer.ps1 -Version candidate` creates a disposable Program overlay only. Baseline is the already retained observer of HEAD `45b1bbc`, built by the historical camera package's `build-observer.ps1 -Version baseline`. `capture-performance.ps1 -Repetitions 2 -Cadence fixed` runs six sequential renderer processes. Existing Florida/Earth/Moon/switch routes have matched 1/60 display and physical credit; new candidate vessel routes are characterized separately. FREE is absent. `consolidate.ps1` retains distributions, GC, indexed tails, transition/switch samples, log hashes and gate summaries.
4. `prepare-launcher.ps1` copies the uninstrumented Release candidate to the existing sample/launcher paths and verifies every copied hash. `closeout-identity.ps1` checks Git/index/remote/tag, KSA identity, exact correction scope, original historical bodies, unrelated evidence, runtime deployment and package budget.

Temporary builds, sealed source copies, raw logs, and both old/new measurements are retained; no automatic deletion. New package budget is 196608 bytes; old evidence remains within its prior 262144-byte budget. The first-playable-launch review is unchanged. Manual retest uses only `manual-route.md`, not diagnostic observers or the historical HOME checklist.
