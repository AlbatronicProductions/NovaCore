# Reproduction boundary

Historical recipe, not authorization for more runs. Use a fresh disposable tree; never patch the canonical checkout. Preserve the original test workload/warmup and zero-byte threshold. The retained observer is diagnostic tooling, not a replacement permanent allocation gate.

1. Archive banked commit `35c2693e0f5ca01c02c4cc5e73f68323e66ed8e7`: `Directory.Build.props`, `src/NovaCore.Core`, `src/NovaCore.EphemerisFormat`, `src/NovaCore.Simulation`, `tests/NovaCore.Simulation.Tests`. Extract into separate baseline/candidate roots. Overlay accepted Simulation source/tests on candidate, verifying the parent fingerprint manifest. No Git checkout/reset of the canonical candidate.
2. Copy `SasAllocationObserver.cs` into each disposable test project. Before the existing argument branches call `SasAllocationObserver.Prepare()`. Add `--sas-only` calling the entire original `SasSignFrameContinuityProofTests` and `--observer-selfcheck` calling `PositiveControl`, each returning afterward.
3. After the original final warmup, capture three `GC.CollectionCount` values in a value tuple, then `BeginWindow()`. Preserve the original beginning counter and 100,000-iteration loop. Read ending counter into `delta`; call `EndWindow(delta)` then `Report(delta, hash, gcBefore)`; retain `Check(delta == 0, ...)`. Reporting MUST remain in the retained non-inlined helper. The contaminated control instead inlined that helper's JSON statement into the proof function; it is not contract evidence.
4. Build `observer.cpp` as `profiler.dll`, using `observer.def`, MSVC x64 `/LD /EHsc /std:c++17 /O2`, link `ole32.lib`. Required headers: dotnet/runtime tag v10.0.11, `src/coreclr/pal/prebuilt/inc/{corprof.h,corerror.h}` and `src/coreclr/inc/{cor.h,corhdr.h}`. The source derives from the previously retained spacecraft-translation allocation profiler. Do not retain SDK/header copies permanently. Use a supported matching runtime; the counter decoder must succeed and a real byte-array control must register before trusting negative observations.
5. Build disposable Simulation tests Debug/Release. Adapt retained `run.ps1` to the new root with `candidate`, `baseline`, `observer` children. `-FxVersion 10.0.11` selects only that child runtime; `-Plain` omits profiler activation. Do not alter default production runtime settings. Define a new bounded plan with Project Control before further execution.

`runs.json` records the actual binaries, runtime and process identity for this investigation. `decisive-excerpts.txt` preserves positive control, contaminated object/stack evidence, its corrected counterpart and the earlier lunar failure. Failed out-of-callback metadata queries printed by the observer are not resolved names; use names captured inside callbacks only. Profiler callbacks perturb timing; never use these logs as replacement performance numbers.
