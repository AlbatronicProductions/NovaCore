# Frozen ordinary retained-step feasibility plan

2026-09-15. Before execution. Scope: standalone pinned BEPU experiment,
no production/permanent-test edits, no canonical publication, live resource
debit, actuator advancement, old qualification, or changes to A/C evidence.
Retained evidence budget256KiB. One disposable root:
E:\NovaCore\build\powered-contact-ordinary-step-event-closure
contains only this project's rebuildable bin/obj. Exact cleanup will be
reported with counts and verification. No other output path is authorized.

## Inputs / mass / timestep

Recover chronology.py/control(kind='average') verbatim in mathematical
definition: d=hp/H; F_eff=dF; tau_eff=d(r_engine cross F); source mass
m0=8+fuel held during H; fixed dry I=(2,2,2); H=1/60 seconds. Exact rational
hp=fuel/q, hc=H-hp. q and fuel are the prior imported binary64 rationals.
Use ONLY source mass, because it is the recovered control; no mass sweep.
Maximum mass relative error<=1.628e-5; rigid-model support error<=1.597e-5Ns,
propulsion velocity mass error<6.782e-7m/s. These do not bound BEPU error.

Prepare integrated wrench products in exact rational arithmetic; project
full-H acceleration (F_eff/m0 plus gravity), angular acceleration tau_eff/I,
and source inverse mass once to float. BEPU sees float(1/60), exactly
0.01666666753590107s; never hp. No minimum dt. This is ordinary numerical
60Hz proof, not the public canonical clock admission API. One exact ledger
packet and separate private physical result; no owner publication implemented.

## Actual world

One retained Simulation per witness, one Box(2,1,1), slab Box(16,2,16) topY0,
body initially center(0,.5,0), dry inverse inertia(.5,.5,.5), source mass.
Pinned2.5.0-beta.29 DLLs and repository integrity checks.8 velocity iterations,
1 substep, mu.5,30Hz critical spring, recovery2m/s, margin.01m, no sleep.
120 actual gravity-only ordinary warm steps. Keep resulting pose/normal
velocity/cache; set only initial tangentX and yawY rates once as fixture
setup (1m/s and/or.12rad/s per prior case). Never reset pose or edit cache.
Capture actual initial geometry/velocity; discrepancies count against bars.
This raw numerical fixture uses I2 and is NOT the uniform-box M14.18 source
admission contract. No claim that unmodified M14.18 admits it.

Observe before call, after normal collision refresh/before solve, substep
end, and final endpoint. Read-only seven-impulse extractor, features/handle,
actual material, contacts, float bits. Exactly one ordinary witness step;
no extra collision refresh or custom solver. No basis/duration/load/D work.

## Sequence / stop

Fresh process per row, matching preparation where paired:
1 centered no-engine baseline,2 low-thrust;3 sliding no-engine baseline,
4 near-unloading;5 yaw/sliding no-engine baseline,6 off-COM;
7 late-exhaustion;8 ordinary mid-event (prior friction40N);
9 tiny-positive;10 tiny-zero. Original ledger inputs are reused unchanged.
Baselines use corresponding source mass held, no fuel burn/engine impulse.
Do not subtract baseline error. All measured errors are absolute vs oracle.

Stop on FIRST physical/admission failure. Only allowed diagnostic followup:
replay that SAME row with16 then32 target iterations, still120 preparation
steps at8 iterations and1 substep, identical input/material/geometry/bars.
These are convergence classification, not acceptance retries or policy fixes.
No remaining witnesses, determinism or timing after failure. No new mass,
spring, preload, geometry, solver or cache mechanism may rescue it.

If all physical observations pass, repeat strongest passing off-COM row
twice and compare ledger, body, feature/order and cache bits same machine.
Cost only after full correctness (including work) qualifies: one Release
process,128 warm and256 measured ordinary complete calls with already
prepared immutable input; report preparation separately if measurable.
No claim of identical boundary to A's numerical-only preparation cost.

## Reference / bars

Accepted Fraction polynomial chronological reference, reused read-only;
no C implementation inside candidate. Centered/slide/yaw baseline uses
independent rigid constant-mass coast equations. Same primitive SI bars
as preceding proof: linear velocity vector error<=.06m/s; angular vector
error<=.001/(sqrt1.5*H); displacement vector error<=.001m;
orientation angle error<=.001/sqrt1.5; support/tangent impulse error<=.48Ns;
twist/moment error<=2*.001/(sqrt1.5*H); penetration<=.001m;
work error<=.07848J. Support must remain present with four contacts and no
material false separation. Do not require equal individual pad impulses:
oracle uses explicit equal-stiffness null-mode, BEPU uses its own compliance.

Endpoint penetration comes from eight transformed corners, not stale depth.
Compare source-to-end displacement against oracle; source compression and
tilt are separately bounded by the same physical scale, never reset away.
Normal/roll/pitch oracle rates are zero. All other axes also count in errors.

## Work / observability

Native seven impulses do not uniquely expose chronological friction work.
Record midpoint-slip and endpoint-slip impulse-work PROXIES, separately
from reference integral, and raw inputs. Do not call either measured work.
Without a justified enclosure or actual work observation, friction-work
qualification remains UNAVAILABLE and full correctness/PASS/cost is barred.
No new profiler, custom row solver or patched dependency to invent work.
Final saturation/zero-row observations are not internal clamp-event counts;
those counts are UNAVAILABLE with this observer.
