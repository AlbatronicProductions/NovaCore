# Bounded performance/payoff plan — declared before execution

2026-09-15. Evidence only. No production/permanent-test/solver changes. Preserve the
accepted physical and ordered discrete friction-work witnesses; do not rerun them.

## Current contract

Current `docs/NOVACORE_CURRENT_STATE.md` retains preferred 6.67–6.94 ms frame
headroom; 8.33 ms terrain, 11.11 ms preferred fully featured game and 16.67 ms
Ultra sustained targets have different scopes. Do not infer frame FPS here.
`ENGINEERING_RULES.md` requires timing under ordinary runtime behavior, separate
checked allocation measurement, and exact zero for the applicable warmed work.
The present ticket explicitly requires zero candidate-specific managed bytes.

NO NUMERIC SUBSYSTEM BUDGET EXISTS for this isolated ordinary event mapper.
M14.18–M14.20 complete-path 0.05/0.10/0.25/0.50 ms ceilings are preserved for
their respective qualified step/export/publication/host boundaries; this probe
does not implement those integrated responsibilities or inherit a new local bar.

## Boundaries and allocation-first stop

- A: ordinary retained native BEPU step, same fixture, H, eight iterations/one
  substep, no event mapping. Exclude cold world construction/preparation.
- B: immutable exact event-input read, current rational duty/effective wrench/
  source-mass preparation and projections, then the same ordinary native step.
- C: that event-specific preparation without a BEPU call.

First build ONE small Release allocation-only C harness. Use the existing off-com
input, the EXACT Rational type extracted from the retained parent Program.cs,
and link the unchanged permanent OrdinaryAllocationMeasurement helper. No
replacement arithmetic, precomputed duty/wrench or friction-work dispatch.
Parsing, immutable input/constants, reference checks and JSON reporting are cold.
Read immutable exact inputs on every mapping call. Perform hp/H, effective force,
torque, mass/inertia divisions, gravity adjustment and backend float projections
inside the window. Repeated mapping is not live fuel debit or canonical commit.

One fresh Release process: 128 Map calls for warmup; unchanged helper byte[128]
positive control; one checked immutable-read/return control; then ONE measured
Map call using the checked 1 MiB no-GC helper. Consume and validate its output bits
against the retained off-com prepared fields after closing the window. The one
call isolates an exact per-operation byte count without exceeding reservation.
No profiler/tiering/PGO/runtime changes, retries, GC traces or subtraction.
Report cheap same-thread GC counts around the window, outside it. Entry/exit
failure is a hard technical failure. Setup allocation is measured separately
and is not a retained-storage claim.

FIRST warmed candidate-specific nonzero count: STOP and REVISE. Do not optimize,
run A/B/D, collect timings, add repetitions, or start allocation attribution.
This explicit stop takes precedence over the three-process performance PASS bar.

Only if C is zero may the main timing plan be finalized before execution:
three fresh Release processes, bounded thousands of samples, normal runtime,
same retained physical regime in A/B. Repeated powered steps may leave the slab
or change event/resource history, so a valid matched lifetime must first be
specified without silently resetting pose/cache or claiming live ledger repeats.
No such world/timing implementation is authorized as a response to allocation.

## Evidence budget and controls

New retained files only in this performance-payoff child: plan, small harness,
reproduction script, one compact result, identity and final report (target <100 KiB).
All build/bin/obj/raw output only under:
`E:\NovaCore\build\powered-contact-ordinary-step-performance-payoff` (target <5 MiB).
No new comparison tree. Inventory exact files/bytes and print reviewed cleanup
plus non-destructive verification. Prior friction-work scratch is currently absent;
do not retry deletion. Preserve all 2,450 starting files and all 65 tag objects.

Independent read-only verifier reviews boundary, source equivalence, result and
the stop decision. Candidate A evidence comparison is NOT DIRECTLY COMPARABLE
to native-world B; no Candidate A run/retirement. Reuse accepted KSA build-5438
lifecycle findings only; make no new KSA allocation/performance rationale claim.
