# One bounded evidence-mapper allocation revision

2026-09-15. Preserve the prior performance report and all starting files. No
production/permanent-test changes, solver work, new KSA mechanism, physics change,
Candidate A retirement or banking. Output only this child and new scratch
`E:\NovaCore\build\powered-contact-event-allocation-closure`. Retained target
<100 KiB; allocation-stage scratch target <5 MiB. Prior scratch is currently absent;
no prior deletion retry. Inventory and print exact cleanup/verification at close.

## Boundary validity, before attribution/correction

The required hot outcome is exact-ledger-to-effective ordinary input preparation.
The existing arbitrary-precision Rational TYPE is evidence-only, not a committed
production design. Removing required duty/wrench/mass work from the hot boundary
would be invalid even though removing its allocating representation is allowed.

| Operation | Classification | Current boundary / requirement |
|---|---|---|
| Rational construction/reduction | EVIDENCE-ONLY | Value wrapper used by current hot map; not mandated ownership |
| Rational division | EVIDENCE-ONLY | Concrete implementation; required quotient semantics remain |
| Exact duration/input read | PRODUCTION-REQUIRED | Immutable exact authority; conversion must preserve tick/second units |
| Event fraction hp/H | PRODUCTION-REQUIRED | Recompute from source values; no event-answer cache |
| Effective force and torque | PRODUCTION-REQUIRED | Preserve weighted product and sign/lever semantics |
| Source-mass division/projection | PRODUCTION-REQUIRED | Preserve source-mass treatment and output contract |
| Binary64/FP32 projection | PRODUCTION-REQUIRED | Six final doubles materialize in old Value calls; compare those and floats |
| Input parsing/constants | SETUP | Already outside C |
| Reference comparison | REFERENCE/ORACLE | Outside C; no live debit/publication is claimed |
| Formatting/logging | DIAGNOSTIC | Outside measured counter; helper reports after closure |
| Delegate/lambda dispatch | SETUP / DIAGNOSTIC | JSON selection/reporting only; direct static Map has no delegate |
| Prepared record struct | PRODUCTION-REQUIRED value output | Returned unboxed, no heap capability |
| Input holder/JSON/report objects | SETUP / DIAGNOSTIC | Never constructed in C |

KSA classification: **NO GENUINELY EQUIVALENT KSA EXACT-EVENT MAPPER
RESPONSIBILITY**. Reuse the accepted exact-resource/subnormal-contract distinction,
not a new claim about all KSA physics. Use current banked NovaCore primitives;
no fresh equivalent responsibility is introduced.

## Allocation attribution plan

Extract the old Rational and Map source without arithmetic edits. One fresh
Release attribution process, 128 warm calls for original and scope-split maps.
Use unchanged checked 1 MiB measurement helper and its positive control. Measure
the original complete map once, then one instrumented map with allocation reads
around its 18 arithmetic/projection scopes. Allocation-counter reads write only
preallocated/stack value storage; no delegates or diagnostics inside scopes.
Measure an empty-scope read control. Require the scope sum and outer measured
delta to equal the original complete-map delta and all output bits to match.
Inspect actual runtime IL/backing-field types cold if needed to explain arithmetic
allocation. No profiler/tracing unless these cheap observations cannot assign cause.

## Reuse gate / one correction

Read actual PropellantInteger, PropellantDuration (ticks), FinitePropellantSegmentation,
PoweredFlightNumerics.TryRatio/TrySeconds and PoweredBinaryScale.MultiplyDivide.
No production change for internal accessibility: link the exact existing source
into the diagnostic assembly. TryRatio rounds to 53 bits; retained Rational.Value
truncates operands to 55 bits before division. Do not assume semantic identity.

After attribution, predeclare ONE coherent B reuse map before running it. Keep
complete weighted products out of ranged binary64 until existing scale projection.
Raw exact-input import/constants may be cold; duty/effective inputs may not.
Compare old/new six binary64 projections plus six FP32 outputs and exact ledger
authority. No tolerance, witness special case or second correction on mismatch.
Any mismatch / missing required production API / remaining warmed allocation:
STOP and return to Project Control, with no timing campaign.

Only after semantic identity: unchanged positive/read controls; 128 warm calls,
three isolated single calls and one 128-call measured window, exact zero each;
same-process determinism, no hidden state, independent verifier. Stop first failure.
Only after all pass resume the previous A/B/C performance plan automatically.
No timing/lifetime redesign is authorized as an allocation rescue.
