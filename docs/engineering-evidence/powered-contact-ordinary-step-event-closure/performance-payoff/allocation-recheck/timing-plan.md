# Frozen A/B/C campaign completion — before timing execution

Allocation recheck and single corrected off-COM native witness both PASS.
This completes the earlier plan's explicitly deferred matched-world lifetime.
No arithmetic or backend mechanism change.

Three fresh Release processes, sequential, one build before all runs. Each:
128 warm A/B pairs, then 1,024 measured A/B pairs; A/B order alternates by sample.
Each member owns one cold-created native world prepared with the original 120
ordinary steps, then receives the original off-COM initial rates. Exactly one
target ordinary interval is measured per world. Cold construction and disposal
are outside timing. There is no pose/cache restore, warmed-world cloning or
repeated live event/debit claim. Independent repeated fixtures keep the measured
contact regime identical rather than flying off the finite slab.

Cold fixture creation and checks run A then B; only the timed order alternates.
Disclose this process-history asymmetry. Differences of independent A/B medians
are descriptive, not an exact causal mapper cost; use B's inner preparation
timestamp and C for direct preparation attribution.

A: the same fixed-wrench six FP32 inputs are prepared outside timing; measure
input binding and one ordinary retained-world step, with no event mapper.
B: read immutable FixedInput and run the unchanged retained corrected mapper;
bind FP32 acceleration/dt and step the equivalent retained world.
C: 128 warm map calls, then 1,024 separately measured map-only calls.

Mass/inertia is immutable over this single-step fixture. Its native inverse mass
is installed at cold construction as in the original witness. B recomputes all
six outputs, including inverse mass, and the result is checked against the actual
native body's fixed inverse mass after the window. There is no mass-update,
body-reimport, live resource-debit or canonical publication cost claim.

All A/B sources and endpoints must match the retained off-COM snapshot bits,
contact rows/features and impulses outside timing. Use original native callback,
integrator and read-only extractor source. A substep counter is attached cold;
exactly one observed substep and one explicit Timestep call, configured fixed
eight iterations. No scheduler override or custom solve. Internal per-iteration
execution is not separately instrumented; do not fabricate an observed count.

Normal runtime timing: no no-GC boundary, no profiler, no tiering/PGO changes,
no priority/affinity change, no synchronization added. Timestamp total and inner
map/read versus input-binding/native portions for attribution, with identical
timestamp layout in A/B. No overhead subtraction. GC collection counts sampled
outside the timed window; report their deltas and top ten observations per arm.
Scheduler/JIT/GC cause is not inferred from a slow sample alone. Cold fixture
time/allocation is reported separately and can influence normal process history.

After timing each process: separate equivalent fresh A/B fixtures for one checked
allocation window each, and a 128-map C series, using the unchanged qualified
helper. Each must be exact zero; no repeat or averaging on failure. Mapping
qualification's original controls/singles remain the prior one-process result.

Nearest-rank P95/P99, conventional middle-pair median and absolute maximum.
Retain summary, top ten indexed tails/components/GC, count and min/max source
checks, not thousands of raw measurements. At most three processes; stop first
material failure. No isolated timing ceiling is invented; judge absolute cost,
baseline delta, tails, zero allocation, work and structural payoff. Candidate A
historical numerical D+12 boundary is not directly comparable to this native
one-step boundary. No Candidate A rerun and no whole-engine FPS inference.
