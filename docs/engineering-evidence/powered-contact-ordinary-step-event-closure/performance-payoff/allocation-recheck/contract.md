# Accepted diagnostic contract and bounded execution plan

2026-09-15. Project Control accepted projection-contract classification B and
authorized this separate active diagnostic contract. Historical allocation-closure
and projection-contract files remain byte-identical. No production/permanent-test
change, new mapper, numerical primitive, cache or solver policy is authorized.

Preserve the exact ledger, banked fixed-workspace semantics, all six consumed
FP32 bits, native prestate/order and physical results. Only old-vs-new binary64
identity for ax/ay/wx/wy ceases to be a failure criterion. InverseMass/H retain
their former double check. Repeated corrected outputs must also be deterministic
against their own initial corrected double/float result.

The active source is mechanically derived from retained Correction.cs. Its
FixedInput, Import, Ratio and Map bodies are unchanged. Map visibility may be
internal for reuse by the native/timing diagnostic. No alternate arithmetic.
All input conversion is cold representation preparation, not cached event answers.

Allocation: ONE fresh Release process, 128 map warmups; unchanged checked 1 MiB
helper and byte[128] positive control; one immutable input-read control; three
isolated warmed map calls; one 128-call series. Every raw count is reported.
Expected controls: immutable read 0, byte[128] 152 on this x64 runtime; each map
window exactly 0. No retries. First nonzero or failed semantic gate stops work.
SDK 10.0.303/runtime 10.0.12, ordinary tiering/PGO, no runtime overrides/profiler.

Only after allocation PASS: one retained off-COM physical witness, original
world preparation/callbacks/order/dt/8 iterations/1 substep. Substitute only the
same corrected mapper's inputs; compare the exact ledger, six FP32 inputs,
source and endpoint bits, contact rows, cache/impulses and stages to off-com-8.json.
No full correctness campaign. Unexpected native difference stops the campaign.

Only after native PASS: finalize the frozen A/B/C timing plan before execution.
The earlier performance-payoff plan explicitly deferred definition of a matched
retained-world lifetime until the allocation gate passed. Three fresh Release
processes; no invented isolated subsystem threshold. No Candidate A rerun.

New retained evidence target <150 KiB excluding compact timing summaries if
needed. Scratch only E:\NovaCore\build\powered-contact-allocation-recheck,
target <10 MiB. No old scratch deletion retry. Exact counts/manual cleanup at
closeout. KSA no-equivalent exact-event-projection finding remains accepted;
no new KSA-equivalent mechanism is being designed.
