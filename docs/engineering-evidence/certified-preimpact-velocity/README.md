# Pre-impact velocity tuple — unbanked candidate

Accomplishment: **Qualify pre-impact velocity tuples at provider-owned contact roots.**
Baseline: banked M14.11 main/tag `556ca77af88257b5727561709c3a091aaec8cee7`;
implementation `3813791380ad035c308bb93cc404102a659d812d`.
Branch: `codex/certified-preimpact-velocity`. No milestone number is assigned.

The [current contract](../../certified-preimpact-velocity.md) owns physical meaning,
checked lifetime, numerical limits and future consumer. This package retains only
decisive validation, measured costs, identity and reproduction. Budget: 32 KiB for
this report and identity/results JSON. **PASS — return UNBANKED for Project Control
acceptance.** [Identity](identity.json) and [final results](validation.json) record the
tested source and exact executed gates.

## Decisive results

Both full solution builds passed with zero warnings/errors. Full Simulation passed
43/43 groups in Debug and Release. New analytical qualification, M14.2–M14.11 focused
gates, ReferenceFrames 11/11, Precision and eight selected headless query/contact-chain
tests passed in each configuration, with no skips. All 46 declared test processes
completed successfully; no failure/retry/forensic follow-up occurred.

Independent analytical fixtures cover zero/nonzero force, canonical epoch 0/-2 s,
rational/irrational analytical alpha, exact-rational affine COM extrema, full-bracket
material-relative velocity and representation/refusal controls. The independent
root-axis ODE/orientation calculation does not reuse production inverse rotations.
The 1/3 impulse's nearest residual is -2^-54; its actual final velocity-addition
residual is -2^-53. Large/tiny increment absorption and direction rounding remain
explicit limitations, not new response policies.

Actual Florida constant-force root: [0.48001532256603240966796875,
0.480015337467193603515625] s. Exact-rational COM and independent whole-bracket W
oracles are contained. COM maximum full width is 1.2624223018065097e-6 m/s;
relative maximum is 2.957945369530535e-6 m/s. The zero-force fixture also qualifies
with moving Earth, maximum COM/relative widths 7.275957614183428e-12 /
1.1865428126700552e-6 m/s. Requests (1e-5,1e-4) qualify; (1e-30,1e-30) returns
Unresolved/RequestedWidth and no usable tuple. All six bounds are bit-identical
between Debug and Release. JSON retains every component and the independent oracle
error budgets. The matching M14.11 exact response remains unchanged, including
nonzero angular impulse in all three components.

Default/mixed-owner/request/qualification substitutions, changed source authority,
immutable refinement/reissuance, deterministic request permutations and full
state/timeline/clock/history non-mutation passed. Confined mass/inertia fault injection
was restored in finally. No production mutation API was added.

Release microseconds (Debug and timer resolution retained in JSON):

| Operation | Median | P95 | P99 | Maximum |
|---|---:|---:|---:|---:|
| Request setup | 0.0 | 0.1 | 0.1 | 0.1 |
| Qualification | 84.7 | 91.2 | 93.0 | 94.6 |
| Checked consumption | 0.8 | 1.2 | 1.4 | 2.6 |
| Paired response consumption | 1.3 | 1.4 | 2.5 | 2.7 |
| Width refusal | 84.8 | 93.8 | 100.0 | 104.8 |
| Stale refusal | 0.2 | 0.2 | 0.2 | 0.2 |
| Unsupported refusal | 0.2 | 0.2 | 0.2 | 0.2 |

Every measured operation allocated exactly zero in both configurations, with successful
checked entry/exit and 8/8 calls. Positive control detected 152 bytes in both.
Timer resolution is 0.1 microseconds: zero setup samples are not free-work claims.
Tuple/value/request sizes are 592/96/16 bytes; additional heap workspace is zero.
Qualification includes motion enclosure work; upstream provider/root/kinematics
construction is excluded. These are per-root costs, not whole-frame promises.

Independent red teams passed source/authority/frame/scope, analytical containment
and real Florida whole-bracket containment. Strongest challenges were double-counting
Earth rotation, treating interval overlap as proof, and charging impulse-only residual
to final state. The implementation/oracles explicitly avoid all three. A test-only
definite-assignment issue was corrected during static review before either full build.
No production correction was required after validation began.

Of 227 existing production C# files, 226 are byte-identical. The remaining file's
Git diff adds only partial. Two new production files and two new test files are
retained; runner changes only register the new gates. Banked refs and the empty
index remain unchanged. Final Git whitespace checks pass.

## Source boundary

Two new internal production files add request/results, private tuple issuance/read
and the bounded motion operation. The only existing production edit makes
`FloridaContactMotion` partial. Its existing calculations and admission are unchanged.
The new method consumes the checked witness's root enclosure, uses cached authoritative
acceleration for COM velocity and rotates the existing full derivative back to root.
It does not add another Earth angular correction or select a root time.

There is one checked read and one bounded motion evaluation per qualification, one
checked read per consumption, and no refinement calls or heap workspace. Source
identity checks protect all earlier methods, public time, event ordering and canonical
response behavior. Exact comparison helpers from the banked response receipt are
reused without changing their contracts or invoking response arithmetic.

## Declared validation plan

Before execution: Debug and Release full solution builds; focused new Simulation
and real Florida headless tests; one fresh full Simulation process per configuration;
focused M14.2–M14.11 Simulation regressions; ReferenceFrames and Precision;
seven existing headless query/contact-chain regressions; positive allocation control;
Git whitespace/identity checks. Stop on the first gate failure. No retries or allocation
forensics, no GPU/window campaign.

Per-operation performance plan: 32 warmups, 101 normal-runtime timing samples,
eight separate checked allocation calls. Existing 1 MiB measurement isolation is
used for allocation only. Exact zero remains required; entry/exit failure is fatal.
The shared deliberate `byte[128]` allocation remains a separate positive control.
No profiler, GC/runtime, tiering or PGO configuration changes.

## KSA / BRUTAL provenance

Installed KSA `2026.9.7.5402`, Bepu and BRUTAL numerics hashes, and six retained
matching source fingerprints were reverified against the
[existing source provenance](../certified-root-contact-kinematics/identity.json).
Assembly metadata lives under the cached source's `Properties/AssemblyInfo.cs`.
Current `VehicleUpdateData.Prepare` establishes prepared kinematics; solver work
produces staged state; `VehicleUpdateTask.ApplyResultsToMainThread` publishes ready
results. **ADOPT** preparation and guarded application ownership. **ADAPT** prepared
kinematics to checked same-root vector evidence. **INTENTIONALLY DIFFER** from taking
float solver output/clamps as proof of NovaCore's exact-root physical law: the banked
rounding counterexamples demonstrate why final-state qualification remains necessary.

Authenticated official history was inspected in the preceding assessment in this
session: [4866 preparation](https://discord.com/channels/1260011486735241329/1260112103134724146/1524556268151374037),
[4867 angular frames](https://discord.com/channels/1260011486735241329/1260112103134724146/1524584112093007943),
[4874 inertia](https://discord.com/channels/1260011486735241329/1260112103134724146/1524639263658999972),
[4878 contact measurements](https://discord.com/channels/1260011486735241329/1260112103134724146/1524677633143476276).
No proprietary source was copied into the candidate and no new dependency was added.

## Reproduction

From `E:\NovaCore`, build `NovaCore.sln` in Debug and Release. Run the matching
`tests/NovaCore.Simulation.Tests/bin/<configuration>/net10.0/NovaCore.Simulation.Tests.dll`
without arguments and with `--certified-preimpact-velocity-only`. The banked focused
switches remain registered in that runner. Run matching ReferenceFrames and Precision
test DLLs.

Run the matching Graphics test DLL with `--category=headless` and
`--test=Certified Florida pre-impact velocity tuple`. Existing acquired Earth runtime
assets and matching headless native dependencies must be present. Permanent tests
emit compact oracle, bounds, identity, allocation and timing results; no temporary
instrumentation is required. Exact executed gates and result summary belong in
`validation.json`.

## Retention

Retain this report, identity/results JSON and permanent tests. No task-specific
scratch tree, raw stdout file, observer, copied build tree or capture is required.
Normal build outputs stay in their standard locations. No unrelated cleanup is
authorized or performed. Return UNBANKED for Project Control; do not start numerical
post-impact selection or private execution.
