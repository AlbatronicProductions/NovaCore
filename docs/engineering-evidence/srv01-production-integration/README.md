# SRV-01 production integration and exhaust correction

2026-09-17. **PASS. UNBANKED. STOP FOR PROJECT CONTROL.**

Branch: `codex/srv01-production-integration`. HEAD/main/origin/main/remote main:
`09463ec6c323daf233205cb08306d38611c22e6e`. All 66 historical tags are unchanged.
No staging, commit, tag, merge or push. Thirty preexisting local legal-review
files remain outside this candidate. Final identities: [final-identities.json](final-identities.json).

## What happened and why it matters

The reusable seven-part spacecraft, explicit sixteen-jet physical topology,
shared exact resource accounting, canonical propulsion and copied presentation
were integrated and qualified. The original manual acceptance was then withdrawn
because no main exhaust appeared. Numerical success did not qualify the visuals.

The proven native defect was lifetime/capacity ownership: READY allocated 3,056
bytes for camera plus 37 craft objects; the first additional plume began exactly
at byte 3,056, outside the mapped/described range. Managed capacity 54 was not
communicated to native storage. GPU consequences were undefined, not proven zero
reads. The original opaque cone also failed the requested visual-quality bar.

The correction prepares 54 objects / 4,416 bytes, preserves capacity across
swapchain recreation and rejects overflow before copying. A typed volume route
uses reusable original proxy geometry and realized nozzle state. Terminal hold
shows no exhaust while preserving copied canonical state/history. These two
corrections remained intact throughout the subsequent KSA-method review.

Project Control required method convergence after an initial generic-volume
visual PASS. The final renderer now adapts directly inspected current KSA's
nozzle-local density volume, emission/extinction integration, importance sampling,
advected noise, opaque-depth clipping and weighted transparency. The user personally
checked this revised build and answered **PASS** to the explicit final checklist.

## KSA responsibility and method

Actual installation: `E:\Kitten Space Agency`, build
`2026.9.10.5438+c9291b5dd3347e847020c8fb3dd11dfce6a728e9`.
Direct DLL, shader, configuration and proxy inspection is pinned in two manifests.
Direct authenticated official #live-changelog messages are linked in
[exhaust-correction.md](exhaust-correction.md). This is not a review based only on
NovaCore summaries. Fourteen inspected installed files rehash unchanged; KSA writes 0.

- **ADOPT:** actual-nozzle ownership, authored exit/gimbal attachment, realized
  output, copied simulation time, once-per-display-frame rendering, independent
  optical emission/extinction and depth-clipped integration.
- **ADAPT:** original vacuum density/visibility profile, independent noise/core/outer
  appearance, full-resolution importance integration and weighted OIT resolve;
  stable sixteen-jet identity; strict immediate shutdown with no stale tail.
- **INTENTIONALLY DIFFER:** NovaCore exact resource/event/canonical publication
  authority remains separate from KSA's resource/controller policy. No new generic
  allocator, gas solver or continuous throttle is inferred from appearance.

[exhaust-method.md](exhaust-method.md) maps every requested mechanism responsibility
to current source and the admitted adaptation. Radius/visible-length use the vacuum
specialization of the inspected expansion/density construction, not an arbitrary
Gaussian cone. All shaders, proxy geometry, colors and procedural noise are
independently authored; no KSA expressive assets were copied.

## Physical and visual authority

[gate0.md](gate0.md) preserves the initial RCS ownership decision and authoring.
Four reusable four-horn blocks supply 16 independently identified jets, plus the
main engine: 17 consumers, two species stores and 34 typed feed edges. The main
remains 600 N; each jet 22.5 N. Binary full-thrust/off is the available authority.

The unchanged four accepted GLBs are visual only. The physical design digest is
`AC23C15AE52ADC5E8E836BD954D9084B10A2699CC01F0A6906A992724EA67FCF`.
Mass, COM, tensor, gimbal and resource laws are never inferred from graphics.
The nominal RCS COM versus visual-housing limitation remains disclosed in gate0;
no manufacturing-density realism is claimed.

Twenty-five pre-correction Simulation source/test identities remain byte-identical.
The final two-second runtime reaches frontier/revision 128 at tick 2,000,000:
fuel 29.9521484375 kg, oxidizer 44.92822265625 kg, mass 704.88037109375 kg,
COM x 1.0418789203343042 m. Different final host overshoot debts are retained,
not discarded or used to change the fixed physical sequence.

## Implementation owners

- Native prepared submission storage, specialized visual-mesh ABI/route,
  conditional plume accumulation/resolve and original production shaders.
- Interop passes prepared capacity and validates the visual-mesh layout.
- StockAssemblyDevelopmentScene maps the main and sixteen jet tuples to copied
  endpoint transforms, gimbal, stable IDs and committed-time/exhaust-speed data.
- The sample supplies one native frame after bounded servicing; cold resources
  are shared and reused. Startup cost is not host credit.
- Permanent native canary and actual production-SPIR-V GPU tests, plus managed
  application presentation/identity/allocation tests. No renderer-clock physics.

All 55 candidate source/test/asset/build paths are listed and hashed in
final-identities.json. Its correction-specific before/after list separates the
renderer repair from the preexisting SRV-01 Simulation implementation. No public
README/license was edited and no historical banked file was committed.

## Validation

| Gate | Result |
|---|---|
| Full solution, final Debug / Release | PASS, zero warnings/errors |
| Native capacity / pre-copy refusal / recreation | PASS Debug / Release |
| Production exhaust SPIR-V and OIT/depth/lifetime | PASS Debug / Release, zero Vulkan validation errors |
| Final SRV-01 ownership/integration | PASS both, 16,260 checks and 24 independent trajectories |
| Original assembly production contract | PASS both, 41,634 checks |
| Full Simulation qualification | 91 reported PASS lines in each configuration; historical qualified physics unchanged |
| Protected M15.1 focused routes | 15 routes PASS Debug / Release after capacity/lifecycle correction |
| ReferenceFrames / Precision / verified BEPU | PASS Debug / Release |
| Launcher | 18/18 Debug / Release |
| Final presentation, strict transport and three prior canonical scenes | PASS Debug / Release |
| Final native legacy grid / frames | 16-frame runs each, Debug / Release, no exhaust pass or API validation error |
| Actual final revised Release visual checklist | Project Control **PASS** |

The independent Decimal coupled-equation oracle retained all 24 schedules and
produced byte-identical references in two processes. Worst 4×/8× refinement:
position 7.134550897634767e-14 m, velocity 6.973153749131656e-14 m/s,
orientation 4.697922286050306e-15 rad, angular velocity 1.9775190850810346e-16 rad/s.
Existing runtime tolerances were not relaxed. See [oracle-report.md](oracle-report.md).

Fourteen checked allocation windows remain **0 B**. Warm application service plus
presentation and terminal presentation also remain **0 B**. Separate byte[128]
control reports **152 B**, checked entry/exit PASS, in both configurations.

The new GPU test checks pure main emission, independent RCS extinction,
exponential background transmission, doubled optical depth, draw-order invariance,
clearing after shutdown, opaque depth before/inside/behind, axial and inside views.
RCS depth sum 14.5544, duplicate 29.1087, measured order difference 0.

The first new offscreen run failed the strict error-count gate on missing Epic
implicit-layer manifests, despite passing image assertions. The final test used
the documented process-local empty implicit-layer directory while retaining
explicit Khronos validation. No API error was waived, no host registry changed.
Mechanical development compile failures and earlier harness limitations remain
in results.json/method-results.json. No full 112-test Graphics campaign is claimed.

## Performance, cold costs and storage

Final Release, three fresh serial processes, 1280×720, 480 frames each:

| Process | Median ms | P95 ms | P99 ms | Maximum ms | GPU average ms | Cold managed preparation ms |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 5.553 | 5.844 | 6.239 | 35.592 | 0.109 | 145.6626 |
| 2 | 5.532 | 5.827 | 6.341 | 35.130 | 0.108 | 142.6501 |
| 3 | 5.547 | 5.836 | 6.032 | 40.196 | 0.111 | 142.9987 |

Cold managed preparation includes catalog, owner, history, assets and bindings,
and excludes native GPU startup; host credit is zero. Native timing includes all
frames, not just warmed active firing. GPU averages are not plume-only measurements.
The live service maxima were 6.3584 / 6.2488 / 6.2308 ms, including cold/JIT paths;
they are not the separately warmed 128/1,024 operation populations.

These medians/P99 support the 150-FPS-class objective. **The 35.130–40.196 ms
whole-frame maxima remain UNATTRIBUTED.** No all-frame 6.67-ms guarantee, causal
renderer blame, or subtraction of percentiles is claimed. Debug max 137.521 ms
and manual max 38.232 ms remain separately recorded, not pooled into Release.

The existing three-process operation campaign has sixteen separately named
populations and is retained in results.json, including historical pre-correction
populations. It is not rerun merely to erase tails or manufacture a faster figure.

Runtime managed conservative retention bound: 344,544 B (includes transients);
forced-GC retained estimates 317,400 B initially / 317,592 B at end. History
199,704 B, credit trace 65,560 B and plan 40,984 B are included, not added again.
Presentation separately uses 1,359,096 B shared mesh buffers, 4,416 B submission
storage and 9,896,672 B in two GPU images at 1280×720. Those images are not counted
against the physics-world historical 8-MiB comparison and are recreated on resize.

## Manual result, red team and limits

Project Control's final explicit **PASS** covers bell attachment, bright core/soft
edges, gimbal alignment, only commanded RCS horns, camera independence and no
plume after shutdown/final hold. The latest window completed 128/128 with no abort.
See [manual-acceptance.md](manual-acceptance.md). This supersedes only the earlier
visual acceptance status, not historical evidence.

Independent read-only review found no material blocker in bounds, optical-depth
composition, attachment lifetime or capacity. Its exact-nozzle-ID test objection
was resolved: tests require main 0 and the actual jet index +1, not only a range.

Qualified for the admitted vacuum presentation profile. Gas/Mach/pressure
modulation, atmospheric effects and exact overlapping-media light transport are
outside this result. Weighted emission is an approximation (overlap is weighted
average, not exact summed light). No production facility/contact/Florida work,
new RCS physics, art redesign, milestone assignment or banking is included.

## Evidence and handoff

Retain source/asset/provenance identities, decisive measurements and failure
history, independent oracle/reproducer, method map and manual result.
[reproduction.md](reproduction.md) recreates the builds and checks without temporary
KSA dumps or old comparison trees. [cleanup.md](cleanup.md) and
disposable-inventory.json list the exact reviewed disposable root and counts.
Automatic deletion was rejected before execution; the reviewed manual command is
provided there. Cleanup currently remains pending for 1,925 files / 382,623,053
bytes. This does not change the technical or final visual PASS.

Subsequent bank-preparation verification on 2026-09-17 found that old output root
absent after authorized cleanup. The paragraph above records the prior handoff.
Project Control has accepted the broader integration; current bank-preparation
status, fresh bounded validation and new output disposition are in
[bank-preparation/README.md](bank-preparation/README.md). No banking occurred.

**Judgment: PASS. UNBANKED. STOP FOR PROJECT CONTROL.**
