# Predeclared numerical contract

Recorded before trajectory qualification. Candidate remains unqualified until every gate passes.

The dedicated fixture has dry mass 8 kg, initial point fuel <=1/128 kg, dry spherical inertia (2,2,2) kg m^2, no gravity/environment/contact, source position components <=1000 m, speed <=100 m/s, angular speed <=1.1 rad/s throughout each interval, body force <=9000 N and body torque <=2 N m. The next canonical interval is <=16667 ticks. This is a bounded first model, not admission of every finite M14.24 resource input into physical dynamics.

Per-interval component ceilings: position 1e-6 m, velocity 1e-6 m/s, orientation angular distance 1e-9 rad, angular velocity 1e-9 rad/s. These ceilings were selected before seeing final trajectory outputs. Exact resource accounting remains independent of trajectory error.

Each exact segment uses normalized local time. Duration is a rounded normalized binary significand plus an independent integer exponent. A positive exact duration never becomes a zero scheduling decision. RK4 uses 128 dyadic partitions, or one partition when H<=2^-40 seconds; at most two segments/256 steps/1024 derivative stages. Stage mass uses exact integer W=D*(dry+successor)+(D-j)*consumed, D=2N, followed by banked correctly rounded kilogram projection and exact power-of-two division. Powered comes before unpowered.

Bounded raw RK derivative sums are accumulated for position, velocity and angular velocity. Each accepted substep is projected from the segment source using H/(6N) times the cumulative sum. This prevents repeated subnormal rounding from erasing a representable total impulse. Quaternion stages use the existing scalar-last Hamilton body-rate convention, with checked normalization at accepted RK steps. No universally correctly rounded quaternion components are claimed.

The independent conservative truncation derivation uses a=HW/2, b=H^2*A/2, B=H*F/8, C=consumed/8<=1/1024. With D2=a^2+b and D3=a^3+3ab, a velocity bound includes B*[D2/(3N^2)+(D3/72+a*D2/8)/N^3+a^2*D2/(48N^4)] plus Simpson quadrature remainder G4/(2880N^4). Rotation derivatives R0..R4 are 1, 2a, 4a^2+2b, 8a^3+12ab, 16a^4+48a^2*b+12b^2; Gn=B*sum(C(n,k)*Rk*(n-k)!*C^(n-k)). The two-segment bounds are velocity <1.701e-7 m/s and position <2.846e-9 m. Normalized quaternion truncation is <4.663e-19 chord per ordinary segment. Tiny single-step velocity defect is <7.755e-24 m/s even at maximum fractional consumption.

Predeclared floating-point reserves: position/velocity 2e-7 in their units, orientation 4e-10 rad, angular velocity 2e-10 rad/s. These require an implementation-level roundoff review; they are not certification by nominal operation count. Final acceptance is conjunctive: admitted domain, finite intermediates, exact segment semantics, error-bound review and independent oracle/reference results. Failure does not authorize changing thresholds.

Tests must include: epsilon fuel/16 N/8 m/s exhaust (duration 2^-1075 s and delta-v one epsilon); fuel 1/128, thrust 2^-64, exhaust 2^-1064 (large exact flow/tiny event); and ordinary full duration with thrust 512 epsilon/exhaust 8 (weak per-step impulse). BigInteger rounding-cell tests independently check the ratio helper. Analytic variable-mass translation and an independently implemented rotating reference will qualify trajectories separately.

## Independent implementation review after qualification

The numerical verifier passed this bounded domain. An initial admission expression could round its angular envelope inward; the reviewer supplied a concrete counterexample. The correction uses upper squared terms/sums with a lower limit square for non-axis magnitudes, retains exact axis checks, and inflates the positive norm/product/sum angular envelope by 32u before BitIncrement, u=2^-53. The permanent test retains the old counterexample. No physical or error threshold changed.

The implementation-level roundoff allowances are: quaternion normalization 8u chord error on the admitted stage domain; normalized Rotate including its cross products 128u|F|; acceleration including stage mass projection/division 256u*Amax plus orientation input error; a normal scaled product including duration projection <4u relative error with separate absolute subnormal rounding; cumulative raw sums gamma128*sum(abs(terms)); and an accepted RK quaternion step 512u chord roundoff including stage construction/projection. These are layered bounds using bounded operands and normalization sensitivity, not an assumption that cancellation-sensitive relative error is bounded.

Over at most 256 substeps, conservative arithmetic contributions are x<2e-10 m, v<5e-9 m/s, Q<4e-11 rad, omega<8e-13 rad/s, all below their predeclared reserves. Propagation uses the constant spherical-inertia angular slope and cumulative reconstruction, norm-preserving exact rotational flow, force-rotation sensitivity <=2|F| times normalized quaternion chord error, and the absence of position feedback into this force law. Absolute subnormal errors are separate; the three permanent tiny witnesses additionally require their representable impulse to survive exactly.

Combining these arithmetic allowances with the conservative truncation bounds stays below all four original ceilings. This is a reviewed error contract for this fixture, not a generic rigid-body integrator theorem or proof of every subnormal quaternion effect. Numerical and authority focused tests passed in both configurations. Overall candidate acceptance remains stopped by the separate full Release clock allocation failure.
