# Independent verification

Verifier: read-only agent `/root/engine_bridge_review`, independent of the single production writer. Reviewed actual production and permanent test source; did not execute tests or write files. Lead supplied the executed receipts in validation.json.

One bounded correction was requested and adopted: reject stored linear/angular segment epochs after interval start. The previous algebraic body-demand path did not exclude that unsupported source. A permanent future-segment test now proves refusal leaves the pending edge, cursor and physical state unchanged. No banked architecture contract was weakened.

| Attack | Final result |
|---|---|
| 1. Lost same-boundary edges | PASS: individual ordered records, repeated-edge tests |
| 2. Latest snapshot used as history | PASS: records drive latch; snapshot is consistency check only |
| 3. Command vs preparation cursor conflation | PASS: explicit separate cursors; only private progress seals |
| 4. Saturation/overflow loss | PASS: captured + queued reservation, commit guard, refusal and drain/refill witnesses |
| 5. Requested vs proposed | PASS: separate throttle, latch, availability and conditional fields |
| 6. Proposal exposed as committed | PASS: no canonical actuator state/revision/application path |
| 7. Canonical mutation | PASS: fixed writes restricted to private storage; snapshots and physical sequence proofs |
| 8. Fuel consumption implied | PASS: required-flow and available-feed-only contract; no resource writes |
| 9. Root-frozen body demand | PASS: body-only contract and independent rotating-body witness |
| 10. Moment sign/frame | PASS: COM, off-COM and mirrored analytical tests |
| 11. Stale/foreign replay | PASS: private seal + boundary + exact live source; retired/old/fabricated tests |
| 12. Refusal atomicity | PASS: checks precede cursor writes; arithmetic/source/event/final-refusal witnesses |
| 13. Allocation | PASS: six checked exact windows and independent known-object control; executed 0/152 bytes in both configurations |
| 14. M14.22 regression | PASS: three optional hooks; existing cheap/replay/allocation/full-suite tests unchanged and passing |
| 15. M14.21 regression | PASS: all endpoint bits match in three 1,200-interval fixture pairs per configuration |
| 16. Rendering/camera dependency | PASS: no such production input/reference; canonical-stream determinism independent of polling |
| 17. Unavailable hardware | PASS: Ignite preserves proposed Enabled and accepted history; unavailable output remains zero |
| 18. Authored constants/units | PASS: explicit SI FP64 definition; single exhaust representation; fixture constants test-only |

Strongest rejected counterexample: a closed command source alone did not prove motion segment availability. The new explicit source-epoch check resolves it. Strongest remaining boundary: a successful preview is still not a physical/fuel authorization, and private retirement does not rewind consumed preparation. Those are deliberate scoped semantics, documented and tested.

Measurement review: allocation counter opens after successful checked no-GC entry and closes before exit; failures propagate; timing uses ordinary runtime separately. Cost scope includes consumption/preparation/copy and cheap validation, excludes input production and harness clock advancement. Cold storage counts five retained objects, with engine reference separate. No tolerance, retries, profiler or substituted performance values.

Final source/test review: **PASS, no remaining blocking finding**. Lead execution: full builds, 63/63 Simulation groups per configuration, focused checks, exact allocation, positive control and physical non-actuation all PASS. Historical refs and unrelated tracked/untracked bytes verified unchanged. Candidate remains UNBANKED for Project Control.
