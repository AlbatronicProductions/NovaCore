# Exactly one proposed implementation ticket

**Qualify canonical SRV-01 admission and publication in a bounded unpowered
retained supported-contact episode.** No milestone number assigned.

**IMPLEMENT THE ACCEPTED KSA-CONVERGENT ARCHITECTURE, NOT A FRESH
REINTERPRETATION.** Project Control must accept/issue this ticket; this record
does not authorize implementation automatically.

## One result

The existing seven-part SRV01.FourHorn canonical assembly, with its original
spacecraft/launch/part/store identities and exact initial resources, occupies a
declared supported placement on a local slab. A single retained native body
evolves it; the existing assembly transaction owner publishes typed successors
and acknowledges the same world. One minimal visible supported route
shows copied committed poses. No qualification-box substitution.

## Ordered scope

1. Prepare an explicit immutable part-collision profile for the existing design,
   bound by digests and part keys. Review dimensions/poses and physical-vs-visual
   coverage before solving. Reuse existing primitives/compound and four-contact
   selection. No mass/geometry/thrust tuning to obtain support.
2. Add the cold explicit SupportedContact consumer/schedule to assembly ownership;
   preserve current FreeFlight defaults and catalog definitions. Contact-only
   20-second/1200-interval lattice supports the banked retained-contact qualification
   campaign; no larger generic command envelope. Compile OFF/gimbal-fixed input,
   source mass/COM/tensor, frame/precision envelope, native storage and history
   before READY. Do not run free-flight resource evaluation at unsupported ticks.
   Contact credit capacity4801 is separate from free-flight4096 and part of the
   retained-storage budget. The displayed route is required only after cheap
   numerical/ownership gates pass, with fresh manual acceptance.
3. Bind the same assembly slot to LocalContactWorld via typed source/expected
   authority, one pending sealed receipt, same-world acknowledgement. Adapt
   COM/material-O conversion explicitly. Share owned numerical and fixed-commit
   primitives; no duplicated solver/transaction engine.
4. Perform the cheap proof in qualification-plan.md, including first three exact
   intervals,120-interval support and refusal/terminal paths. Stop on conflicting
   authority, new physics policy or unsupported geometry.
5. Complete permanent lifetime/accounting/support/determinism tests, exact-zero
   allocation/storage, inherited full-operation performance and regressions.
   Then minimal copied-pose display and fresh manual acceptance; no visual polish.

## Scope constraints

One known craft, one resting orientation/placement, local slab, fixed gravity,
fixed source properties, no propulsion, no geometry/topology edits, no external
clock/config mutation accepted. Constant translating-frame regression only.
Supported outcome may include bounded settling; initial acquisition from moving
free flight is absent. At completion hold; at unsupported result fail closed.

Departure policy is deferred because current KSA separates support observation,
consumer eligibility and removal while retaining one authority. Preserve future
handoff invariants, but no transition or native-world auto-release in this slice.
Earth thrust/weight is irrelevant to that scope decision. No Florida, terrain,
landing gear, liftoff, atmosphere, arbitrary vehicle framework or Blender.

Current source's native dt follows the admitted integer interval; preserve it.
No solver iteration/substep/material/coverage-policy change. No main/RCS/gimbal
powered qualification or exact-exhaustion extension is bundled in this admission
ticket. Their canonical authority remains intact and existing regressions run.

Contact runtime save/restore is explicitly unsupported. Existing free-flight
save/load/replay remains unchanged and is tested. A new cold episode after
completion is not a silent resume. No general persistence front.

## Completion bar

One real assembly identity and complete typed successor; original part definitions,
resources and source properties; retained native body/caches; exact lattice and
debt/history; one physical consumer; meaningful shape/support oracle; all failure
classes; exact0 B plus positive control; <=8MiB; declared inherited performance;
current M15.1/M15.2 behavior; fresh manual supported-scene acceptance. Any material
failure stops for classification. Return UNBANKED with exact cleanup accounting.
