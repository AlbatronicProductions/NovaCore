# Certified final paired post-impact velocity acceptance

**PASS — READY FOR PROJECT CONTROL ACCEPTANCE. UNBANKED.**
Proposed accomplishment: **Certify final paired post-impact velocity states**.
No milestone number, private execution or banking was assigned.

## Baseline and identity

HEAD/main/origin/main/M14.12 remain
`e5fde08bcae834d8abc107cd26e7601fbfa3e1e0`; branch
`codex/certified-postimpact-velocity`. All M14.1–M14.12 tag objects/targets and
229 banked production C# fingerprints remain unchanged. Nothing staged.

The seven original candidate production/test identities are in [identity.json](identity.json).
Only the accepted immutable runtime-version preparation changes that checkpoint:
math-file SHA-256 is
`882a1b7d8849304ee2a98aa3333fd2a6654e084b26b1dd3769c52f1a789840ef`.
Other candidate production/test identities, including restored permanent tests,
match. No production/test edits occurred during resumed acceptance.

## Evidence map

- [Current complete results](acceptance.json): all 50 executed commands by gate,
  both full Florida certificates, exact bits, performance, layouts and red teams.
- [Numerical contract](../../certified-postimpact-velocity.md): versioned recipe,
  budgets, authority, arithmetic and future execution boundary.
- [Original stopped validation](validation.json): preserves the first 256-byte
  witness and six analytical fixtures; its status is historical.
- [Bounded allocation cause/correction](refusal-allocation.md) and
  [six-run matrix/identity](refusal-allocation.json): preserve the narrow proof,
  accepted correction and byte-exact test restoration. Their resume status is historical.

## Accepted allocation correction

The earlier eight-call refusal measurement reported 256 bytes. Six diagnostic
processes independently measured 32 bytes per call. Installed CoreLib IL proved
`Select -> ArithmeticSupported -> Environment.Version -> new Version(10,0,12)`.
The candidate now prepares only the immutable runtime-version predicate once at
arithmetic-owner initialization. Per-call Windows/x64 and arithmetic witnesses
remain. No warmup, threshold, numerical recipe or physical policy changed.

## Responsibility, arithmetic and finite recipe

The checked same-root M14.11 law plus M14.12 velocity tuple produce immutable
FINAL root-linear/body-angular binary64 values. They are derived evidence, not
authoritative state, a command, consumption token or mutation permission.

The first arithmetic profile is Windows x64/.NET10.0.12, nearest/even, gradual
underflow and separate operations. Exact zero becomes +0; nonzero spin is not
clamped. Per-call bit witnesses remain active. No selected-path FMA or FP-mode
change exists. Version/recipe constants are build-local; there is no receipt
serialization/import or caller-supplied version to forge.

The center and 64 outward endpoint corners are deduplicated in fixed order.
Maximum 65; no neighbors, refinement or retry. Precomputed component and physical
budgets must pass strict resolution guards before selection. Admission uses actual
selected-state defects and requires residual lower >=0 and work-energy upper <=0.
Choose lowest residual upper, then mass-weighted error upper, then canonical
unsigned-bit order. This is a sufficient finite recipe, not a complete lattice solver.

## Real Florida chain and final values

Both configurations recreated current physical terrain and M14.9–M14.12 inputs
through production owners. Independent guarded Earth ODE/decimal brackets and
exact-rational whole-root calculations certified the selected represented states.

Both fixtures: candidate **5**, **65 unique/evaluated**, **1 admitted**.
Constant-force/off-center final vectors:

```text
linear root m/s = (-29649.117242956567, -5410.196727028991, -2179.5695584237037)
angular body rad/s = (-0.26619050895886387, -0.11585225043673181, -0.013561041019835149)
linear hex = c0dcf44780e89a09 c0b522325cb3dbc1 c0a107239d2965f0
angular hex = bfd10943ea9ef878 bfbda87e3acb342c bf8bc5e41d7072cb
```

Constant-velocity control:

```text
linear root = (-29649.195081786824, -5410.224356627422, -2179.5245093215735)
angular body = (-0.24244695758109136, -0.10551878857577955, -0.012351568596309329)
linear hex = c0dcf44c7c38519d c0b522396f6f996b c0a1070c8c7c5d36
angular hex = bfcf08807ce9a98c bfbb0347820be82f bf894bc7796751e8
```

| Certificate | Constant force | Constant velocity |
|---|---|---|
| Normal residual | [3.4948618737827324e-12,5.36353530052859e-6] | [3.494775470099162e-12,1.5620808881757106e-6] |
| Tangential upper | 1.281509339143708e-5 | 2.467339297123622e-7 |
| Coupling upper | 5.0211017320412964e-5 | 1.5019169098875446e-6 |
| Work-energy enclosure | [-0.0932223257092834,-0.09320741752615826] | [-0.07732265806621522,-0.0773219396145606] |
| Energy-defect enclosure | [-1.3606430453974248e-5,6.473204343764185e-7] | [-2.3859966908751904e-7,2.39978574406925e-7] |
| Energy-defect budget | 1.4253780501699369e-5 | 4.786052057643998e-7 |
| Mass-weighted error upper | 2.282178025883452e-11 | 3.876498277153072e-13 |

Constant-force effective momentum defects, component intervals:

```text
dP.X [-1.0602496331557635e-5,-2.9103830456733697e-11]
dP.Y [-3.7640711525455126e-6,-7.275957614183424e-12]
dP.Z [3.637978807091712e-12,6.135589501354845e-6]
dL.X [-1.8850120623303827e-6,-1.1102230246251563e-16]
dL.Y [-1.2332089782685078e-6,-4.163336342344336e-17]
dL.Z [-1.988255282220353e-7,-6.938893903907227e-18]
```

The JSON retains all target/error/momentum enclosures and every a priori budget
for both fixtures. It stores signed Int64 bits as strings and hexadecimal, avoiding
loss from JSON-number parsing. Before parsing, complete raw certificate lines were
compared exactly across Debug/Release: **PASS, both fixtures**.

## Refusal, authority and replay

Focused tests pass one-third negative-residual rejection, lost-unit refusal,
non-axis exact normal, stored-bit attitude, all nonzero angular components,
asymmetric inertia, moving-material work, unequal scales, signed zero/subnormals,
FMA adversary, broad/nonfinite/overflow refusal, finite-set exhaustion and replay.
Independent exact-rational arithmetic verifies every reported physical defect.

Real integration passes default/forged evidence, mismatched root/witness/requests/
receipts, refined/substituted evidence requiring reissuance, spacecraft/clock/
timeline, geometry, force/torque, mass/inertia, Earth/frame and terrain/provenance
invalidation. Snapshots protect both velocities, revisions, history, pending events,
public clock and root evidence. No impulse or canonical event was emitted.
M14.4 canonical atomic and M14.5 represented-zero policies remain unchanged.

Actual unsupported OS/runtime/hostile FP-mode execution was not induced. Supported
arithmetic controls passed; incompatible-environment refusal is statically reviewed.
The timed “unsupported refusal” is invalid/default witness rejection. Wrong-version
receipt injection is not an exposed operation, rather than a claimed dynamic test.

## Release performance and allocation

Fixed protocol: 32 warmups, 101 individual ordinary-runtime timing samples,
separate checked 1 MiB allocation windows with eight calls. No runtime settings
changed. Times below are microseconds per call; no whole-frame claim.

| Path | Median | P95 | P99 | Maximum | Bytes / 8 |
|---|---:|---:|---:|---:|---:|
| Reconstruct banked source evidence | 3177.6 | 3232.5 | 4601.3 | 4673.7 | 13376 |
| Successful 65-candidate qualification | 106.2 | 109.0 | 113.3 | 117.1 | 0 |
| Checked consumption | 3.2 | 3.3 | 3.3 | 3.3 | 0 |
| Resolution refusal | 7.1 | 7.3 | 7.4 | 7.9 | 0 |
| Fixed-set exhaustion | 406.0 | 442.9 | 448.6 | 497.8 | 0 |
| Stale refusal | 0.8 | 0.9 | 0.9 | 0.9 | 0 |
| Unsupported witness | 0.3 | 0.4 | 0.4 | 0.4 | 0 |

Source reconstruction owns preexisting allocating evidence acquisition; it has no
zero-allocation promise. All reusable qualification/read/refusal paths are exactly
zero in Debug/Release; every entry/exit passes. Positive controls detect 152 bytes.
The 65-candidate workspace is streamed; no per-candidate collection exists.

Measured CLR value sizes: receipt2384, values624, budgets104, Prepared888, Pair48
bytes. Named selector storage subtotal: Prepared + four Pairs + best/next values =
**2328 bytes**. This excludes caller inputs, nested call temporaries, JIT spills and
return-copy layout; it is not a measured stack frame or peak memory claim.
Source-derived data is prepared once; each qualification reads the three checked
sources before candidate evaluation and never reevaluates/refines the root.
The separate fixture/path timings are not interchangeable scaling coefficients.

## Full validation and red teams

All **50 declared build/test commands passed**, no retries or acceptance failures:

- Debug/Release full solution builds: zero warnings/errors.
- Focused complete numerical tests: PASS both.
- Real Florida integration: PASS both, two qualified fixtures each.
- Full Simulation: **44/44 groups per configuration**.
- Eleven focused M14.2–M14.12 commands per configuration: PASS.
- ReferenceFrames: **11/11 groups each**; Precision: PASS both.
- Eight predecessor/query/stale headless cases plus candidate: **9 pass,0 fail,0 skip**
  each. Unrelated renderer/window campaigns intentionally not selected.
- Complete Florida certificate/bit comparison and `git diff --check`: PASS.

A/B/C independent numerical review: PASS; strongest challenge was confusing finite
sufficiency with completeness or treating normal-box points as independent unit
normals. The contract preserves same-root correlation and explicit refusal.
Actual-state certificates are checked without goal-sign clipping or nominal-work
substitution. Rational and real-root tests passed.

D/E/F authority/platform review: PASS with the hostile-environment/version-import
limits above. Runtime metadata preparation preserves the original predicate;
per-call arithmetic checks remain. Final values are not impulse commands. No
private execution or future general-solver commitment is introduced.

## KSA / general-solver boundary

Current installed KSA identity and source anchors remain in `identity.json`.
ADOPT prepared authority -> transient calculation -> staged final state -> guarded
application. ADAPT exact-root qualification behind NovaCore authority.
INTENTIONALLY DIFFER from iterative float/clamped velocity mechanics for this
bounded law: executed one-third and lost-increment witnesses require represented
FP64 final-state certification.

Relevant previously verified official engineering history:
[frame correction](https://discord.com/channels/1260011486735241329/1260112103134724146/1524584112093007943),
[inverse inertia](https://discord.com/channels/1260011486735241329/1260112103134724146/1524639263658999972),
[contact measurements](https://discord.com/channels/1260011486735241329/1260112103134724146/1524677633143476276).
These inform ownership, not proof of NovaCore's numerical guarantees.

The specialization does not define persistent contacts, friction, manifolds,
multi-constraint solving, support/rest, warm starting or landing gear.
**KSA / Bepu General Contact Solver Convergence Assessment is mandatory before
persistent support/rest**, presumptively adapting Bepu behind NovaCore authority.
No Bepu dependency or general solver was added.

## Reproduction and storage

From the fingerprinted candidate, build `dotnet build NovaCore.sln -c Debug` and
Release. Run each configuration's compiled
`tests/NovaCore.Simulation.Tests/bin/<config>/net10.0/NovaCore.Simulation.Tests.dll`
with `--certified-postimpact-velocity-only`, then no arguments for the full suite.
Focus flags remain translation-only, contact-only, contact-response-only,
isolated-contact-only, physical-event-epoch-only, exact-event-motion-only,
earth-event-only, florida-proof-only, root-kinematics-only, certified-response-only
and certified-preimpact-velocity-only (each prefixed by `--`).
Run ReferenceFrames/Precision DLLs without arguments. Graphics uses
`--category=headless --test='<exact case name>'`; the JSON lists all nine cases.
No source edits, profiler or global environment changes are needed.

Retain concise results, complete decisive certificates, identity and reproduction;
not raw stdout or copied assemblies/source. This final README replaces duplicated
intermediate narrative; original measurements and provenance remain in retained
JSON/reports. Package budget **64 KiB**. No diagnostic scratch/tree was created;
ordinary build outputs remain local rebuildable deployment artifacts.
