# Reproduction

From PowerShell in E:\NovaCore, Python3 standard library only:

```powershell
python -B 'E:\NovaCore\docs\engineering-evidence\powered-contact-chronological-event-feasibility\chronology.py'
python -B 'E:\NovaCore\docs\engineering-evidence\powered-contact-chronological-event-feasibility\tiny-audit.py'
```

These write only the retained five result JSONs and supplementary tiny
differential JSON beside source. No dotnet restore/build, backend,
native observer, temporary tree or pycache is required. The first script
executes the fixed nine cases once in order; the second explicitly checks
the same tiny/zero pair more strongly. Assertions fail at first mathematical
admission/accuracy failure. No retries or automatic input adjustments.

The two scripts reproduce physical/ledger values. Current retained JSONs
also record a review-only correction of zero-case demand reporting and the
original execution code hash; those provenance annotations are historical,
not repeated dynamically by a fresh run. No test-gate bar was changed.

Review model-contract.md and rank-conditioning.md first. No input or bar
search is performed. High precision costs are proof costs, not a proposed
production runtime. 24-digit printed JSON values are presentation of
700-digit comparisons and exact rational reference/ledger operations.

Source anchors to recheck with rg (do not substitute old KSA decompilation):

```powershell
rg -n 'SolveDescription|Timestep|InverseMass|source.Mass|Acceleration' 'E:\NovaCore\src\NovaCore.Simulation\Spacecraft\Contact\Staging\LocalContactWorld.cs'
rg -n 'requiredFlow|Exhaust|thrust /' 'E:\NovaCore\src\NovaCore.Simulation\Transactions\SimulationTransactionEngine.EnginePreparation.cs'
rg -n '8d|5120|9000' 'E:\NovaCore\tests\NovaCore.Simulation.Tests\PoweredFreeFlightTests.Physics.cs'
Get-FileHash -Algorithm SHA256 -LiteralPath 'E:\Kitten Space Agency\KSA.dll','E:\Kitten Space Agency\KSA.deps.json','E:\Kitten Space Agency\BepuPhysics.dll','E:\Kitten Space Agency\BepuUtilities.dll'
```

Accepted direct installed5438 tokens/helper closure and real Discord message
links are retained in the sibling event-lifecycle evidence. If installation
identity changes, do not assume methods still match; mark stale and inspect
current binaries before making a new mechanism claim. KSA rationale not
documented in that actual history remains UNPROVEN.
