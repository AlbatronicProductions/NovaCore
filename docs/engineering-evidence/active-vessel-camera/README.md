> SUPERSEDED after Project Control manual acceptance FAILED at Step 5 HOME. FREE is now DEFERRED; HOME is unbound. This file records the prior candidate, not current acceptance. Use [the correction package](../active-vessel-camera-free-correction/README.md) and [revised manual route](../active-vessel-camera-free-correction/manual-route.md).

# Unified active-vessel camera — Project Control handoff

## WHAT HAPPENED

Implemented the authorized camera front through the existing Solar architecture. Engineering validation passes; the uninstrumented candidate is deployed to the normal launcher/sample paths. Project Control manual acceptance is pending. [Manual route](manual-route.md).

## CURRENT REPO TRUTH

Main remains `45b1bbcdd8d0e1d42f79f3bb5124e80753ce0e8f`; M15.3 bank remains `544f25ad618bcad7a2753de69108ca3c4095df11` with its existing tag. Eleven production/test paths plus this evidence package are intentionally uncommitted. Index remains empty. Prior review/legal/integration material is preserved. [Repository truth](repo-truth.md), [entry identity](entry-identity.json), [final identity](identity.json).

## CURRENT KSA CAMERA IDENTITY / LIFECYCLE

Installed 5438 identity is unchanged from the accepted review. Authenticated live history through 5460 contains no material camera-boundary supersession. No KSA writes. [Currentness and convergence](ksa-current-camera.md).

## ADOPT

Shared canonical followable identity, viewed target distinct from controlled vehicle, explicit refocus, precision-relative transport.

## ADAPT

Existing Solar orbit/follow and existing free controller consume NovaCore's copied publication, explicit display epoch, lifetime generation and environmental-body fallback.

## INTENTIONALLY DIFFER

Use material origin O to match NovaCore's renderer; do not import COM smoothing or a broader viewport/target-registry framework.

## NOVACORE CAMERA OWNER

SolarSystemScene remains the sole focus/orbit/navigation owner. Detached movement reuses FreeCameraController. [Owner map](owner-map.md).

## TARGET IDENTITY

Canonical session spacecraft ID, presentation lifetime generation and copied observation; one fixed binding. Render identity is not authority. [Target contract](target-contract.md).

## VIEW TARGET VS ACTIVE VEHICLE

Focusing Earth/Moon, detaching or refocusing never changes the session's active spacecraft, command ownership or canonical physical state.

## DISPLAY EPOCH

Copied target refresh occurs after Solar publication and before final camera-relative submission. Physical and display epochs are explicit; stale epoch/root/lifetime is refused. [Epoch/precision contract](epoch-precision.md).

## MATERIAL / VISUAL FOCUS ANCHOR

Material origin O, using the same existing embedding as rendered SRV. Actual COM change is tested without target jump.

## ENVIRONMENTAL BODY CONTEXT

SRV view retains Earth/Florida environmental authority independently of the view-target kind. Existing terrain exclusion remains active.

## FOCUSED FOLLOW

PASS in permanent tests: the camera translates with the copied endpoint while preserving player orbit demand.

## ORBIT

PASS: existing mouse behavior moves the eye around SRV. Terrain-constrained eye placement is aimed back at O; spacecraft state is unchanged.

## ZOOM

PASS: shared Solar multiplicative zoom, close inspection through existing Solar maximum, no hidden mode switch. Vessel clipping remains useful at large roots.

## FREE CAMERA

PASS: Home detaches; existing camera WASD/QE/look/speed behavior moves only the camera. Active binding and simulation remain independent.

## ALTERNATE / CELESTIAL FOCUS

PASS: Earth, Moon, Sun and Mars, plus protected Solar navigation regressions.

## REFOCUS ACTIVE VESSEL

Selected binding: **F**. Free: **Home**. Existing input ownership/conflicts were checked. A single saved vessel distance/yaw/pitch is restored. [Bindings](input-bindings.md).

## TARGET RETIREMENT / FALLBACK

PASS: invalid/retired generations cannot dangle or silently resurrect. A viewed retired vessel falls back to its environmental body. A live binding cannot be replaced without explicit retirement.

## MOVING-TARGET PROOF

PASS: actual existing 128-interval, two-second qualified free-flight episode, including resource-driven COM change. Additional 100 m/1 km/100 km copied translations are camera-only fixtures, not new launch physics.

## LARGE-COORDINATE PRECISION

PASS through roots up to 7e12 m; measured follow-delta error and encoded relative reconstruction error are zero in the tested fixtures. Centering tolerance is 2 mm. Physical observation/Save nonmutation checks pass.

## ALLOCATION

Exactly 0 B in applicable warmed follow/orbit/zoom/display, free/display and free/refocus windows, Debug and Release. Positive control: 152 B. Inherited celestial Focus costs 48 B per discrete action and is separately disclosed. [Allocation record](allocation.json).

## STORAGE

One 88-byte copied observation, fixed identity/environment fields, one three-double saved view, bounded flags and publisher generation. No registry/history growth; 10,000 refocus operations preserve one binding. Retained-memory witnesses show no increase. Object alignment is not represented as a fabricated exact total heap size.

## PERFORMANCE

Twelve integrated processes; matched fixed-credit before/after plus live and candidate-only route characterization. Controlled whole-run P99: baseline 6.566–6.628 ms; candidate 6.531–6.573 ms. No persistent matched-route regression demonstrated. Startup and approximately 11 ms first-refocus following-frame tails remain, including one unproven 17.833 ms transition callback. [Performance disposition](performance.md), [measured populations](performance.json).

## REGRESSION

PASS: 94 Debug/Release gates, zero failures; both solution/native builds have zero warnings/errors. Protected M15.3, Solar, terrain, input, ABI, Simulation, ReferenceFrames, Precision, Camera, dependency and Launcher gates pass. Deployed uninstrumented 480-frame smoke exits 0. [Validation](validation.json), [regressions](regression.json), [permanent coverage](test-contract.md).

## MANUAL ACCEPTANCE

Required: **YES**. Status: **PENDING**. Native F/Home interaction, launcher UI and player-facing visual acceptance belong to Project Control. Normal launcher → FL Launchpad → Play is prepared. [Instructions](manual-route.md).

## RED TEAM

PASS: independent source/test attack on all 25 requested risks. Independent measurement review reports no blocking camera regression with the retained limits. One bounded live-binding correction was made within the accepted architecture. [Verification](verification.md), [implementation](implementation.md).

## JUDGMENT

**STOP FOR PROJECT CONTROL**.

Engineering gates pass. Full camera qualification is not declared before required manual acceptance.

## MILESTONE NUMBER

**NOT ASSIGNED**.

## BANKING

**NOT AUTHORIZED**. No staging, commit, tag or push.

## WHAT HAPPENS NEXT

Project Control performs the prepared manual route and supplies acceptance or a specific defect. Any subsequent promotion review/banking needs explicit disposition. No engine controls, launch stack, departure, SAS/navball or other front is opened.

[Reproduce](reproduce.md) · [Evidence lifecycle](cleanup.md)
