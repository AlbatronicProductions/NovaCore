> SUPERSEDED after Project Control manual acceptance FAILED at Step 5 HOME. FREE is now DEFERRED; HOME is unbound. This file records the prior candidate, not current acceptance. Use [the correction package](../active-vessel-camera-free-correction/README.md) and [revised manual route](../active-vessel-camera-free-correction/manual-route.md).

# Bounded implementation

SolarSystemScene resolves its existing `FocusTarget.SceneObject` through one canonical copied observation. Orbit/zoom, celestial navigation, terrain exclusion and projection remain in Solar. Detached movement reuses FreeCameraController. The sample binds the current Florida Solar consumer at construction and refreshes after each Solar publication.

StockAssemblyDevelopmentScene adds presentation lifetime and copied-observation adaptation only. The physical session, launch creation, solver and resource behavior are unchanged. The isolated `--scene=srv01-florida-support` diagnostic remains its existing bounded presentation; the player route is the existing FL Launchpad Solar consumer.

Native/managed input appends F/Home camera actions and matching ABI checks. No new production scenario or benchmark flag was added. Performance instrumentation is a disposable compile overlay retained as evidence tooling; it is never deployed for manual acceptance.

One bounded lifecycle correction was made during independent review: a live binding could previously accept replacement while the selected scene-object ID still described the old vessel. The correction rejects live replacement and validates the selected ID at placement; a permanent outcome assertion covers the attack. The shared camera architecture did not change. Free projection refresh was completed within the original environmental clipping design.

Test-harness preparation issues (regional fixture initialization, unsupported contact Save API, and explicit overload typing) were corrected before passing populations. The observer's first launch lacked copied managed dependency assemblies; its deployment script now explicitly copies the already built dependencies. That failed setup attempt is not included as performance data.
