# Acceptance resumption

Project Control accepted the current performance architecture for continued
correctness validation. Isolated publication impact LOW; write-only commit
NEGLIGIBLE; aggregate warp multiplicity UNMEASURED. Existing timing evidence is
retained. No performance optimization or new timing campaign is authorized here.

Baseline and all prior candidate source/test fingerprints matched on entry.
Production remains frozen. Changes are confined to permanent tests and evidence.

## Bounded validation plan

First compile and run focused gap, allocation and ownership tests in Debug.
Fix only new test-construction defects or stop on a proven production conflict.
Then one full solution build per Debug/Release; one fresh full Simulation process
per configuration; one ReferenceFrames and Precision process per configuration;
focused M14.1 query/stale and M14.2–M14.16 selectors/integration cases per
configuration. Run real Florida publication, acceptance gaps and allocation matrix
in each configuration. No GPU/window campaign, profiler or timing rerun.
Stop on an unrelated failed permanent gate, with no automatic forensic expansion.

Allocation uses eight independent consuming samples per stage and 32 reusable
reads/refusals, following eight fresh consuming warmups. Source/proof reconstruction
uses two warmups and eight independently measured reconstructions. Each uses the
qualified 1 MiB checked boundary with hard entry/exit failure and exact-zero hot-path
requirements. Legitimate engine/history setup and test replay reconstruction/
record-comparison allocation are reported separately, with a real byte[128] control.

No production code, history layout, lifecycle or timing workload changes are planned.
The new fixture accepts captured source values and qualification requests so replay
can reconstruct fresh evidence from a serialized coupled record. JSON record
comparison is test infrastructure, never a production permission or runtime dependency.

## Development checkpoint

Focused Debug gap tests passed: nine history-driven debt/cadence variants, seven
incompatible-record refusals before publication, independent StaleEndpoint, full
pending/cancelled payload snapshots, direct source mutation refusal, ownership,
occupied history capacity and exact large-debt subtraction. Default JSON handling
did not reconstruct readonly SimulationRate; a test-only converter now invokes its
existing invariant constructor. No production serialization was changed.

Debug allocation matrix passed all exact-zero paths. Per invocation measured
legitimate setup: engine/history 11,512 bytes; fresh source/proof replay 65,248 bytes;
test-only serialized record comparison 45,688 bytes. Positive byte[128] control:
152 bytes. Standalone Simulation ownership test passed. Full gates follow this plan.

First full Debug process: 13 groups passed, then CelestialContractTests reused a
standalone borrow after binding its store to SimulationState. The frozen ownership
contract intentionally expires that borrow. Independent review approved one test-
only adaptation: assert expiration and reacquire stateView.Celestial before the
unchanged hash/immutability/traversal/allocation checks. Production unchanged.
This related test correction requires rebuilt Debug/Release and fresh full runs;
it is not a retry of unchanged failing evidence or allocation forensics.

Second full Debug process passed 27 groups, then the old SpacecraftAttitudeIntegrationTests
standalone borrow returned false after ownership binding and a committed attitude change.
Independent review approved the same narrow lifetime adaptation: assert old-view refusal,
reacquire engine.State.Spacecraft before the unchanged warmup/100,000-iteration loop.
No production code or workload/threshold changed. Remaining Program.cs borrow acquisitions
were audited; no additional mismatch was found. Final rebuilt Debug/Release full suites
passed 48/48 groups. All 68 final acceptance test processes passed. Exact selectors,
cases and allocation values are retained in resume-results.json; README is the final report.
