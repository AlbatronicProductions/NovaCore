> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Cache-duration provenance: qualification stopped at coast admission

**Judgment: REVISE. Operator UNQUALIFIED and UNBANKED.**

The private duration-provenance implementation passes the original interior powered piece and the bounded 20-case composition. The first actual retained-world coast admission returns `UnsupportedSupport`. No coast solve or reference runs, and no coast result is installed. The specific rejected support predicate was not recorded and remains **UNKNOWN**. There was no retry or admission-policy change after this failure.

## Baseline and scope

HEAD, main, origin/main and remote main remain `49057fecceb0f725d5f551ec40e2780971b0d81d` on `codex/powered-contact-numerical-prerequisite`. All 2,054 pre-existing tracked/untracked inputs remain byte-identical; all 65 historical tag refs remain unchanged. Protected M14.21-M14.24 and M15.0 identities are in [identity.json](identity.json).

Only the new standalone [OwnedCacheProbe project](../../../../tests/NovaCore.ContactNumerics.OwnedCacheProbe/NovaCore.ContactNumerics.OwnedCacheProbe.csproj) and this evidence directory are added. Production, existing tests, Simulation runner, solution, BEPU dependency bytes, Blender files and previous investigation evidence are unchanged. This is numerical prerequisite work, not a live production integration.

## Implemented duration-history contract

[CacheHistory.cs](../../../../tests/NovaCore.ContactNumerics.OwnedCacheProbe/CacheHistory.cs) retains seven signed exponent-scaled impulses: four normal, two tangent and one twist. Linear impulses have N.s units; twist has N.m.s units. The accepted cache binds world/body/constraint/manifold generation, ordered features, accepted piece/kind, producing duration, load and endpoint velocity. This state has no canonical resource, actuator or engine capability.

Exact event numerator/denominator is retained alongside the numerical duration scale. The seed retains the exact binary64 bits of the actual widened binary32 solver timestep, `4580461061261492224`, corresponding to `0.01666666753590107 s`; it does not substitute nominal 1/60. Numerical initialization uses the retained significand/exponent. The exact event identity is not replaced by that approximation.

The implementation combines signed impulse and duration significands/exponents before any projection. It never first materializes a potentially underflowing/overflowing ratio. Within the admitted support domain the order is duration initialization, unchanged current-piece CommonNormal load correction on normal impulses only, then unchanged normal/tangent/twist solve for eight sweeps. Current-piece physical prediction and softness use the current duration; the previous duration is initialization provenance only.

Admission requires the same lineage/features and accepted endpoint/native cache projection, four finite supported rows, the same normal basis, positive depths within the existing 20 mm bound, inward previous/current generalized load including torque, bounded normal speed, and feasible cached impulses. Unsupported states refuse. Strict basis equality is a declared domain restriction, not an established explanation of the observed coast refusal. Unloaded compressed soft contact remains an explicit counterexample to universal ratio exactness.

Preparation and numerical solves do not promote provenance. Installation prepares the next accepted tuple, marks continuation unsafe, writes private cache/endpoint, verifies all seven native cache projections, then promotes the matching prepared tuple. Unexpected failures poison private continuation. The accepted scaled cache and its binary32 native projection are separate, explicitly checked representations. State-machine seams cover speculative/discarded preparation, stale/foreign lineage, replay, mismatched install completion and invalidation; they do not qualify a canonical publisher integration.

## Executed qualification

The [execution contract](contract.md) was retained before implementation/execution. SDK `10.0.303`; runtime `.NET 10.0.12`.

| Gate | Result |
|---|---|
| Standalone Debug and Release builds | PASS, zero warnings/errors |
| Debug contract/component process | PASS, 165 checks, 18 records |
| Release contract/component process | PASS, 165 checks, 18 records; report byte-identical to Debug |
| Final native-readback ordering change, Debug build | PASS, zero warnings/errors |
| Original actual powered piece | PASS |
| Frozen duration/load/midpoint composition | 20/20 PASS, including original plus 19 additional cases |
| Actual powered endpoint/cache/provenance installation | PASS, piece 1, h = 1/128 s |
| Actual coast admission | FAIL: `UnsupportedSupport`, before reference/solve |
| Release retained sequence and all later gates | UNREACHED under first-failure stop rule |

The final installation-order change does not execute in `contracts`; the earlier Release build did not include that last harness change. It was compiled and exercised in the final Debug retained sequence. No final Release sequence or rebuild was performed after the correctness stop. No full solution or production acceptance is claimed.

| Absolute error | Original powered piece | Maximum over 20 cases | Unchanged bar |
|---|---:|---:|---:|
| Summed normal impulse, N.s | 7.772526796068213e-7 | 3.0528216343195247e-6 | 1e-4 |
| Linear velocity, m/s | 2.3900377891757947e-7 | 7.747304912061694e-7 | 1e-4 |
| Angular velocity, rad/s | 4.3713515883680234e-7 | 1.3497906143186619e-6 | 1e-4 |

Separate normal, nonzero-tangent and nonzero-twist analytical fixtures passed ratios 1, 1/2, 1/4, 2 and the original interior ratio. Ratio 2 is a pure component arithmetic/solve witness; the ordinary owner correctly refuses its duration above the existing admitted interval ceiling. It does not expand that domain. No clamp fired in these witnesses. Twist correctness is demonstrated here; a unique twist convergence benefit is not claimed.

The exact positive `2^-1075` event and seven-component scaled cache roundtrip pass as representation tests. The case with a divergent standalone ratio and finite complete product returns 1/64. These results do not qualify tiny retained-world constrained response. The current ordinary projected consumer explicitly refuses nonzero scaled impulses that project to zero.

## Actual powered-to-coast failure

One world performs the unchanged 120 preparation steps, then the original powered piece at midpoint mass 8.00390625 kg, 32 N, h = 1/128 s. The powered cache/endpoint are installed and native projections verified before provenance promotion. World/body/constraint/features and installed pose/velocity remain coherent before coast admission.

The next coast input uses dry mass 8 kg, zero engine force/torque, exact duration 17707/2000000 s (0.0088535 s), and the newly accepted powered duration as predecessor. The duration ratio is 1.133248. It does not reuse the original preparation duration.

Admission reaches `Supported` and returns `UnsupportedSupport`. The current evidence excludes earlier lineage, feature, endpoint and native-cache mismatch branches, but does not identify which internal support predicate fails. Do not attribute this result to a changed normal, duration arithmetic, BEPU instability or failed coast convergence.

Coast collision refresh had already changed private preparatory state. The failure handler invalidates continuation and preserves the last accepted piece-1 cache, endpoint and h = 1/128 provenance. It does not claim rollback of the whole private world. Canonical mutations remain zero. No failed coast cache or duration is promoted.

## Deferred acceptance

Powered-to-coast reference/support qualification is incomplete. Complete tiny-event, selector consistency, off-COM/asymmetric response, long support, M15.0 no-constraint compatibility and complete retained-sequence determinism are UNREACHED. The identical Debug/Release component reports are not a substitute for retained-sequence determinism.

Warmed allocation/positive control and formal Release timing were not run because correctness stopped. Zero allocation and performance are unqualified. The standalone harness has reporting/reference/setup allocations and is not a qualified hot path.

Representation sizes measured during component tests: `CacheDuration` 576 bytes; `AcceptedCache` 904 bytes. These are fixed value sizes, not total retained-world storage. The owner holds fixed accepted/prepared installation state, no growing duration-history list or per-contact provenance object. Complete storage measurement remains deferred.

## Red team and next responsibility

[Independent review](verification.md) supports the passing bounded results and the refusal/invalidation accounting, and returns REVISE for the overall operator. Native readback precedes metadata promotion. The strongest remaining objection is that the declared support domain does not yet admit the required actual coast continuation. Its precise rejected predicate is unknown.

Project Control can scope one admission-predicate witness for the retained powered endpoint before deciding whether the support domain or basis/cache handling needs correction. This report does not authorize or implement that investigation. No solver tuning, basis tolerance, fallback reset, additional sweeps or retry was applied.

Reproduction is retained in [reproduce.ps1 â€” retired record](../../powered-contact-ordinary-step-event-closure/CONSOLIDATION-MANIFEST.md). It runs the bounded contract processes and one Debug retained sequence, stops on its nonzero exit, and does not proceed to later gates. [results.json](results.json) retains component results, all 20 compact matrix measurements, the decisive actual installation/refusal records and original report hashes. Previous [convergence — consolidated record](../historical-early-convergence.md), [preconditioning](../preconditioning/) and [duration-history](../duration-history/) evidence is preserved unchanged.

## Final Git and evidence hygiene

Final fingerprint comparison: all 2,054 starting inputs unchanged. New files are the three OwnedCacheProbe files and seven files in this evidence directory. Nothing staged; no tracked working-tree modification. `git diff --check` and `git diff --cached --check` pass. New untracked text was also checked explicitly for trailing whitespace, JSON parsing, PowerShell syntax and local evidence links.

Final `git status --short` (existing prerequisite work is intentionally preserved):

```text
?? docs/engineering-evidence/powered-contact-architecture/
?? docs/engineering-evidence/powered-contact-numerical-prerequisite/
?? tests/NovaCore.ContactNumerics.ConvergenceProbe/
?? tests/NovaCore.ContactNumerics.CoupledProbe/
?? tests/NovaCore.ContactNumerics.DurationProbe/
?? tests/NovaCore.ContactNumerics.OwnedCacheProbe/
?? tests/NovaCore.ContactNumerics.PreconditionedProbe/
?? tests/NovaCore.ContactNumerics.Probe/
?? tests/NovaCore.ContactNumerics.WorldProbe/
```

The seven concise evidence files total less than 96 KiB. Disposable output remains 60 files / 3,865,415 bytes because automatic approval review rejected the single deletion attempt before execution as `blocked by policy`. No retry occurred. [Cleanup disposition and exact manual command](cleanup.md) are retained separately from the numerical blocker.

UNBANKED. No live powered contact, departure, Florida integration or M15.1. STOP FOR PROJECT CONTROL.
