> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Duration provenance correction and qualification order

Declared before implementation/execution. Baseline `49057fecceb0f725d5f551ec40e2780971b0d81d`; branch `codex/powered-contact-numerical-prerequisite`. Preserve all 2,054 pre-existing files, all tags and historical convergence/preconditioning/duration-history evidence. This is a standalone private numerical prerequisite; no live canonical contact integration or banking.

## Representation and ownership

Retain a fixed accepted cache value containing seven signed exponent-scaled impulses, world/body/constraint/manifold-generation identity and four ordered feature identities, accepted piece sequence/kind, exact producing duration identity, actual numerical producing scale, and accepted load. Exact M14.24 numerator/denominator stays separate from its rounded53-bit numerical scale. A stock-BEPU preparation seed records the exact supplied binary timestep bits instead of a nominal1/60.

Preparation returns a private owner-bound value and mutates nothing. Repeated preparation/discard is harmless. Accepting an independently checked private solve/cache install advances cache, duration and piece together exactly once. Old/foreign preparations refuse. Ordinary refusals preserve the installed pair. World/body/manifold reset, disposal or unsafe post-mutation failure invalidates the lineage. A canonical refusal after an accepted private solve would retain the NEW pair; host/canonical acknowledgement does not produce a new solve duration.

Use signed normalized products `lambdaSig*newSig/oldSig` with checked exponent combination before projection. Never project h_new/h_old first. Retain a scaled impulse channel independently of BEPU's rounded transport cache; compare the latter against its last accepted projection before use. Zero/new caches have no fabricated previous duration. Reject unsupported provenance and arithmetic explicitly; no clamp or minimum ratio. Verify ordinary→2^-1075→ordinary cache roundtrip as a representation test only, not the later full tiny-world gate.

## Scoped initialization policy

The first supported solver fixture remains the retained four-contact convex box/planar slab, fixed dry spherical inertia, pinned material and eight normal→tangent-block→twist sweeps. Admission checks actual retained identity/features, finite geometry, positive penetration within .020 m, current and previous inward support load, current contact-normal speed within the existing .0005/(16667/1e6) support-speed bound, and finite feasible cached normal/friction impulses. These are concrete support predicates, not a caller-supplied steady flag. Unloaded compressed contact, missing/changed support or unqualified cache transport refuses; no fallback reset is introduced.

Ratio scaling is a predictor within the admitted regime, not an exact universal physical law. Preserve the unloaded soft-contact counterexample. Prepare ratio for all seven components first, then use the unchanged CommonNormal current-piece load correction on normal rows only. The accepted source remains linked unchanged. Current h, not cache h, supplies physical/free prediction and spring/effective coefficients.

## Bounded execution and bars

1. Debug and Release build of only the new standalone prerequisite project. One Debug and one Release permanent lifecycle/component test process. Test owner/sequence/generation/refusal/speculation/invalidation; exact/scaled duration storage; all-component tiny roundtrip; five ratios1,1/2,1/4,2,actual1/128 for separate normal-only, nonzero-tangent and nonzero-twist analytical support fixtures. Compare unchanged eight sweeps to the independent coupled reference and explicit steady rates. Reference physical input cross-check uses the closed-form box wrench. All original1e-4 impulse/linear/angular bars remain; current spring-coefficient equation check is explicit. Doubled ratio is an algebraic component test, not expansion of the powered interval domain.
2. One fresh Debug retained-world process: same120 stock1f/60 preparation steps, exact original1/128 powered piece, source8+1/128 kg, midpoint8+1/256 kg,32 N. Require all1e-4 bars. Then20 frozen-input duration/load/midpoint compositions (ratios1,1/2,1/4,1/8,actual; loads0/16/32/64), all same bars. No individual world recreation.
3. In that SAME world, install accepted powered endpoint/cache/provenance, then coast through the exact remaining8853.5 ticks at mass8, zero thrust. Refresh real geometry; use newly accepted powered duration and previous32 N load. Verify same body/constraint/features, piece order, checked install, physical continuity, original accuracy/support/penetration bars. Stop on first genuine failure before installing any failed result. Do not retry, tune or add another correction.
4. Only after the complete interior passes: equivalent Release sequence once, then retained-world tiny event, selector, off-COM, long support, M15 no-constraint comparison and deterministic repeat, in that order. Before each applicable gate derive the actual bounded reference and use original declared bars. The known need for a scaled real-world tiny response is not waived by the early arithmetic roundtrip. A newly exposed representation/physics conflict stops the ticket.
5. Only after all correctness: exact-zero allocation with existing checked helper/positive control for complete/no-work/refusal/tiny paths, bounded storage and direct normal-runtime Release timing. No performance/allocation campaign after a correctness failure. Formal counts will be declared before those executions if reached.

No production source/permanent Simulation-runner change is required to establish this standalone prerequisite. The new standalone tests are retained permanent qualification tests, not proof of production integration. Full operator PASS requires every deferred gate; an early stop leaves it UNQUALIFIED. Preserve concise source, report, results, hashes and reproduction; do not rewrite earlier failures.
