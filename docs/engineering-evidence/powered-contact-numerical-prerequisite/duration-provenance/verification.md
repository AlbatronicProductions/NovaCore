> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Independent verification and claim limits

Independent read-only verifier: `/root/powered_final_verifier`. Verdict: **REVISE / operator UNQUALIFIED**. The final review ran no tests and changed no files.

## Findings resolved before the actual retained run

- Bind completion to the exact prepared installation, preventing Begin(A)/Complete(B) with differing durations from promoting a different tuple.
- Include angular external load in compressive-support admission; normal external acceleration uses both translational and inverse-inertia torque response.
- Check the ordinary duration ceiling even when load is unchanged.
- Verify native seven-component cache readback before accepted provenance promotion; keep failures inside the invalidating installation boundary.

These were local lifecycle/admission corrections before observing the actual coast failure, not changes made to rescue that failure.

## Final result review

- The actual powered install writes private cache and endpoint, verifies their cache transport, then calls `CompleteInstall`. All seven projections are checked before promotion.
- There are exactly 20 unique passing duration/load cases: the original plus 19 additional cases, not 21 solves.
- Observed maxima are 3.0528216343195247e-6 N.s normal impulse, 7.747304912061694e-7 m/s linear and 1.3497906143186619e-6 rad/s angular error, all below 1e-4.
- The coast record contains `UnsupportedSupport` at admission. It contains no coast solver or reference result and no coast installation.
- The earlier `Prepare` branches passed, so world/body/constraint identity, features, endpoint velocity and native cache projection matched the accepted lineage. `Supported` contains multiple predicates; the failing one is **UNKNOWN**.
- The recorded last accepted cache, endpoint and duration exactly match the powered installation. Piece remains 1 and duration remains 1/128 s. Private continuation is invalidated after coast preparation. Whole-private-world nonmutation is not claimed because collision refresh already occurred.
- No canonical capability is present in the new cache owner; production and existing source are preserved by final fingerprints.

## Red-team disposition

| Attack | Evidence / disposition |
|---|---|
| Duration detached from installed cache; speculative promotion | Owner-bound prepared tuple, exact completion match, native readback before promotion; lifecycle tests pass |
| Stale cache generation/reset; old piece replay | Explicit lineage and monotone piece checks; invalidation/refusal tests pass |
| Rounded-zero tiny duration; overflowing standalone ratio | Exact/scaled provenance and all-component full-product arithmetic tests pass; full tiny-world consumer UNQUALIFIED |
| Normal/tangent/twist mismatch, friction caps | Separate nonzero component fixtures across five ratios pass; no clamps in witnesses; changing-basis/general-friction qualification not claimed |
| Old softness; twice/reversely scaled load correction | Current-duration coefficients and actual linked implementation inspected; duration-first then unchanged CommonNormal verified |
| Wrong coast predecessor | Actual coast admission records powered piece-1 h = 1/128, then refuses support; coast solve UNREACHED |
| Selector physical duration confusion | New selector gate UNREACHED; no selector change or qualification claimed |
| Off-COM, long drift, M15 no-contact mismatch | UNREACHED; no broad safety claim |
| Hidden solve in duration transform | Transform is a fixed seven-component significand/exponent operation; linked eight-sweep solver unchanged |
| Per-step allocation or provenance growth | Fixed provenance fields; warmed allocation and complete storage UNREACHED |
| Scaling presented as universal law | Explicit support admission and retained unloaded counterexample disprove universal exact conversion |
| Canonical resource/actuator mutation | No live integration or canonical capability; existing files byte-identical |

The first unsupported actual continuation is a qualification blocker. The evidence does not prove a duration-scaling numerical error, an exact normal-basis mismatch, or coast divergence. Do not loosen the scope or repeat qualification automatically.
