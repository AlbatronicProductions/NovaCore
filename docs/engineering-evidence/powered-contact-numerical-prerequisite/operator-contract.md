> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Preregistered numerical prerequisite and early rejection gate

Status at declaration: no numerical probe has run. This is not a production operator contract or a qualified supported-propulsion capability.

## Scope and order

Preserve all existing source, tests, dependency bytes and architecture evidence. Use a standalone diagnostic executable, with the banked exact-duration arithmetic compiled by source link and the repository-qualified BEPU binaries. No canonical transaction, resource debit, actuator mutation, solver world reconstruction or presentation code is involved.

First compare at most three numerical mechanisms at their necessary scalar contact-row boundary:

1. Exact external preintegration followed by ordinary FP32 constraint work.
2. Similarity-scaled stepping with the current constitutive equations; separately check stable algebra to distinguish representability from reciprocal overflow.
3. External preintegration plus a factored binary64 compliant-row adapter, retaining the original impulse as the initial iterate. Compare to ordinary binary64 updates to expose cancellation. Also reject the weaker force-only endpoint-residual shortcut analytically. Only the isolated normal-row algebra is initially implemented; coupled rows/friction/active-set and retained-world integration are separate obligations, not implied by scalar success.

The row is the zero-lever-arm normal component of an 8 kg body against an immovable plane, with current 30 Hz / critical-damping spring and recovery limit 2 m/s. It is a necessary primitive test, NOT a substitute for finite-body geometry, four-row selection, friction or a retained-world trajectory. Execute exactly eight correction iterations, the current production budget. Existing impulse is neither cleared nor rescaled.

Early process budget: one Debug and one Release execution of the same deterministic primitive witnesses, each rebuilt once before execution. A technical harness compilation failure may be repaired; numerical results may not trigger retries or threshold changes. If no mechanism survives, stop before world/long-sequence/allocation/performance qualification. A passing rejection witness is not an operator PASS.

## Independent primitive bars

- Exact duration authority must remain positive. The banked witness has fuel=2^-1074 kg and flow=2 kg/s: h=2^-1075 s. A 16 N force on 8 kg gives the independently exact 2^-1074 m/s free increment. Require the exact represented binary64 bit (one), not a tolerance swallowing zero.
- Also use h=2^-140 s, positive even in FP32, to separate duration-cast loss from spring reciprocal overflow. All constitutive coefficients must be finite; compare to the algebraically stable binary64 reference.
- Normal-row response at depth=0, fixed mass=8 kg, coast h=1/60 s and inward velocity -2^-150 m/s: the independent solution is v=s*v_free and lambda=-c*v_free/K, where K=1/8, k=(omega*h)*(omega*h+2), c=k/(1+k), s=1/(1+k). The active branch is strictly positive. Its velocity and impulse must both remain nonzero and have relative error <=2e-6 (a declared scale-separated response precision, NOT a generic relative bound on subnormal FP32 rounding; equivalent absolute bounds are emitted). This tests a represented incremental contact response, not real-number exactness or full body output accuracy.
- Run cold and unchanged warm impulse=1 N s only as primitive initial-guess controls. Arbitrary cache initial guesses are valid inputs to the projected iterative equation; this does not claim that this cache/velocity pair was captured from a settled physical world. The final exact equation is independent of the initial guess. The warm/cold comparison cannot itself qualify or condemn full-world stability.
- A similarity transform must preserve omega*h, friction, damping and all dimensional conversions; no minimum dt or physical spring change.
- Any force residual claimed to survive contact must undergo the same row response. For the above active row, post-adding the full free increment instead of s times it is a structural failure regardless of its small absolute size.

These bars are fixed before executable results. They are early necessary conditions for a representation that promises this incremental precision, not new banked gameplay thresholds.

## Full qualification bars if a mechanism survives

Dedicated fixed-COM dry-8 kg body with fixed dry inertia and central point resource; no reinterpretation of the banked 1,000 kg article. Start with the 2 x 1 x 1 m box and planar slab. Use current eight iterations, current materials and four-row policy where compound selection applies. Gravity 9.81 m/s^2. Source mass 8+1/128 kg, positive successor mass >=8 kg. Ordered powered/coast pieces; immutable input only.

- Peak geometric penetration <=0.020 m; established support drift <=0.0005 m and support speed <=0.0005/(16667/1e6) m/s, retaining applicable M14.21 thresholds.
- Centered support reference: Jn = g*h*(ma+mb)/2 - T*h within a motionless supported powered piece. Coast uses successor mass. For moving compliant support the exact balance adds mb*vb-ma*va+q*(yb-ya), q=(ma-mb)/h. Endpoint momentum alone is incomplete when mass changes. Proposed absolute impulse bar 8 kg * 0.0005/(16667/1e6) m/s, tied to the existing support-speed budget, subject to a stronger analytically derived per-piece bar before world execution.
- No-contact position/velocity <=1e-6 m / m/s, orientation/angular velocity <=1e-9 rad / rad/s where within the banked M15 domain; no claim outside its model. A first-order position update is not silently exempted.
- Off-COM force/moment use fixed dry inertia and evolving orientation. Derive a converged independent coupled oracle and angular error/work bound BEFORE any off-COM candidate run; this remains a prerequisite to entering that gate, not a chosen post-hoc tolerance.
- Long support: 1,200 exact integer intervals, final 600 supported; strongest sequence repeated twice, same-build endpoint identity. No positive secular energy/penetration growth or repeating mass-change oscillation within the above geometry/speed bounds.
- Exact zero warmed bytes and deliberate allocation control, separately from timing. Only after correctness passes: bounded retained bytes and one direct Release timing sample with explicit counts; no frame campaign.

## Mass cadence and selector requirement

No mass cadence is qualified by this declaration. A complete candidate must specify source, stage and successor mass consistently for force integration, inverse properties and selector predicted linear/angular motion. A convenient logarithmic mean is not an exact universal contact mass. Source/endpoint patching and stale acceleration are prohibited. No full solver or selector migration is authorized by a passing scalar witness.

Evidence budget: 128 KiB, excluding separately classified rebuildable diagnostic outputs. Retain source, equations, concise results, hashes and reproduction only.
