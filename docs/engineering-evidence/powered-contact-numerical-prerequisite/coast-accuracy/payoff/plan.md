> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Measured payoff closure — predeclared bounded plan

Baseline HEAD/main/origin/main/remote main 49057fecceb0f725d5f551ec40e2780971b0d81d; branch codex/powered-contact-numerical-prerequisite. Preserve all 2,104 starting nonignored files and 65 tag refs, including all prior coast-accuracy evidence. Only new payoff/ evidence and one ignored build/powered-contact-coast-payoff tree. No production/permanent-test change, adoption, cache/world install, banking or reopened cause investigation.

## Correctness sequence (one Debug process)

1. Verify current exact coast patch/A/rhs against retained convergence.json, source-linked original baseline eight-sweep result, and accepted guess. Run original retained D formula then unchanged PieceKernel.Solve for eight sweeps. Construct D before reference; persist full measured inputs, initialization, final cache/velocity, error/residual and predicates using FileMode.CreateNew + FileStream.Flush(true), close, then proceed. Any failed predicate is persisted before stopping.
2. Same ordinary initialization, unchanged operator: measure 9,10,11 sweeps once each; write each immediately. Twelve remains the prior measured comparator and is rerun only if necessary for Release same-process equivalence. Determine smallest tested passing count in 9..12; no increase adopted.
3. Four exact prior synthetics: identical, tiny-normal, tangent-only, lever-only. Original D plus eight actual source-linked solver sweeps, each persisted immediately. Stop on physical failure. Check same-input baseline reproduction and pre/post-D friction domain.
4. Moderate-normal and normal-and-tangent: admission only. Persist expected refusal with caps and unchanged input; no reference construction or solver call on these arms.
5. Check new diagnostic storage adapter bit-identical to original D/solver output (all seven impulses, six velocity components, clamp counts), including each measured ordinary count. Do not change numerical expressions/order. Write summary only after per-case files; authoritative cases survive a later failure.

The nonallocating adapter removes only Clone/LINQ ownership conveniences: one caller-owned double[7] buffer reset from the same immutable initial cache per operation. D computes the same current A/b terms and same q-residual arithmetic in place before identical eight sweeps. It checks current input/output feasibility, finite positive h/omega/denominator and finite result, without epsilon/clamp. No reference argument exists in its preparation API.

## One fresh Release direct-cost/allocation process, only after correctness passes

Build Release once. Verify original source-linked solver versus storage adapter on baseline8, D8 and the smallest measured passing ordinary count. Every impulse/velocity/clamp count must be bit-identical. These are small same-process equivalence checks, not new causal runs.

Preallocate patch, seed/work buffer and sample arrays. Warm 4,096 calls per arm, fixed in advance to exercise both preparation and solving before samples; no warmup adjustment or runtime/tiering/PGO changes. Measure 4,096 individual calls per arm with Stopwatch.GetTimestamp, rotating ABC/BCA/CAB order per round. Every call resets the same frozen seed. Consume returned velocity in a retained checksum outside each timing interval. Include seed copy, D checks/preparation where applicable, unchanged solver, and endpoint velocity reconstruction. Exclude cold parsing/patch construction, reference, serialization and reporting. Report median, nearest-rank P95/P99, max, absolute differences, quartile-block medians and raw timestamp-pair overhead (no subtraction). No batching; do not label this whole-frame cost. One process, no timing retry.

After timing, use unchanged source-linked OrdinaryAllocationMeasurement (checked 1 MiB temporary no-GC region, entry/exit hard failures). Measure 4,096 warmed D8 calls, reset per call; no reporting inside window. Require exactly zero; a nonzero result is persisted as FAIL, not subtracted. One independent NoInlining byte[128] positive control under the same helper must report nonzero; expect one object, nominal x64 152 counter bytes, actual delta governs detection. No managed-object observer or full allocation campaign. Allocation scopes apply to the storage adapter, not the original allocating diagnostic API or an integrated world.

Durable allocation/timing files and an attempted-process marker prevent overwrite/retry. If the process fails, prior case records survive. No coefficient tuning, third class, D+extra sweeps, cap changes, or unrelated qualification.

Recommend only from measured correctness/domain/direct cost/allocation/storage/lifecycle tradeoffs. If neither D nor ordinary extra sweeps clearly wins, return both to Project Control.
