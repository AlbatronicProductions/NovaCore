> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# First-coast accuracy attribution — predeclared bounded plan

Preserve all 2,088 current nonignored files and 65 historical tag refs. Baseline49057fecceb0f725d5f551ec40e2780971b0d81d, branch codex/powered-contact-numerical-prerequisite. No production, prior probe, permanent test, or prior evidence changes.

Source-link the unchanged PieceKernel, BasisProbe transport, DurationInitialization and CommonNormal. Read the retained exact coast capture. Recompute transport/duration/load initialization and require bit-identical initial cache, final8-sweep cache/velocities, and physical errors against basis-compatibility/original-coast-witness.json. STOP if replay differs.

One attribution execution: actual candidate sweep counts1,2,4,8,12,16,24,32. No higher/adopted count and no timing/allocation campaign. References must remain interior/admissible with residual<=1e-12. Physical bars remain1e-4. A false reference-admission control is recorded and not scored using an invalid reference.

Construct current A=K+alpha*diag(K_nn), alpha=1/[omega*h*(omega*h+2)], b=bias-J*vfree. Independently replay block Gauss-Seidel for normals0..3, tangent2x2, twist. Compare signed results to actual candidate within1e-12. Record stage residuals and clamp feasibility. Derive ordered homogeneous map G; decompose initial error into mean, X, Z, checker pressure modes, tangent block and twist. Sum propagated signed vectors before taking norms. Require reconstruction<=1e-12; do not extend linear claims through any clamp.

Controlled anchor P is the accepted powered PIECE EQUATIONS: old geometry/depth/source velocity, midpoint mass8.00390625, h=1/128, force32N. Its initializer is the accepted powered OUTPUT cache, with its actual producing duration/load, used as a historical guess. It is not oracle initialization. Verify P passes before calling it a passing control.

From P substitute only: engine force0; current h; dry mass; refreshed lever/normal geometry with old depths; new depths with old geometry; successor linear velocity; successor angular velocity; successor full velocity. Always use the same accepted historical cache, valid basis transport, correct duration scaling and current load correction. Also compare load32 versus0 at the POWERED SUCCESSOR state/geometry/depth, keeping producing h/mass fixed.

For full-coast anchor C, add a preregistered2x2 velocity/depth factorial: old/new source velocity crossed with old/new depths, while current h/mass/geometry/load remain fixed. Signed final cache errors get exact main-effect and interaction decomposition. Do not add norms as causal fractions.

Separating-speed sensitivity: frozen C equations except linear Y set to0, actual Y, and admitted bound .0005/(16667/1e6). Keep angular velocity/XZ fixed; report actual derived contact speeds (some may exceed the point-speed bound at the nominal body-Y upper value). No support-domain reinterpretation.

Frozen equation-term attribution distinguishes source J*v, gravity/engine h terms, contact bias, K and spring alpha. Mathematically decomposition may use the reference for verification, not to construct an initializer. Source angular and tangential velocities are derived, never independent simultaneous physical knobs.

After initial evidence establishes a dominant mismatch, separately predeclare and execute ONE diagnostic initialization D removing only that mismatch, with no oracle value in its inputs/call graph. Compare A/B/C/D at the same8 sweeps and same equations. No stacking correction, no install. Reuse all six admitted prior synthetic systems to test the same mechanism; their recorded initial caches are fixed causal witnesses, not live configurations. If D fails the bars, report incomplete cause.

Stop after cause and one correction-class recommendation. Costs are arithmetic/storage estimates only; no performance, off-COM, tiny-event, long-support, selector or M15 qualification.
