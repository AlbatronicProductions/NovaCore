> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Exact exhaustion input refinement before Phase 6

Synthetic and local Debug/Release gates have passed. No retained-world corrected piece has run. The initial contract's 8,333+8,333 tick split was a numerical partition, not yet tied to an exactly decoded fuel/flow pair. Replace that planned input before execution with a genuine exact resource exhaustion, without changing physical acceptance bars:

- initial fuel:1/128 kg; dry mass8 kg; source8+1/128 kg;
- flow:1 kg/s, exactly representable; centered engine32 N;
- admitted integer interval:16,666 ticks;
- powered exact duration:(1/128 kg)/(1 kg/s)=1/128 s=7,812.5 ticks;
- coast exact duration:16,666-7,812.5=8,853.5 ticks;
- exact integer units and banked PoweredFlightNumerics.TrySeconds derive both piece scales;
- banked TryStageMass derives powered midpoint8+1/256 kg and dry successor8 kg.

One Debug execution only. Same retained BEPU world prepared by120 original ordinary steps, then current geometry refreshed for the powered piece. Require converged-reference applicability/residual and all three1e-4 local physical error bars before installing that result or advancing the coast piece. Stop immediately on a material failure. Do not infer its cause merely from duration correlation or change the preconditioner after observing it.

The new correction still models a load change at the current piece duration/mass; it does not claim a duration-history cache policy. This test decides whether the unchanged eight-sweep continuation is adequate across that boundary.
