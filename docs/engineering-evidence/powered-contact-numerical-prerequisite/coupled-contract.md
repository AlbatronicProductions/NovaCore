> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Candidate C: preregistered two-row continuation

Declared after scalar Debug/Release, before any coupled execution. This continues C; it is not another numerical candidate and does not retry either completed scalar run.

Scalar C survived. Continue within the authorized private-operator scope. Do not stop merely because coupled work is not implemented. The normal-row proof below still cannot replace a finite-body/world qualification.

## Fixed problem

Two planar normal rows at body-relative x=-1,+1 m, upward normal, dry mass 8 kg and fixed Iz=2 kg m^2. Zero penetration bias, current 30 Hz / critical damping; eight ordered PGS sweeps. Matrix K has diagonal 1/8+1/2=0.625 and off-diagonal 1/8-1/2=-0.375. Warm guesses (1,1) N s are retained; cold guesses (0,0) are a control. Normal and angular increments are both nonzero. No friction is claimed by this primitive.

Use separate ordinary baseline and normalized response component with a common power-of-two scale. Do not prematurely add the increment to the baseline. Propagate the normalized response through other rows' cached impulses, not independently through two scalar gains. Guard the active-branch assumption: every baseline row iterate must remain strictly positive; otherwise this bounded linear-response primitive refuses. General active-set/friction work remains pending.

Reference: independently form the 2x2 Gauss-Seidel affine sweep matrix and its first/eighth powers. Compare both baseline and normalized response after one and eight sweeps. Require absolute error <=1e-12 for baseline/normalized values (conservative accumulated binary64 arithmetic allowance for this bounded 2x2 problem); no native subnormal relative-error claim. Final projections at scale 2^-1074 allow one binary64 epsilon absolute error and require a nonzero represented contact impulse. The 2^-150 projection must preserve nonzero velocity/impulse response.

Check h=1/60 s; positive tiny h=2^-151 s then coast; changed mass 8+1/128 -> 8 with force, K and prediction all supplied the SAME per-piece midpoint mass as a candidate approximation. This primitive establishes only input-cadence consistency, not full changing-mass physical accuracy. Use current inverse Iz throughout. Residual force and torque are immutable values. Zero residual control must produce zero residual outputs, even with warm baseline guesses.

One Debug and one Release process, each built once. Identical results required. No tuning. A failure must be retained and classified before any correction. Scalar source/results remain untouched.

Remaining full gates: finite-body geometry/selector, friction/active changes, complete exact duration integration, changing-mass reference, off-COM trajectory, long support, free comparison, allocation/storage/performance. No acceptance claim from this primitive alone.
