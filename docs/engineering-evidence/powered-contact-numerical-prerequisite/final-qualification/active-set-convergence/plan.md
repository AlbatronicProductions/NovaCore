> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Bounded active-set convergence/payoff study

No original source, prior evidence, production or permanent test modification. No correction
is adopted. Preserve D, transport, duration preparation and friction equations. All additions
are isolated diagnostics in this directory. No cleanup of previously blocked paths.

1. Run the retained frozen boundary diagnostic binary once into a fresh subdirectory. Require
   exact equality of historical/preparation/proposal/error/clamp evidence with the previous
   baseline eight-sweep failure. If it differs, STOP.
2. Generate a diagnostic solver copy with parameterized iteration count and optional trace;
   verify the same initial state and exact eight-sweep result. Use counts 1,2,4,8,9,10,11,12,
   16,24,32, stopping once the curve and first tested passing count are clear. Preserve the
   frozen 1e-4 normal-sum/linear/angular bars. Known curve failures are measurements, not retries.
3. Trace individual normal/tangent/twist updates and normal residual before/after tangent.
   Independently reconstruct error propagation and decompose common/differential normal,
   tangent and twist contributions. Keep any oracle initialization explicitly diagnostic.
4. Compare actual, scaled-interior, zero-friction and cold diagnostic histories against the
   SAME current equations. Compare tangent-only initialization arms without adoption. Build
   bounded source-law accepted-history witnesses for the four active-set transitions.
5. Compare D and no-D at the same tested counts for the actual coast. Predict the powered
   branch from its own saved inputs; execute it only if prediction cannot settle attribution.
6. Only after a passing bounded count and causal checks, compare local Release cost of D+8,
   D+first-tested-pass and no-D+same count. Predeclare timing workload and sample count before
   execution; keep tracing/reporting outside timing. This is local payoff, not integrated
   performance qualification. No threshold or sweep policy is adopted.
7. Independent review, one correction-class recommendation, identity checks, STOP for Project
   Control. No remaining prerequisite campaign, live integration, milestone or banking.

Unexpected material failure of reconstruction, identity, equations or reference validity stops
the study. A known numerical failure in the declared controlled curve/arms is retained openly.
