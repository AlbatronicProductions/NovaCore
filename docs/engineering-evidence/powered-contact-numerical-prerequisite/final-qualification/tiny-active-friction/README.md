> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Exact tiny active-friction proof: reference qualified, continuation admission refuses

## WHAT HAPPENED

**REVISE. B — ACTIVE-FRICTION REFERENCE QUALIFIED AND CANDIDATE FAILS.**

The unchanged consolidated operator passes both tiny active-friction proposals and normal
installations. The first following-coast call, baseline branch, returns `BasisRefusal`.
No powered coast call, coast sweeps or coast installation followed. No correction was made.

The pinned law uses four normal impulses, one shared circular tangent impulse and one twist
impulse. Independent1100-digit references and separate original-equation/inequality checks
qualify the actual tiny active set and the different following-coast set. See
[friction source contract](bepu-friction-contract.md), [derivation](reference-derivation.md),
[tiny comparison](tiny-comparison.json), and [coast reference](coast-reference.json).

## WHY IT MATTERS

The tiny-reference blocker is resolved, and the candidate's tiny represented response is
correct within the frozen bars. The complete constrained numerical prerequisite is still
unqualified: its own accepted saturated cache cannot enter the next-piece transport path.
This is an admission-composition failure, not a demonstrated D/eight-sweep accuracy failure.

## ACTIVE-FRICTION REFERENCE

Both branches independently have four positive normal impulses, saturated tangent with the
guard inactive, and negative saturated twist. Normalized original-equation/projection/KKT
residuals:2.54105548e-1100 baseline,3.23142553e-1100 powered, versus1e-900 bar.
Raw tangent1.51667704e-16 exceeds guard1.00000002e-16 and cap2.42333029e-323 N s.
Raw twist-6.91914452e-20 exceeds the negative cap-2.70936563e-323 N m s. Boundary, sign,
positive normal, finite value and bias-branch checks pass. KKT is conditional/fixture-specific.

## BASELINE VS POWERED RESPONSE

| Output | Independent true response | Candidate represented-field comparison |
|---|---|---|
| Linear velocity | Y=4.940656458412465442e-324 m/s; X/Z below binary64 range | Scaled increment Y projects to exactly+epsilon; rounded absolute endpoints identical, as expected |
| Angular velocity | Components2.076e-646,1.715e-645,7.376e-646 rad/s | Rounded absolute and scaled increment pair differences zero, as expected |
| Four normal impulses | About-6.134924e-645 N s each | Rounded53-bit cache pair differences zero, as expected |
| Tangent impulses | (+8.311889e-646,+2.952702e-645) N s | Rounded53-bit pair differences zero, as expected |
| Twist impulse | +3.429527e-645 N m s | Rounded53-bit pair difference zero, as expected |

The nonzero real differences are retained in [separated response](reference-results/separated-response.json).
Zero in a rounded absolute field is not presented as preservation of a sub-ULP response.

## TINY PIECE

Frozen before execution: existing1e-4 absolute physical bars, exact rounded endpoint bits,
<=2e-6 normalized relative error per scaled cache/increment component, exact reference-derived
epsilon projection in the increment response, and zero other represented pair differences.
All pass. Maximum scaled cache relative error:1.86928282e-16 in each branch.
Maximum increment relative error:1.01513552e-13 baseline;1.72090945e-15 powered.
Each branch has0 normal,8 tangent and8 twist clamps during exactly eight sweeps.
These are normal pinned solve projections, not admission repairs.

## FOLLOWING COAST

Exact duration16666/1000000-2^-1075 seconds. Independent references from both actual installed
sources and both unrounded tiny reference sources pass: all normals active, tangent/twist
interior, maximum direct residual9e-1101. Actual represented source references agree.
Unrounded reference-source coast Y response4.19844217e-325 m/s rounds to zero. The discarded
tiny cache differential after duration scaling is representable as[-8,-8,-8,-8,+1,+4,+5]
binary64 epsilons; this is an initializer witness, not a converged reaction claim.

Candidate baseline coast: `BasisRefusal`, before transport elimination/duration/load/D/solve.
Absolute coast errors and response comparison: **NOT PRODUCED**. Powered coast: **NOT RUN**.
The saturated-to-interior transition is qualified only in the reference, not the candidate.

## PROVENANCE / EXACT EVENT AUTHORITY

Two retained worlds, one per matched branch, with no reconstruction. Both tiny installs
advance accepted generation1->2 and piece0->1 exactly once. Producing geometry/cache and
exact duration advance together. Explicit numerator/denominator limb arrays verify the
positive exact2^-1075-second event; its numerical FP64/FP32 duration projection is zero.
Seven owned scaled impulses remain nonzero while their native FP32 projections are zero.
Both accepted load histories remain distinct. No canonical engine/resource/actuator exists
in this standalone driver, hence no canonical mutation. Coast refresh marks native state
unsafe; accepted tuples remain unchanged on refusal. Both worlds are disposed/invalidated
on process exit. No unsafe continuation is retained.

The first refusal is the old-cache strict tangent comparison. Saved magnitude and cap are
identical, mantissa1.2262187786083485/exponent-1072. Twist magnitude and cap are independently
identical, mantissa1.3709542721275163/exponent-1072. See [admission witness](admission-witness.json)
and unchanged `tests/NovaCore.ContactNumerics.IntegratedProbe/GeneralizedTransport.cs:10,17`.
The returned default target is not an accepted zero endpoint.

## EXECUTION / RED TEAM

SDK10.0.303; .NET10.0.12. Two Debug driver builds passed, zero warnings/errors; the second
added the bounded coast staging/pause before any candidate execution. One candidate process
(PID44164, managed thread2) ran the two tiny proposals, two installations, and one failed
baseline coast admission. No retries. Separate cold reference calculations and a captured-
input arithmetic witness are not additional candidate runs. A PowerShell metadata-list
command had a syntax error, was corrected, and affected no numerical run or source identity.
No Release/allocation/performance/long/regression campaign followed the stop.

Independent reviewers passed the reference and reached tiny evidence, and returned one
concrete REVISE for output/input admission incompatibility. [All20 attacks](verification.md).

## IDENTITY / REPRODUCTION

HEAD/main/origin/main/remote main:49057fecceb0f725d5f551ec40e2780971b0d81d.
Branch:codex/powered-contact-numerical-prerequisite. M15.0 remains
4607d8c802006d5e1a01c595ab608cf53a4dab6b; all historical tags including M14.21-M14.24
remain protected. All2163 prior nonignored files remain byte-identical. Production,
permanent registered tests, all prior probe/evidence files and Blender are untouched.
No tracked changes or staged paths. Additions are confined to this evidence directory.
See [identity](identity.json) for measured fingerprints and disposable disposition.

This ticket's new generated scratch is33 files/2,172,802 bytes: bin9/1,504,811;
obj22/578,962; Python cache1/34,779; console log1/54,250. The single reviewed removal
attempt was rejected by automatic approval review as "blocked by policy" before execution.
No deletion was retried or worked around. These are disposable, not evidence inputs.
Prior stopped-ticket scratch was left untouched. No bulk source tree or runtime dump was
created. Retained numerical JSON contains the precision needed to reproduce this proof.

Reproduce only in a fresh output directory with Python3.11 stdlib and the pinned SDK:

```powershell
& 'E:\NovaCore\docs\engineering-evidence\powered-contact-numerical-prerequisite\final-qualification\tiny-active-friction\reproduce.ps1' -OutputDirectory 'E:\NovaCore\build\tiny-active-friction-reproduction'
```

The driver pauses between proposal, independent comparison, installation and coast reference.
The reproduction script advances only after each check and expects the retained first coast
refusal. It does not retry, weaken a gate or run later qualification.

## WHAT HAPPENS NEXT

STOP FOR PROJECT CONTROL. No numerical correction, no change to D/eight sweeps/friction,
no live powered contact, departure, remaining prerequisite campaign, M15.1 or banking.
**UNBANKED.**
