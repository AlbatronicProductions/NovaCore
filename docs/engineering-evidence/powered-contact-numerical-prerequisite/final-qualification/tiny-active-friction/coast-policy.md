> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Following-coast policy, before its first candidate call

The tiny pair passed and was normally installed. Keep two retained worlds. Both generation2
sources have identical represented velocity/pose/scaled-cache fields but different accepted
load histories (zero engine versus16 N). Do not erase either history. Use the exact remainder
16666/1000000 - 2^-1075 seconds with its original integer numerator/denominator provenance.

The existing positive-exact-duration geometry query produced each actual current coast
geometry and captured native cache before any candidate coast solve. Freeze two independent
references from each actual installed source, and also two from the unrounded high-precision
tiny reference endpoints. Preserve the branch-specific cache and producing duration in the
record. The converged current-equation reference does not treat warm start as final state.

Test the interior seven-equation reference first; admit it only if all normals are positive,
tangent and twist are strictly admissible under the pinned maps, and direct final-velocity
equation residuals are <=1e-900. If it fails, stop rather than impose its active set.
Report the discarded tiny cache differential multiplied by h_coast/h_tiny: a tiny double-zero
alone does not prove absence of downstream representable cache response.

If reference admission passes, candidate admission itself must succeed with the original
basis transport, duration scaling, load correction, D, feasibility and eight sweeps. Do not
modify strict prepared-cache feasibility or add active-cache fallback. Any refusal stops
the run immediately; no powered-arm retry or bypass after a baseline-arm failure.

For an admitted candidate keep each branch's existing1e-4 normal-sum/linear/angular physical
bars against its installed-source reference. Compare the copied native FP32 endpoint branch
difference to the independently projected reference branch difference exactly; here the
installed-source references may agree even when unrounded physical references differ.
For scaled cache/increment branch differences, compare against differences of the independent
53-bit projections within one ULP at max(abs(reference baseline component), abs(reference
powered component)), separately for each corresponding physical component (a fixed represented
resolution bar, not an ordinary1e-4 allowance); explicitly report true unrounded response.
Do not infer that nonzero ideal sub-ULP responses survived. No qualification claim is made
until both absolute and response gates, installation and lineage checks pass. The admitted
reference is the unique solution of the interior linear system, not a global uniqueness
proof over every possible active set. The one-ULP response gate is a new frozen resolution
witness, not a theorem about eight-sweep roundoff.
