> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Bounded active-friction reference plan

Declared before new reference or candidate execution. The existing consolidated operator,
D, eight sweeps, friction law, source world, fixture and every prior file remain unchanged.

Read pinned BEPU commit f73164bb3c9ca733eb3329f1f6b1cea4e216ece7 first. Use the actual
four-normal/shared-tangent/shared-twist block law, including the tangent denominator guard.
Import retained source binary64 values exactly. Preserve the actual FP64-prepared Jacobian
moment arms and radii; evaluate their equations independently at 1100 decimal digits.

The reference solves four normal equations by pivoted elimination after substituting the
candidate active tangent direction and twist sign. Independently solve each engine branch's
direction with at most six fixed-point updates. Recheck the unscaled original equations,
normal signs, pinned projection, cap boundaries and conditional friction stationarity in a
separate 1200-digit formulation. Required normalized residual and conditional KKT residual:
at most 1e-900. Verify active-set inequalities rather than assuming the old interior solution.
An independent read-only reviewer will cross-check the arithmetic and projection policy.

Only after reference qualification, freeze a separate response/representation policy and
reference hashes. Then run one baseline and one powered candidate tiny proposal, unchanged.
On any material candidate failure stop without correction or installation. If both pass,
install normally and verify exact duration/cache/geometry generations; only then qualify
the two branch-specific following coasts. No later prerequisite campaign is authorized.

Report high-precision differences AND differences of individually rounded represented
fields. They are not interchangeable. A difference that cannot fit in the absolute endpoint
field is not by itself a candidate failure. An explicitly represented per-piece increment or
scaled cache must be judged at its own representation, including possible downstream effect.
