> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Frozen before candidate execution

The independently qualified branches both have four positive normal impulses, saturated
tangent friction with the denominator guard inactive, and negative saturated twist.
Their nonzero source motion is retained exactly. No free-flight outcome is imposed.

1. Retain the ordinary absolute velocity/angular-velocity and summed normal-impulse error
   ceilings of 1e-4 in their respective physical units. Also require the proposed absolute
   endpoint bits to equal the independently rounded tiny reference endpoint bits. All six
   absolute endpoint differences between branches must be zero. This is a new, explicit
   equality witness for this fixture, not a generic solver accuracy promise.
2. For each nonzero tiny absolute cache and per-piece increment component, require relative
   error <=2e-6 using decoded exponent-separated values. This reuses the prerequisite's
   declared scale-separated response accuracy budget; it does not relax native FP32
   underflow or allow a zero cache to pass a large absolute tolerance.
3. Subtract decoded Scaled increments before projection. The reference's Y increment-pair
   difference must match within 2e-6 relative error and project to exactly positive
   double.Epsilon (binary64 bits 0000000000000001). This value is a result of the active
   reference. It is not imported from the unconstrained free-flight requirement.
4. Other increment components and all seven cache components have zero difference after
   independently rounding each branch to its actual 53-bit scaled representation. Require
   zero difference of those candidate represented fields. Preserve and report the nonzero
   high-precision real differences separately; do not claim they survived that rounding.
5. The tiny native FP32 cache and velocity projections are checked independently. The
   reference expects all native tiny impulses to project to zero and native body endpoints
   to remain identical. Exact positive duration and owned scaled cache remain separate.
6. A tiny PASS is not a coast PASS. Following-coast reference work must retain both the
   high-precision branch histories and actual installed branch histories. A discarded tiny
   cache difference must be projected through the duration ratio before declaring it
   inconsequential. Preserve the original ordinary physical ceilings, and define the
   coast response comparison from the qualified coast reference before candidate coast
   execution. If candidate admission fails, stop; no fallback or policy correction.

Reference precision is 1100 decimal digits, with an independently formulated 1200-digit
original-equation/inequality check and a separate read-only 1100-digit implementation.
The predeclared normalized equation/boundary/conditional-KKT bar is 1e-900. All passed.
The model is the pinned BEPU block map on the captured FP64-prepared Jacobian/radius input,
not a global maximum-dissipation cone optimization.

The candidate driver links every consolidated operator file unchanged. It retains two real
worlds, one per branch, and pauses after proposals for external comparison. Installation
is authorized only on PASS. A later cold coast query uses the existing ObserveTinyGeometry
method (which accepts positive ExactEvent durations generally), then reads the actual native
cache. This splits existing geometry preparation from the unchanged operator solely to
freeze the coast reference before its first solve; it does not replace the numerical path.
