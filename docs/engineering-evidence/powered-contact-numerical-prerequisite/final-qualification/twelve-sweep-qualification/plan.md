> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Twelve-sweep simplification and constrained qualification

No banking or production/permanent-test change. Preserve prior source and evidence.
This folder contains the selected standalone prerequisite variant and bounded qualification.

## First gate, declared before execution

Generate an isolated variant with exactly12 ordinary sweeps and only D omitted. Keep the
original transport, duration transformation, common-load predictor, equations and body data.
Apply the already accepted two-stage friction policy: closed historical admission with
positive normals, arithmetic-valid intermediate transformations, closed current feasibility
before the first sweep. No clamp/reset/oracle/epsilon is added to admission.

Reuse the original small regression runner and independent PieceKernel reference. Its
physical bars remain1e-4 normal-sum/linear/angular; reference residual1e-12 and admissibility
remain required. Add one existing composition representative: actual producing duration
divided by8, midpoint mass, centered64N load. No new geometry or easier conditions.

1. Original centered32N witness.
2. Actual1/128 powered piece.
3. Captured first coast.
4. Duration/load/midpoint composition representative above.
5. Compatible generalized basis coast (captured geometry); transport error<=1e-12.
6. Original two-piece actual retained BEPU world sequence, including normal installation.
7. Actual accepted tiny baseline historical tuple to independently referenced following coast.

The existing two current-friction-infeasible basis cases and lifecycle refusal remain
required. The smallest runner covers overlapping responsibilities explicitly; it is not
seven invented fixtures. Each subprocess gets fresh output. Stop at a no-D regression;
select D+12 without rescuing no-D, as specifically directed by the simplification decision.
If no-D passes all applicable gates, freeze no-D+12. No later mechanism switching.

## Selected-candidate continuation

Resume tiny baseline then powered following coast, using the saved accepted installation,
exact duration limbs, native signed-bit cache and separate branch loads. Preserve the
previously frozen coast-policy.md: each absolute physical bar1e-4; native endpoint response
exactly equals independently projected reference response; each cache/increment response
within one ULP of the corresponding independent component scale. No response tolerance
substitution. No powered run before baseline passes.

Any selected-candidate material failure stops all later phases. Only if those pass, proceed
to boundary witnesses, selector, off-COM, long support, M15 comparison, determinism and then
integrated allocation/storage/performance. Predeclare those fixtures/bars before execution.
No integrated claims may be inferred from earlier local timing.

## Evidence and cleanup

No earlier blocked cleanup is retried. New bin/obj are rebuildable disposable outputs.
At closeout record each exact path, file count and bytes, and retain/print an explicit
PowerShell manual deletion command plus non-destructive verification. Do not delete source,
references, decisive result records or the exact reproduction inputs.
