> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Phase4: exact retained-world reference applicability first

Declared after ordinary regressions and actual powered/coast installation passed, before tiny execution.
One fresh Debug process. New cold source world uses the same2x1x1 box/slab,120 original ordinary
preparation steps and dry8kg (8+epsilon rounds to8). Exact fuel2^-1074kg / flow2kg/s gives h2^-1075s;
centered force16N, gravity retained. No alternate initial velocity, duration floor or contact clamp.

The tiny response must be validated against an admissible physical reference before accepting a result.
Current accepted independent7-row reference and two-row separated response qualify the interior branch,
not active Coulomb friction. Settled retained-world tangential velocity can be nonzero even when tiny normal
impulses/caps scale with h. First solve the actual current interior equations using exact BigInteger rational
elimination, retaining every h term. Verify zero exact residual, positive normals and the exact squared
tangent inequality lambdaT0^2+lambdaT1^2 < (.125*sum(lambdaN))^2. This avoids reciprocal overflow,
projected-zero tolerances, asymptotic substitution and a high-sweep candidate used as oracle.

Report scalars as signed mantissa/exponent approximations only; the decision is exact rational. The current
contact refresh may use the zero FP32 projection solely for the collision-envelope query at the existing
pose. No solver timestep/time advancement is supplied zero. All reference equations use exact h.

If the reference is infeasible, STOP as a material admitted-domain blocker before candidate tiny solving,
installation or coast. Do not call the infeasible linear answer a physical oracle, clamp it, erase tangents,
change the fixture or automatically introduce an active-friction correction. Return the precise required
branch to Project Control. If admitted, continue separated baseline/normalized response integration before
claiming tiny success; a single Scaled mantissa is insufficient to preserve a tiny response beside baseline.
