> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Bounded paired-response attribution plan

The current-install and actual Discord-history gate was completed first. KSA responsibility equivalence is PARTIAL. Its float timestep cannot represent the positive exact 2^-1075-second input. The retained NovaCore scaled cache is read by the following solve; native endpoint equality cannot prove it irrelevant. Independent review found no conclusive KSA redirect. This permits attribution, not a new numerical mechanism or a relaxed contract.

Predeclared execution, Debug, SDK 10.0.303 / runtime 10.0.12:

1. One fresh process with the unchanged selected D+12 operator and unchanged coast driver. Require the same thirteen failures and six native passes, including exact component operands. Stop if not reproduced.
2. One diagnostic process with source-generated observation hooks and configurable diagnostic D/sweep count only. Require its D+12 output to match step 1 exactly before interpreting any trace. Snapshot S0 through S6 and every sweep; retain compact numerical data, no runtime tracing.
3. Independent identical-input repeats for baseline and powered; stop on mismatch. One zero-contribution powered-kind control using the same tiny source and exact duration, followed by its coast. No additional native world is needed for this operator/lifecycle control.
4. Frozen coast equations: D+12,16,24,32 and no-D+12. No candidate policy change. Compare reference-sized ULP, absolute residual, native export and all thirteen fields. No further sweep counts.
5. One identical later constrained-piece pair, diagnostic only, using D+12 saved outputs and exact same later geometry/body/load/duration. Promote only the diagnostic owner tuple using the ordinary FP32 endpoint projection; do not install in a live BEPU or canonical world. Record admission, cache, active-set clamps, endpoint bits and differential. This is one frozen-equation sensitivity witness, not full physical continuation qualification.
6. Independently audit supplied high-precision reference rounding. If needed, solve the candidate's fixed coast linear equations directly with exact represented inputs to distinguish arithmetic floor from finite-iteration error. No equation change.

The optional 2x-tiny control is omitted unless necessary after these results. All original source, prior evidence and frozen bars remain unchanged. No remaining qualification, allocation, storage or performance campaigns. Any technical harness correction is reported; never retry a numerical failure to seek PASS.

New disposable output is confined to this directory's `bin` and `obj`. Retain the exact reviewed cleanup and non-destructive verification commands at handoff. No old blocked cleanup is retried.
