> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Independent red-team result: PASS

Read-only workers inspected current KSA, NovaCore ownership and the saved numerical/reference results. Only the lead wrote the bounded evidence and disposable diagnostic source. No worker changed files or ran candidate numerical work.

## Gate and source integrity

- Actual installed KSA assemblies/manifest and decisive current DLL method bodies were inspected, and readable full decompilation provenance was rechecked. Actual Discord live-changelog messages were read by the lead. Neither NovaCore summaries nor installed release-note JSON substituted for them.
- Current implementation and selected history agree on lifecycle ownership. Backend friction-history TODOs were not promoted to numerical theorems.
- KSA responsibility equivalence is PARTIAL: ordinary contact/cache lifecycle is relevant; float timestep and outer resource consumption do not prove exact subnormal event support. Current KSA contact aggregates also feed rolling resistance, defeating any claim that all private impulse information is irrelevant.
- Exact event authority does not by itself require one-ULP cached response. Debt/revision/history uniqueness is a publication requirement, not a blanket justification for private arithmetic.

## Numerical integrity

- Unchanged selected source and driver reproduced both complete coast outputs and the thirteen comparison operands exactly. Two Debug builds succeeded, zero warnings/errors. The expected reproduction exit1 is retained as failure evidence.
- Diagnostic D12 target, increments and proof must exactly match the uninstrumented reproduction before continuing. That check passed. Snapshot callbacks only observe copies; no callback value enters solver arithmetic.
- Baseline and powered identical-input repetitions agree in every stage. The null-force powered-kind prepare/install/coast control also agrees. No random/runtime cause was substituted for a deterministic numerical explanation.
- First initializer divergence is S3; first post-D cancellation residual is S5. Same source/free/J/K/h/load equations, zero clamps and the exact D-arithmetic reconstruction locate the finite arithmetic mechanism. No-D materially worsens it; D is not exonerated from roundoff, nor falsely blamed as generating the original physical load.
- D16/D24 do not monotonically eliminate paired errors; D32 coalesces all thirteen. Absolute residual is still nonzero, so32 is neither absolute convergence nor a new selected policy. Since the represented state and fixed sweep map are identical, equality persists in further iterations of that same map. A nonzero unavoidable paired floor is disproven for this pair.
- Independent integer ties-to-even rounding checks all thirteen installed-source and unrounded-source reference projections. Zero difference of rounded absolute references is not zero unrounded physical response. No candidate error was used to alter oracle output.
- One identical later frozen-geometry constrained piece preserves the saved differing cache through owner promotion and then yields identical outputs. This is a valid local sensitivity witness, not a refreshed-manifold/full native trajectory. No later physical piece was qualified or published.

## Contract red team

- Seven caches are prospective retained fields and drive next preparation, transport and admission. They were not mislabeled report-only; the original failed coast proposals remained uninstalled.
- Six increments construct the endpoint and participate in an install seal but are absent from accepted numerical state. Stale proposal storage is not next-solve numerical ownership. They were not mislabeled canonical state.
- Absolute PASS and native FP32 equality were not used to dismiss the frozen thirteen FAILs. Neither convergence at32 nor disappearance in one subsequent piece implies global harmlessness.
- No tolerance, subtraction, zero snapping, epsilon, changed D, new iteration policy or new precision representation was introduced. The proposed split protects physical/replay/continuation outcomes; any future quantitative cache bound must be derived from the equation/transport response and independent physical budgets, not observed D12 errors.
- The one-ULP diagnostic remains unchanged and failed. Contract class D is a Project Control recommendation, not automatic test migration or candidate acceptance.

Strongest objection: identical native endpoints and one frozen-geometry continuation cannot prove cache discrepancies harmless across all contact/duration transitions. **Survived by narrowing the conclusion**: one causal pair and one downstream witness, no global irrelevance or production qualification. Recommendation is an ownership/contract review before further mechanisms, not retirement of the retained cache.

No additional direct solution,2x-tiny input, solver count, trajectory, allocation, storage or performance campaign was run after the bounded cause and minimum achievable paired difference were established.
