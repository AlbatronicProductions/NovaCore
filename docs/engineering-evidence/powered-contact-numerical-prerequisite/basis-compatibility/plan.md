> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Bounded basis compatibility probe — predeclared plan

Baseline: `49057fecceb0f725d5f551ec40e2780971b0d81d`, branch `codex/powered-contact-numerical-prerequisite`. Preserve all 2,076 starting files and 65 tag refs. No production or existing diagnostic/test changes.

Use the retained coast capture; do not recreate the world or run another piece sequence. Build one diagnostic executable against unchanged PieceKernel/CommonNormal/DurationInitialization and the pinned binary. One execution computes the following finite witnesses; no retries to improve numerical outcomes.

Transport preserves the full six-component impulse about the body COM using the actual FP32-generated basis widened to FP64. Four normal rows, shared friction-center tangents, scalar normal-axis twist form a 6x7 Jacobian. Choose the unique solution preserving the projection of the old four normal values on the NEW normal-row nullspace. This is the minimum-change normal-distribution choice, not a change to constraint equations. It may refuse even if a different admissible distribution exists; no active-set search is authorized.

Precisely, preserve the COM-referenced generalized kick in world-oriented coordinates, applying the same current inverse mass/inertia on both sides. This does not preserve torque about one fixed external spatial origin as COM translates; that different contract would add (oldCOM-newCOM) cross P once. Old producing levers come from the captured powered-source geometry, not the refreshed coast. A future owner needs to retain that producing geometry; AcceptedCache alone currently lacks it.

Compatibility requires same ordered unique features/body/world/manifold generations, exact accepted cache/endpoint/duration lineage, finite positive-depth four-contact common-normal patch, valid BEPU handedness (n dot (t1 cross t2) < 0), finite approximately orthonormal FP32 construction (8*2^-23 construction bound), and same normal hemisphere (strict positive dot, no angle epsilon). Both old/current levers and patch centers are explicit inputs. Nonsingular, sufficiently conditioned transport and strict positive normals/current tangent+twist cap feasibility are required. No hidden projection/clamp. Normal hemisphere alone is insufficient.

Numerical transport bar before duration/load: absolute Euclidean linear impulse error <=1e-12 N s and angular impulse error <=1e-12 N m s, eight orders below the existing1e-4 physical qualification gates. Also require a conservative scaled dense-solve screening bound gamma(512)*condition_infinity*max(1,solution_infinity)*max(1,patch_radius) <=1e-12. This screening is not a formal interval certificate; direct residual reconstruction is independently checked. No normalization of stored rows. Reference residual <=1e-12; old eight-sweep gates remain summed-normal impulse, linear velocity and angular velocity errors <=1e-4 in their existing units. Report component errors too.

Original coast: A unchanged exact-basis refusal; B unchanged scalars; C transported; D cold; E independent-reference initialization. B/C get the identical accepted duration transform then current-piece load correction; D is genuinely zero; E is reference. All solve the same current equations with8 sweeps. No install, no authority capability, no subsequent coast piece.

Synthetic finite set, same nonzero cache [0.25,0.25,0.25,0.25,0.01,-0.005,0.02]: identical; tiny normal rotation (1e-8 rad); moderate normal rotation (0.08 rad); tangent-only rotation(0.7 rad); combined rotations; tangent reflection(refuse); normal flip(refuse); changed identity(refuse). Add one real Helpers sign-seam crossing, one explicitly excessive normal rotation(0.3 rad, expect friction refusal), one lever-only shift(0.0001 m), one excessive lateral lever shift(3m, expect positive-normal refusal), and one isolated nonzero twist witness. Admitted synthetics report8-sweep errors where the unchanged interior reference is applicable; failures remain evidence, not grounds to tune. Synthetic geometry is a controlled algebraic witness, not a claim of live physical contact for arbitrary orientations.

Duration transport commutation tested with the original exact producing/current durations; linear map proof applies in exact arithmetic on the fixed admitted domain. Load correction always uses current rows/mass/duration and runs last. Probe original remaining support predicates without modifying CacheHistory; stop before any additional piece on a new refusal.

Independent verifier must audit raw results, source and derivation before PASS. Stop after correction class; no broad qualification, allocation, performance, production transport, or banking.
