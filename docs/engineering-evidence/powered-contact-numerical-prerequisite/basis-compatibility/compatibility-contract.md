> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Proposed bounded numerical domain — not implemented in the owner

Correction class: **A. BASIS TRANSPORT VALID**, restricted to this admitted full-wrench domain. This is not qualification of coast continuation or generic BEPU manifold transport.

1. Same body/world/manifold generations, same constraint type, same ordered unique feature pairing. No contact creation/loss/reordering or automatic rebind. Original capture verifies accepted piece1 -> proposed piece2, producing Powered -> Coast, installed endpoint velocity, native projection, and exact producing/current duration.
2. Owned historical producing geometry AND cache lineage. Retain four old lever arms, the normal/actual basis or exact reproducible basis definition, and friction-center membership. AcceptedCache does not currently hold all of this. Historical evidence supplies it only for the diagnostic. Same feature IDs alone do not certify arbitrary manifold compatibility.
3. Current four common-normal rows remain in the existing strict positive-depth supported scope (0<depth<=.020m); same friction material/representation. No depth-active-set transition, contact replacement, contact-type change, or second dynamic body is admitted here.
4. Finite valid frames with BEPU handedness. Squared-length/orthogonality construction checks use the retained8*2^-23 FP32 bound. Normal dot product must be positive: a hemisphere-orientation refusal, not a small-angle acceptance test. No rotation-angle epsilon.
5. New projected footprint has rank2; augmented map is invertible. Use the declared minimum-change normal-nullspace rule. Keep original/refreshed levers explicit; unchanged positions are not assumed.
6. Reconstructed absolute P and L errors must each be <=1e-12 in N·s and N·m·s. The gamma512/condition screen in plan.md must also pass unchanged. These numerical tests are distinct from eight-sweep convergence gates.
7. Historical cache and transported output must be strictly positive in all normal components and strictly inside actual current tangent/twist caps. No silent clamping. Refusal of this selected distribution does not prove no other distribution exists; searching the nullspace for one is outside this probe.
8. Preserve the existing current support, finiteness, inward previous/current load, point-speed, duration, mass/inertia and current-piece load-predictor checks. Algebraic transport does not grant a prepared CacheHistory capability.

The original snapshot meets this domain. Its lever Y changes by−0.00005543231964111328m and its tangent axes remain unchanged. Six of12 planned synthetic cases are admitted; six refuse. Ten expected statuses match. The sign-seam case expected Ready but fails the declared conditioning screen (1.0723362841109055e-12>1e-12); the3m lever case refuses on conditioning before the anticipated nonpositive-normal condition. These expectations are preserved in raw results; no bar/order was revised after observing them.

For pure old normal impulse in an ideal rotated frame, required tangent/normal ratio is tan(theta). The actual four-contact friction cap is mu/4=.125, so feasible rotations are constrained by friction itself. This provides a physical refusal boundary; no arbitrary angular epsilon is needed. The.3-rad synthetic is refused as TangentCap.

The retained solver's default same-type component reuse is a warm-start heuristic, not evidence of generalized-impulse preservation. This proposal is a stricter prerequisite contract, not a declaration of an upstream bug. Permanent adoption requires separate Project Control authorization, old-geometry ownership, and unresolved coast accuracy work.
