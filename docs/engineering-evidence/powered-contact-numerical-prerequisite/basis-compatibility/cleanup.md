> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Bounded scratch disposition

New disposable path: `E:\NovaCore\build\powered-contact-basis-compatibility`.

Reviewed contents: generated project, bin/obj products, copied dependency DLLs/licenses and duplicate result JSON. 34 files, 1,947,338 bytes. The two result files were copied into this evidence directory and SHA-256 checked byte-for-byte before cleanup. Probe.cs and reproduce.ps1 retain the build/run recipe; no unique source is in scratch.

One guarded deletion attempt was rejected before execution by automatic approval review: `blocked by policy`. No retry/workaround. Current check confirms the new directory remains; files disposed by this ticket:0.

Manual Project Control cleanup, after reviewing this report:

```powershell
Remove-Item -LiteralPath 'E:\NovaCore\build\powered-contact-basis-compatibility' -Recurse -Force
```

Do not broaden this command. Repository source/dependency bytes and retained evidence are outside its target.

The prior `E:\NovaCore\build\powered-contact-coast-admission` path is now absent on the final read-only check. This ticket did not delete it or retry its previously blocked cleanup; the cause/actor of its removal is not established. Other historical scratch was not inventoried or cleaned in this ticket.
