> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Independent causal review

**PASS — CAUSE PROVEN / CORRECTION RECOMMENDED**, limited to this local transition. Original numerical candidate qualification remains failed/unqualified.

Three read-only agents reviewed distinct source, numerical and adversarial questions; the lead alone wrote diagnostics and ran experiments.

- **Reproduction:** unchanged source rebuilt; full failed output is byte-identical to retained provenance hash. The old raw build duplicate is disposable; retained original validation.json and its SHA preserve the evidence even if that scratch path is absent.
- **Reference:** analytical normal/moment modes are independent of the seven-row assembler. Captured depth asymmetry retained; tiny normal tilt has a predeclared allowance. Reference/momentum witness passes. The momentum identity applies to the frozen-midpoint solve, not an entire changing-mass trajectory.
- **Iteration interpretation:** all integer sweeps through 32 were examined. No joint crossing exists. Passing one error at sweep 3 is not acceptance. No extrapolated count is proposed.
- **Warm/cold:** warm helps. Blind clearing is unsupported. Ideal initialization is diagnostic only and never used by the derived predictor.
- **Load/mass:** source mass + load-only is compared on the same geometry/state/cache; signed factorial errors show load dominance. Norm ratios are descriptive only.
- **Load regime:** 0..32 N cases are unclamped; 64 N has two tangent projections. No all-load affine claim.
- **Coupling:** pure common-error propagation through the actual ordered G generates the checker and rolling/friction components. The checker itself is force/torque-null. Observable angular error is associated with z-offset normal imbalance and Z friction. A locally solved friction block does not make the whole system converged.
- **Counterfactual:** only an additive analytical common-load change is applied. Existing differences and tangent/twist caches remain. No converged endpoint, arbitrary scale, iteration increase, geometry change, load ramp or new force is used. Residual returns to the independently retained mass-only level rather than vanishing artificially.
- **Scope:** derived initializer is supported for the tested centered, nearly flat four-contact 0→32 N case only. No generic contact policy or production fix is qualified.
- **Cost:** strongly typed invocation avoids per-call reflection. Initializer preparation is timed. Results include diagnostic allocations and are eight-call averages. No individual maximum, tail attribution, zero-allocation or whole-frame headroom claim.
- **Preservation:** original candidate and evidence fingerprints are checked separately. No banking, milestone, canonical mutation or new powered-contact gameplay.

The reviewers found no force-application, normal equation, friction cap, update-order or reference-branch defect explaining away the failure. The evidence supports a coupled initialization/convergence cause and the bounded class-C recommendation. It does not imply a BEPU defect, require architecture reopening or permit qualification to resume automatically.
