> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Disposable instrumentation

Retain all files in this concise evidence directory. The observer/source generator reconstruct the diagnostic build, and `rejected-input.json` preserves the exact six relevant capture/admission records. Prior source and evidence remain unchanged.

Disposable path: `E:\NovaCore\build\powered-contact-coast-admission`.

Remaining: **37 files, 2,264,369 bytes**. This comprises generated copies, compiled output, build intermediates and duplicated raw reports. The raw run's hash and equivalence to prior accepted evidence are preserved in identity.json; the admission report is retained byte-for-byte.

A single guarded automatic deletion attempt was rejected before execution by automatic approval review as `blocked by policy`. No deletion or retry occurred. No alternative deletion mechanism was used.

Reviewed manual cleanup:

```powershell
Remove-Item -LiteralPath 'E:\NovaCore\build\powered-contact-coast-admission' -Recurse -Force
```

The previous ticket's `build/powered-contact-duration-provenance` was absent when rechecked. This ticket did not remove it and does not attribute that filesystem change. No retry of its earlier blocked cleanup occurred.
