> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Bounded coast-admission attribution

Declared before diagnostic execution. Preserve all 2,064 existing nonignored repository files and all historical refs. Baseline `49057fecceb0f725d5f551ec40e2780971b0d81d`; branch `codex/powered-contact-numerical-prerequisite`.

Create one disposable standalone Debug build from the fingerprinted OwnedCacheProbe and unchanged linked sources. Add only read-only capture, boolean-preserving ordered predicate recording, admission-only comparisons and a hard stop before coast reference/solve. Do not modify the retained owner, numerical equations, physical inputs, solver settings or admission thresholds.

Run one fresh process: reproduce the original preparation/powered piece and existing 20 frozen cases; capture source; install the exact accepted powered result; refresh the original coast contacts; capture successor BEFORE admission/invalidation. Evaluate the unchanged actual coast admission with ordered short-circuit recording. In the same paused state, compare another powered admission with identical current mass/duration and unchanged geometry, and a diagnostic support evaluation omitting only cache-related predicates. No solve is permitted for any of these admissions. Stop execution once attribution and these predeclared read-only comparisons are saved.

Record positions/orientations/velocities, actual and numerical mass/inertia, ordered solver contacts, normals/offsets/depths, feature and cache lineage, previous/current durations, loads, exact first false predicate and nonmutation witnesses. Independently reconstruct all eight box corners and contact-plane geometry, corner normal velocities, support footprint and load direction using copied endpoint values. Physical overlap/support and numerical-domain admission are distinct questions.

No Release matrix, later numerical gates, allocation or timing. No correction, tolerance, basis reset or fallback. The prior policy-blocked output is untouched. Retain concise capture, reproduction source/script, source identities and independent review; no bulk dump.
