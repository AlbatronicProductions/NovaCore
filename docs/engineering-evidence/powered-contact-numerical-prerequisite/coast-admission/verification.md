> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Independent review

Verdict: **PASS — CAUSE PROVEN / CORRECTION RECOMMENDED**. Refusal classification: **B. CORRECT NUMERICAL-DOMAIN REFUSAL**. The overall powered-contact operator remains UNQUALIFIED.

Read-only reviewers: `/root/powered_final_verifier` (predicate/instrumentation/result integrity) and `/root/powered_contact_bepu` (source/geometry ownership). Only the lead created the disposable observer. Reviewers ran no solver or world processes and changed no files.

## Pre-execution review

Boolean wrappers preserve each original rejecting expression and short-circuit order. The separate history-free classifier returns no prepared capability. The normal actual coast refusal throws before reference/solve. A second unconditional diagnostic guard also prevents a coast solve if admission unexpectedly succeeds. Capture hooks precede invalidation. Native readback remains before metadata promotion.

## Result attacks

| Attack | Finding |
|---|---|
| Wrong UnsupportedSupport branch/composite ambiguity | One executable status return; actual P15 row0 is `row.Normal != normal`; P1-P14 pass |
| Capture after invalidation | Actual coast and powered-comparison inputs have invalidated=false and accepted tuple unchanged |
| Physical departure mistaken for admission issue | Independent four-corner overlap and2 m² support hull; positive separating speed explicitly reported; future dynamics untested |
| Stale selected geometry or flipped normal | Callback/prestep normal, offset, depth all match; order unchanged; upward normals differ by3.264627262966613e-9 rad |
| Cache identity mistaken for geometry failure | Earlier cache/lineage checks pass; actual retained/native projections match; historical exact basis restriction is distinct |
| Coast zero thrust or duration causes direct refusal | Supported does not read h/kind; same-endpoint powered comparison fails at same P15 before current-drive check |
| Old duration inherited by coast | Captured predecessor is accepted powered1/128 s; successor17707/2000000 s, exact positive; old stock duration not reused |
| Normal tolerance quietly widened | Original files/hashes unchanged; real predicate unchanged even in diagnostic clone |
| Refusal is correct but inconvenient | Accepted prior contract explicitly restricts the exact basis; classificationB, not a demonstrated admission bug |
| Normalized/idealized input substituted | Actual stored float pose/cache captured; no renormalization in independent geometry calculation |
| Frame or generation mismatch | Same fixture identity and actual handles; standalone fixture has no canonical/frame epoch capability |
| Observer changes numerical powered result | Original powered result and20-case summaries match prior records; normalized raw report hash matches exactly |
| Coast dynamics declared qualified | No coast reference, solve or installation; no later gate run |

## Documentary caveat

Raw observer `Duration.exactPositive` is not meaningful for the `ActualSolverBinary` preparation seed: that path stores actual solver bits and has a default Exact field. The observer computed `!Exact.IsZero` without an origin/validity qualifier. Interpret that seed exclusively through origin/solverBits/numerical scale. Exact powered and coast durations are valid and unaffected. The raw capture is preserved unchanged, the observer is not rerun or corrected, and no exact-event claim is based on the seed label.

The separate history-free classifier still checks finiteness of derived previous-load values. Its intended exclusions are historical normal equality, previous-load drive inequality and impulse feasibility, not every read of a previous-load field. The installed pre-refresh world record has old prestep rows, expressly not a fresh geometry witness.

## Recommendation boundary

Qualify bounded normal-basis/cache compatibility at constrained-piece boundaries for this retained planar-support regime. Preserve feature ownership, correct normal/tangent/twist interpretation and current-piece Jacobians; do not simply remove equality or borrow an unrelated unit-length tolerance. This is the next missing numerical responsibility. Admission correction alone does not prove eight-sweep coast accuracy or later full qualification.
