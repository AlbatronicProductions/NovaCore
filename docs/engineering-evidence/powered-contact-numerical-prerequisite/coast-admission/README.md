> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Coast UnsupportedSupport: exact retained-normal domain refusal

**PASS — CAUSE PROVEN / CORRECTION RECOMMENDED**

**Classification B: CORRECT NUMERICAL-DOMAIN REFUSAL.**

The first actual false admission condition is `row.Normal == accepted.Normal` for row 0, feature -4. Retained normal `(0,1,7.002423640933841e-11)` differs from the refreshed solver normal `(0,1,-3.1946030265572745e-9)`. The changed Z component is sufficient to refuse; there is no tolerance in this equality. Fourteen earlier Supported predicates pass. Later support predicates, duration scaling, CommonNormal, coast reference, coast solve and coast installation do not execute.

The changed normal is fresh: independently captured callback and extracted solver rows agree. The retained expected normal remains the original seed under the explicitly declared existing same-basis contract. The accepted powered pose changed, collision refresh generated the new basis, and the existing numerical admission refuses it. There is no demonstrated stale geometry, wrong duration, cache corruption, physical departure or compound-selector defect.

## Evidence and controls

- [Exact predicate decision tree](predicate-trace.md): source clauses, evaluation order, values/units and first failure. Later captured facts are distinguished from checks actually executed.
- [Rejected input](rejected-input.json): source, installed powered endpoint, fresh coast contacts, owner/cache/duration data and two admission-only controls, captured before invalidation. Raw capture preserved unchanged.
- [Source vs successor](source-vs-successor.md): physical/cache changes and unchanged lineage/order.
- [Independent support witness](support-witness.md): stored-pose corner/support-function agreement, support hull and separating speeds.
- [Selector/basis ownership](selector-comparison.md): this is the ordinary convex box/slab path, not the M14.21 compound selector; pinned source anchors.
- [Independent verifier](verification.md): pre-run instrumentation approval and post-run cause/classification review, including documentary limits.
- [Identity](identity.json), [predeclared plan](plan.md), [observer source](Probe.cs), [reproduction — retired record](../../powered-contact-ordinary-step-event-closure/CONSOLIDATION-MANIFEST.md).

At the identical successor endpoint/current dry mass/coast duration, a powered 32 N admission also rejects at P15. Thus zeroing engine force or requesting Coast does not explain this particular refusal. A separate history-free physical-support classifier returns true; it removes basis/history/cache applicability checks only for observation and cannot return a prepared solver capability. Neither comparison executes a solve.

## Physical interpretation

All four bottom corners remain below the slab plane by .0003590856666937925.. .00035909069820483364 m. Independent support-function and deepest-corner calculations agree exactly. Selected support XZ area is 2 m² with the COM strictly inside, .5 m from the nearest edge. All eight corners remain within the slab footprint. The normals still point upward and coast gravity drives inward at -9.81 m/s².

Separating point speeds of roughly+.007093 m/s are real and below the existing .029999400011999758 m/s bound. The snapshot demonstrates continuing overlap, not the absence of separating velocity or future contact dynamics. It does not prove coast accuracy.

The old/new normal angle is3.264627262966613e-9 rad. Its small size does not prove that retaining all scalar cache components under a newly generated tangent/twist basis is safe. The prior accepted prerequisite contract explicitly restricts normal-basis changes. Therefore this is classificationB, not a proven harmless or overly narrow assertion repair.

## Correction class and payoff

Recommend **bounded basis/cache compatibility qualification at constrained-piece boundaries**, for this same retained planar-support regime. Establish how current normal/tangent/twist bases may remain compatible with accepted caches, or how a qualified basis transition prepares them, while preserving feature ownership and current-piece equations.

Do not remove the equality check, reset the cache, or invent an angular tolerance in this ticket. The existing unit-length tolerance is not an angular-equivalence rule. No correction was implemented.

This identifies the next specific numerical responsibility. Resolving it removes this admission blocker but does **not** unlock an already-qualified coast result: coast has never reached reference/solve in these retained runs. Its eight-sweep error bars and all later prerequisite gates still require qualification after Project Control authorizes the appropriate work.

## Execution and preservation

One fresh Debug process on SDK 10.0.303 / runtime .NET 10.0.12 reproduced the failure. Two preparatory Debug builds passed with zero warnings/errors. A diagnostic source-generator anchor initially matched two source locations; it was made unique before any build/run. There was no numerical retry. Independent reviewers performed read-only source and saved-data analysis.

Original powered results and all 20 composition cases match prior accepted evidence. Replacing only top-level checks 140→139 in the current raw run report reproduces the prior raw SHA-256 exactly. The extra check asserts the diagnostic powered comparison did not mutate accepted state. One powered installation remains accepted; after coast refusal continuation is invalidated with the same accepted cache/endpoint/1/128 s duration. Canonical mutations: 0.

All 2,064 existing nonignored files remain byte-identical. HEAD/main/origin/main/remote main remain `49057fecceb0f725d5f551ec40e2780971b0d81d`, branch `codex/powered-contact-numerical-prerequisite`. All 65 historical tags, including protected M14.21-M14.24 and M15.0, remain unchanged. Production, permanent tests, earlier prerequisite evidence and Blender are untouched; only this evidence directory and disposable build instrumentation are new. Nothing staged or committed.

The observer's preparation-seed `exactPositive` label is inapplicable to ActualSolverBinary; that seed is qualified by actual solver bits. Exact powered/coast duration captures are valid. See verifier caveats. No raw value was rewritten to conceal this label defect.

## Reproduction boundary

Run `reproduce.ps1` from a fresh reviewed output location (it uses `build/powered-contact-coast-admission`). It verifies retained source identities, creates disposable instrumented copies, builds Debug, and runs one process. It stops on the expected UnsupportedSupport exit. The hard guard prohibits coast reference/solve even if admission unexpectedly succeeds. Existing compiled/run output is refused; the script never deletes it or retries it.

Only read-only document/data validation and preservation checks follow the captured cause. No coast solve, tiny event, off-COM, long support, M15 comparison, allocation, timing, live powered contact, departure or Florida work ran.

## Final Git and cleanup

`git diff --check` and `git diff --cached --check` pass; staged paths and tracked working changes are both zero. New evidence JSON, PowerShell syntax, local links and untracked whitespace were checked separately. Final `git status --short` retains the existing unbanked investigation trees:

```text
?? docs/engineering-evidence/powered-contact-architecture/
?? docs/engineering-evidence/powered-contact-numerical-prerequisite/
?? tests/NovaCore.ContactNumerics.ConvergenceProbe/
?? tests/NovaCore.ContactNumerics.CoupledProbe/
?? tests/NovaCore.ContactNumerics.DurationProbe/
?? tests/NovaCore.ContactNumerics.OwnedCacheProbe/
?? tests/NovaCore.ContactNumerics.PreconditionedProbe/
?? tests/NovaCore.ContactNumerics.Probe/
?? tests/NovaCore.ContactNumerics.WorldProbe/
```

The only new nonignored paths in this ticket are under `docs/engineering-evidence/powered-contact-numerical-prerequisite/coast-admission/`. Generated instrumentation is confined to its reviewed build directory. [Cleanup disposition](cleanup.md): 37 files / 2,264,369 bytes remain after one automatic deletion was rejected before execution as `blocked by policy`; no retry. The prior ticket's output directory is currently absent, with no deletion performed by this ticket.

UNQUALIFIED operator. UNBANKED. No M15.1. STOP FOR PROJECT CONTROL.
