> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Primitive evidence limits

This clarification preserves the original preregistrations and measured outputs.

The two-row `coupled-contract.md` declaration included force, inverse properties and prediction sharing midpoint mass. The executed CoupledProbe varies mass only in K and velocity reconstruction; its free inputs are prescribed constants. It does **not** establish force or selector mass cadence. Its micro-to-coast composition carries the normalized response velocity/impulses but restarts the ordinary baseline velocity/guess. It establishes linear response composition only, not complete physical/cache continuity. The affine oracle checks the same finite iteration, not convergence to the converged constraint solution. After eight sweeps, warm/cold ordinary impulses still differ; full stability remains a separate gate.

The scalar closed-form reference contains one FP32 intermediate (`2 * spring.AngularFrequency / 60`), unlike the adapter's double evaluation. Its reported relative errors therefore include reference rounding and are not pure discretization errors. The declared 2e-6 primitive bar passes, but stronger later references must evaluate explicitly in double or independently higher precision.

The exact h=2^-1075 witness proves positive authority and one-bit **free preintegration**, not a full contact response. The scalar contact witness uses h=2^-151, and the two-row minimum-scale projection is a normalized-response primitive. Neither is a retained-world tiny-event qualification.

The cache witness proves seven actual cache slots survive an unchanged geometry refresh. It does not test physical solving, feature reorder/replacement, shrinking manifolds, or a residual precision extension surviving those changes.

No full operator, supported propulsion, selector consistency, allocation, timing or long-sequence PASS follows from these primitives.

## Four-row reference independence clarification

The four-row preregistration said to independently assemble K. The implemented references share the captured `Patch.K`, `Patch.J` and velocity reconstruction with the candidate. Their linear-system elimination and affine block-iteration solution are independent of the candidate update functions; their geometry/Jacobian assembly is **not** an independent physical oracle. Two read-only reviewers checked that shared assembly directly against the source equations for the admitted spherical-inertia fixture and found it correct. The failure is therefore a local iteration-accuracy witness, not full independent trajectory qualification. This discrepancy in the declared implementation detail is retained explicitly; the preregistration and its pre-run hash are unchanged.
