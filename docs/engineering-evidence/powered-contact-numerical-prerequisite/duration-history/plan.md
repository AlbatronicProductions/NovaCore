> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Bounded duration-history causal study

Declared before diagnostic implementation/execution. Preserve all existing source, tests, evidence, refs and binaries. No correction adoption or resumed operator qualification.

## Source-derived candidate and limits

Pinned BEPU stores accumulated normal/tangent impulses in N s and twist in N m s; warm application is W J^T lambda. It offers explicit impulse scaling but the inspected default path performs no automatic old/new-dt normalization. Raw reuse is an admissible impulse guess, not dimensionally invalid. This study tests its finite-eight-sweep quality for retained steady support.

For constant physical reaction f, lambda(h)=h*f. Under unchanged geometry/mass, inactive caps and zero contact velocity, this also solves the soft normal law: K f=-J a, d_i=Kii*f_i/omega^2, alpha=1/[omega*h*(omega*h+2)], bias=d_i*omega/(omega*h+2). Therefore lambda(h1)=(h1/h0)*lambda(h0) satisfies the changed equations. Tangent/twist impulses and Coulomb bounds scale with h as well. A compressed, unloading contact with no supporting force is NOT steady: the one-row solution omega^2*h*d/[K*(1+omega*h)^2] is a counterexample to universal ratio exactness.

Candidate A: multiply all seven cached impulses by rho=h1/h0, then add the accepted current-piece CommonNormal load shift to normal rows. This preserves force-normalized differences/friction/torque state, not unchanged raw impulse differences. Force-normalized storage is algebraically the same candidate, not a second mechanism. Reversing the order multiplies the current-piece load shift by rho and is a negative ordering control.

Candidate B is reserved only if A fails to explain the original case: a scalar softness-aware common-demand difference, derived separately before any execution. No third candidate or tuning. If A explains the bounded evidence, do not implement/evaluate B.

## Predecessor identity and capture

The actual120-step BEPU preparation used binary32 `1f/60`: h0=(double)(1f/60), NOT mathematical1/60 or binary64 `1d/60`. Record both values and bits. Preparation uses9.81f; diagnostic local equations preserve the previous9.81 binary64 acceleration for every comparison. Do not infer causality from those small precision differences.

One fresh Debug capture process constructs the same world, box/slab, material, source mass8+1/128, eight sweeps,120 preparation steps, then same midpoint-mass admission/collision refresh at h=1/128. It copies actual geometry/velocities/seven impulses/features and reproduces the prior corrected failure against retained exact errors BEFORE the study proceeds. No endpoint/cache install, coast step or second world. If failure differs, stop.

## One fresh Debug local study process

All variants read that frozen capture. No repeated world creation or physical settling for variants. No change to actual solver or reference code: link existing PieceKernel/CommonNormal unchanged. Same eight sweeps and all three1e-4 physical bars.

1. Independently verify analytical steady support and steady nonzero tangent/twist at h0,h0/2,h0/4,h0/8,2h0 and actual1/128. Use a separate exact symmetric analytic patch, constant normal reaction W=8*9.81, tangential components W/16 and W/32, twist W*sqrt(1.25)/16, and the generalized balancing external wrench. Derived depths enforce steady compliant equilibrium. Reference impulse/velocity agreement <=1e-8 N s and m/s or rad/s (allows captured FP32 depth representation). Verify eight-sweep response under full ratio scaling, and compare normals-only scaling as a component control. Not an article/off-COM trajectory test.
2. Duration-only retained witness: source mass/load/state fixed; ratios1,1/2,1/4,2 and actual1/128. Compare raw,zero,all-component ratio,and converged-reference initialization diagnostic. Recompute current spring/effective coefficients. Doubled h0 is an algebraic kernel diagnostic only; it exceeds CommonNormal's prepared16667-tick domain, so no load correction is invoked or domain widened there.
3. Complete small2x2x2 local factor table: D=old h0 / actual1/128; M=source / midpoint; L=0 /32 N. This contains the four requested cases and provides unambiguous signed-factor interaction terms. Apply accepted load preconditioner where L=32, no duration transformation yet. Report signed seven-impulse and six-velocity errors; do not subtract norm magnitudes as causal contributions.
4. After duration-only/factor evidence, apply candidate A to the original full D/M/L failure. Keep equations/reference unchanged. Compare ratio-before-load with load-before-ratio negative control. If A fails, stop this process and return to derivation/Project Control rather than tune.
5. If A passes, duration/load/mass composition: fixed ratios1,1/2,1/4,1/8 and actual1/128 at loads0/16/32/64 N, midpoint mass; one result per distinct tuple. Reuse existing controls where identical. The2h0 duration-only/analytic rows cover the doubled-ratio transform; powered2h0 is outside current CommonNormal domain and explicitly excluded, not silently accepted. Report clamp changes and every error. Stop on a candidate engineering failure; no indefinite extra ratios or loads.

Independent reference: retained pivoted seven-row solve, strictly positive normals/interior friction and residual<=1e-12. Its algorithm is independent of PGS but it shares J/K construction. Analytical steady and transient counterexamples independently check physical interpretation. Ideal initialization is diagnostic only and is never read by the ratio transformer or CommonNormal.

Ordering proof is algebraic: desired rho*lambda_old + beta(h1)*u; reverse order gives rho*lambda_old + rho*beta(h1)*u. Source/history metadata are observation-only. No new duration provenance field is installed in the candidate/world; that is a future implementation obligation. A cold-cache baseline is a diagnostic, not a proposed reset policy.

Cost is a static operation estimate: one prepared ratio plus seven scalar products and unchanged common correction; no benchmark/allocation campaign. No full interior/coast, tiny retained world, selector, off-COM qualification, long support, M15 comparison, manual acceptance or production edits. Stop after causal judgment and bounded recommendation.
