> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Interior-piece warm-start duration-history study

**PASS — CAUSE PROVEN / CORRECTION RECOMMENDED.** The constrained numerical operator remains **UNQUALIFIED / UNBANKED**. No correction has been adopted and full qualification has not resumed.

## Baseline and execution

HEAD/main/origin/main/remote main: `49057fecceb0f725d5f551ec40e2780971b0d81d`; branch `codex/powered-contact-numerical-prerequisite`. All 2,045 pre-existing files and 65 tag refs are preserved. Protected M15.0 and M14.21–M14.24 identities, pinned BEPU binary/manifest hashes and diagnostic source hashes are in [identity.json](identity.json).

The [plan](plan.md) was declared before execution. One fresh Debug process recaptured the genuine retained world and reproduced the original failure. One fresh Debug local-study process produced 59 bounded records. No numerical retry, second transformation, Release matrix, profiler or full-qualification execution occurred. Diagnostic build: PASS, zero warnings/errors; SDK 10.0.303, runtime .NET 10.0.12. A later evidence-summary PowerShell array-precedence error was corrected; only JSON/closed-form arithmetic analysis was repeated, not either numerical process.

The new standalone DurationProbe links all prior numerical/preconditioning sources unchanged. It is not registered in the permanent Simulation runner or solution. Canonical state mutations, endpoint installs, solved-cache installs and coast steps are all zero. The single capture performs the previously used preparation and midpoint collision refresh; it does not claim its temporary BEPU world was untouched.

## What happened and cause

The [pinned source/math trace](source-and-math.md) identifies normal/tangent N s and twist N m s applied directly to velocity during warm start. The inspected default path does not automatically rescale them when substep duration changes. Current-h spring/softness/effective terms are correctly recomputed in the failing kernel.

The previous cache was produced by 120 actual `Timestep(1f/60)` calls. Its duration is **.01666666753590107 s**, not binary64 `1d/60`. The new piece is exactly **1/128 s**; rho is **.4687499755527837**. Reading this predecessor value from the call provenance, rather than assuming 60 Hz exactness, is necessary for the witness.

Duration alone, with geometry, mass, gravity/load, source velocity and contact data held fixed, reproduces the failure. Retaining the same reaction-force estimate by scaling old impulses to the new duration removes it. Thus the proven cause is **an inadequate old-duration warm-start predictor for the changed-duration steady-support equations under the fixed eight-sweep budget**. It is not a dimensionally invalid cache, a BEPU equation defect, stale current-h spring data, endpoint transport or canonical publication defect.

## Duration-only controls

Each cell is `(normal-sum impulse N s / linear m/s / angular rad/s)`. All three bars are 1e-4. Raw and ratio use the same genuine captured cache and the same unchanged equations. The zero arm clears only the diagnostic initial guess; it is not a proposed policy. Ideal initialization reads the oracle only for a control.

| h/h0 | Raw cache | Ratio cache | Raw / zero / ratio / ideal |
|---:|---|---|---|
|1|8.718e-10 / 1.670e-10 / 2.536e-10|same|PASS / FAIL / PASS / PASS|
|1/2|.002358130 / .000704234 / .001280480|3.241e-10 / 1.939e-10 / 3.795e-10|FAIL / FAIL / PASS / PASS|
|1/4|.000106460 / .000168968 / .000337168|4.977e-11 / 6.278e-11 / 1.251e-10|FAIL / FAIL / PASS / PASS|
|2|.003754804 / .000812513 / .001328228|1.124e-9 / 2.691e-10 / 4.597e-10|FAIL / FAIL / PASS / PASS|
|actual .4687499755527837|.002228146 / .000685270 / .001253516|3.416e-10 / 1.951e-10 / 3.811e-10|FAIL / FAIL / PASS / PASS|

The doubled-duration row is a zero-load algebraic diagnostic outside the current CommonNormal maximum duration. No powered-domain widening or retained-world doubled interval was performed.

## Interior factorial decomposition

D changes h0 to 1/128; M changes 8.0078125 to 8.00390625 kg; L changes centered thrust from 0 to 32 N. The accepted common-load preconditioner is active wherever L changes. No duration correction is used in this table.

| D/M/L | Normal impulse error | Linear error | Angular error | Result |
|---|---:|---:|---:|---|
|0/0/0|8.717720e-10|1.670389e-10|2.535904e-10|PASS|
|0/1/0|3.052822e-6|7.747304e-7|1.349790e-6|PASS|
|0/0/1|8.717775e-10|1.670164e-10|2.535297e-10|PASS|
|0/1/1|3.052822e-6|7.747304e-7|1.349791e-6|PASS|
|1/0/0|.002228146247|.000685270488|.001253515882|FAIL|
|1/1/0|.002228608440|.000685524488|.001253900084|FAIL|
|1/0/1|.002197412306|.000677051122|.001238925528|FAIL|
|1/1/1|.002197733509|.000677262179|.001239233175|FAIL|

The full signed seven-impulse/six-velocity factorial terms are retained in [results.json](results.json), not inferred by subtracting error norms. Normal signed contributions are: baseline -8.718e-10; D -.002228145376; M -3.051950e-6; L -5.523e-15; DM +2.589758e-6; DL +3.073394e-5; ML +8.327e-17; DML +1.409887e-7. They reconstruct the original signed result within 8.674e-19 across all 13 components.

Duration is the dominant factor; mass has a small below-bar effect, and the accepted load correction removes the load-only error. The original combined raw-duration arm has one transient tangent clamp; this interaction slightly offsets the duration error. Nonlinear clamp changes are measured interactions, not evidence of one globally affine error propagator.

## Original interior case and ordering

The capture reproduces all seven prior corrected solved-impulse bits and both velocity-error values exactly. The reported normal error here uses abs(sum of signed row errors), differing from the prior sum subtraction by only 4.16e-17 N s; the gate and physical meaning remain unchanged.

| Initialization | Normal impulse error N s | Linear error m/s | Angular error rad/s | Result |
|---|---:|---:|---:|---|
|Accepted load correction only|.002197733508988789|.0006772621786819396|.0012392331745507306|FAIL|
|Duration ratio, then accepted load correction|7.772526795790657e-7|2.3900377891757947e-7|4.3713515883680234e-7|PASS|
|Load correction, then ratio|.0003296624590587022|.00010140477522230424|.00018548052126605023|FAIL|

Original load-only normal guess is approximately .279 N s per row; ratio-first/load-second is approximately .1051102 N s, close to the independent .1050496 N s solution. The transformed initial error remains nonzero; eight unchanged sweeps do real work. Reference residual remains **1.9945269477068763e-17**, with identical reference impulse/velocity bits across cache-only arms. Corrected clamps are 0/0/0.

## Bounded composition and component evidence

Five durations (ratios 1,1/2,1/4,1/8,actual) times four loads (0,16,32,64 N) at midpoint mass give **20/20 PASS**, including the original tuple once. Maxima across those loads:

| Ratio | Normal impulse error | Linear error | Angular error |
|---:|---:|---:|---:|
|1|3.052822e-6|7.747305e-7|1.349791e-6|
|1/2|9.475360e-7|2.829346e-7|5.143805e-7|
|1/4|1.397262e-8|2.038743e-8|4.065882e-8|
|1/8|2.750350e-9|5.181383e-10|7.762427e-10|
|actual|7.772527e-7|2.390038e-7|4.371352e-7|

Every corrected composition has zero normal/tangent/twist clamps. The six-duration analytical steady witness has nonzero tangent/twist rates, force-normalized error zero, and independently checked force/torque/depth inputs. Its full ratio arm passes all six. Normal-only scaling fails velocity/angular bars at doubled duration, but its final twist error is zero: no separate twist-convergence benefit is claimed. A scalar unloading counterexample explicitly rejects universal ratio exactness.

## Recommendation and limits

Recommend **one** all-component impulse-ratio predictor followed by the already accepted current-piece common-normal load correction. Force-normalized storage is algebraically equivalent, not another tuned mechanism. A second softness-aware candidate was unnecessary and was not evaluated. No change to eight sweeps, equations, material, geometry, error bars or final physical solution is recommended by this study.

Require explicit private cache-producing duration/generation provenance and numerically safe scaled ratios in any authorized implementation. Lifecycle and tiny-ratio behavior remain unqualified. No speculative callback or canonical authority change is needed to record accepted private numerical history. Preserve new pending cache/duration after a canonical precommit publication refusal; invalidate unsafe continuation after post-step failure. Details and source anchors are in [source-and-math.md](source-and-math.md).

Static additional arithmetic is one ratio plus seven products for this witness. No formal allocation, performance, storage, long-sequence, full powered-to-coast, tiny-world, selector, off-COM, M15 comparison or visual acceptance run occurred. The earlier preconditioner is preserved unchanged; the complete constrained operator is still unqualified.

## Evidence and reproduction

Retained: this report, preregistered plan, source/math trace, compact numerical results, independent [verification](verification.md), identity manifest, reproduction script, and two standalone diagnostic source/project files. Raw detailed duplicate JSON and build outputs are disposable after summary/identity preservation. No prior evidence is rewritten.

From PowerShell 7 on the same qualified SDK/runtime/machine, run `& 'docs/engineering-evidence/powered-contact-numerical-prerequisite/duration-history/reproduce.ps1'` from the repository. It builds only the standalone probe, runs one capture and one local study, stops on failure, and performs independent closed-form/result checks. `-SummarizeExisting` analyzes saved JSON without running either numerical process. Raw output hashes permit reproduction comparison. Source search: inspect the pinned files listed in the math trace, and search NovaCore for `ScaleActiveAccumulatedImpulses`, `ScaleAccumulatedImpulses`, `Timestep`, `WarmStart`, and previous-duration metadata.

Final hygiene: `git diff --check` passes; nothing staged and no tracked modifications. The new untracked files also pass explicit whitespace, JSON, PowerShell-parse and local-link checks. One attempted cleanup of ONLY the new `E:\NovaCore\build\powered-contact-duration-history` was rejected by automatic approval review as **blocked by policy**, before execution. No retry or workaround followed. It remains **34 files / 2,141,344 bytes** of rebuildable diagnostic outputs. No earlier blocked directory was targeted. No files were disposed by this ticket. Optional exact manual cleanup after review:

```powershell
Remove-Item -LiteralPath 'E:\NovaCore\build\powered-contact-duration-history' -Recurse -Force
```

No production/permanent-test modifications, no stage/commit/tag/push/bank, no live powered contact, no departure, no Blender work and no M15.1. **STOP FOR PROJECT CONTROL.**
