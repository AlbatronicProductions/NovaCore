> Historical fallback evidence. Original judgments and measurements remain unchanged. Old campaign commands/counts describe their original runs; current reproduction and retirement records are in [../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md](../powered-contact-ordinary-step-event-closure/REPRODUCTION-INDEX.md).

# Bounded mechanism comparison

Exactly three serious mechanisms were considered. All are private numerical consumers, not canonical owners.

| Mechanism | Correctness / tiny event | Warm start | Selector / free reference | Cost | Disposition |
|---|---|---|---|---|---|
| A: exact wrench preintegration then ordinary FP32 constraint state | Positive 2^-151 event has a nonzero binary64 free effect but projects to FP32 zero; ordinary row cannot distinguish zero control | Ordinary warmed FP32 result also loses response | Not qualified; no reason to proceed after representation loss | No timing campaign | REJECT this mechanism as sufficient adapter |
| B: similarity-scaled stock timestep | h=2^-140 is FP32-positive, but reciprocal spring softness becomes NaN; scaling preserves w*h and the singular expression | Full reuse not tested | Not qualified | No timing campaign | REJECT unmodified stock scaled mechanism; stable algebra incorporated in C |
| C: factored constrained kernel with a planned split precision representation | Scalar/two-row normalized response survives; real cache access works. Actual seven-row ordinary powered local solve misses eight-iteration reference bars | Genuine settled impulses retained unchanged; powered residual remains above bars; cold control worse | Full powered selector, exact cache-residual transfer and zero-constraint M15 outcome NOT QUALIFIED | No timing after material correctness failure | REVISE; no full operator selected |

C's actual seven-row implementation currently handles ordinary double local state. It does not yet extend the normalized tiny channel through all seven rows and changing manifold features. A successful scalar or cache primitive does not supply that missing contract. The current failure occurs before claiming those later integrations.

The powered case uses the SAME captured geometry/cache as unchanged and mass-only controls, so its error cannot be attributed to geometry reauthoring or different cold state. It does not separate the powered load change from every possible force/mass interaction; mass-only passes. No stronger attribution is needed to withhold qualification of this candidate.

No fourth mechanism, iteration increase, spring tuning, minimum dt, cache reset policy, resource callback, production modification, long trajectory campaign or performance campaign was introduced after the failure.
