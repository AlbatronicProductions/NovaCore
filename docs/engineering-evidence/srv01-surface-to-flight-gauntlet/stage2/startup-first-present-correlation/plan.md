# Targeted startup / first-present check

Predeclared 2026-09-17 before any capture: **A1 B1 B2 A2**, four fresh Release processes only. A = `srv01-powered-support`; B = `srv01-supported-contact`. No free-flight or qualification-box population. No retries, replacements or extra runs. Each automatically starts and stops after **180 native frames**, observing startup and about one second of normal continuation, not repeating the prior 20-second population/qualification. Partial episode completion is intentional. Same actual default **960 x 540**, windowed Vulkan/FIFO, default logging, fixed initial camera, no intentional input. Verify actual extent in every log. Runtime/background cache state is uncontrolled; no claim of historical-harness equivalence.

Preserve exact candidate physics and original legacy clock/timing expressions. Temporary QPC/Stopwatch stamps only; preallocated first-256-callback storage; native first-eight-frame lifecycle storage. No hot logging, sleeps, prewarming, window/show/Present change or optimization. Post-render output and same-route reference replay are outside timing. Restore all temporarily edited source bytes before launching captures. Separate scratch `E:\NovaCore\build\srv01-startup-first-present`; never retry deletion of the previous population root.

Observe T0 as earliest managed-entry timestamp (not exact OS process creation); record native renderer entry separately. T1 window created plus shown; T2 after Commands/bootstrap/backend readiness; T3/T4 first native frame begin/end; T5/T6 first Present invocation and return/result. **T5/T6 are inside T3/T4**, not a sequential numbering promise. Record acquire result and startup recreation. T6 is API return, not compositor/scanout completion. T7 first subsequent callback after a successful Present return, with prior render-submit fence completed and scene objects present. It does not prove human-visible perception.

For T8 record every service entry/return and find the first invocation that actually advanced canonical frontier. That entry bounds physical work, not a timestamp inside BEPU. T9 is the first copied observation showing published frontier, an upper bound on first commit, not an invented exact instruction timestamp. Preserve the first callback's Start timestamp and each original legacy sample/elapsed value. Record host credit, debt, serviced count, state/revision/history/resource values; compare copied endpoints to a one-interval reference afterward.

Analyze separately: (1) window-created/shown to first successful Present, (2) pre-first-callback/backend preparation, (3) legacy Start/sample interval containing the first frame suffix and outer pump, (4) normal cold first-service. A pre-callback white/unpresented interval is outside both original timers and cannot be substituted for a display-counter gap. Project Control supplied the visual white-window observation; timestamps establish lifecycle only. Preserve historical595.898/1228.1696ms attribution as unproven unless retained evidence establishes it.

No additional threshold/budget. Compare absolute timeline spans and actual overlap. First-present absence/error, malformed timestamps, missing source identity, dimension mismatch or canonical comparison failure makes a capture invalid; keep it without replacement. All four scheduled captures remain evidence. Source indicates startup is shared, but classification waits for measured data and must distinguish a shared blank-startup responsibility from the missing historical display interval.

Stage2 UNQUALIFIED; Stage3 CLOSED; no manual acceptance request, milestone, source correction, staging or banking. No new KSA gate unless evidence relocates responsibility into physics/publication ownership. Independent reviewer checks timing, classification and the ticket's24 attacks.
