# Stage 2 bounded closure — REVISE

2026-09-17. **Stage 2 remains UNQUALIFIED; Stage 3 remains CLOSED.** The four
correctness findings are closed by code and focused/permanent coverage. Integrated
qualification stopped after the second predeclared live capture repeated a large
unexplained display-callback gap. No third capture, manual acceptance, optimization
or further campaign stage was run after that stop. UNBANKED.

## Performance contract provenance

**B — HISTORICAL / PROVISIONAL / SUPERSEDED MICRO-GATE.** The primary Project
Control ticket `871aeec9-08d9-44b3-8276-11bdb173def8`, current banked
`powered-contact-production-integration/final-qualification/plan.md`, its final
report, and `PoweredContactTests.Characterization.cs` establish that
0.050/0.100/0.250/0.500 ms were provisional first-ticket micro-gates. The current
campaign did not reinstate them. The new explicit closure steer authorizes using
the accepted integrated contract. Independent source review agrees.

The candidate powered characterization therefore reports all values without that
inherited blocker. The original Stage 1 unpowered gate is unchanged. No performance
optimization, solver change or threshold relaxation was performed. The parent's
historical first failure (0.0905/0.1308/0.1839/0.2248 ms) remains untouched.

Current acceptance needs integrated headroom, bounded work and tail review; an
isolated median cannot grant it. The preferred 6.67–6.94 ms whole-engine context is
not a newly invented local budget. See [predeclared plan](plan.md).

## Current KSA and live history

The actual `E:\Kitten Space Agency` installation was freshly inspected read-only:
build `2026.9.10.5438+c9291b5dd3347e847020c8fb3dd11dfce6a728e9`. Installed KSA,
BepuPhysics and BepuUtilities identities matched the retained full hashes. The
authenticated live-changelog entries 4428, 4549, 5174 and 5434 were directly read;
the current 5441–5448 view was checked for supersession. [KSA record](ksa.md)
contains exact history URLs and installed method tokens.

ADOPT ordinary retained solving, source properties during the solve and subsequent
resource/property update. ADAPT exact two-store authority, one-use prepared input,
atomic publication and NovaCore's material-origin continuity. KSA's ready flag is
not claimed to provide NovaCore's replay/revision guarantees. No evidence says KSA
uses NovaCore's numerical remainder transport. No proprietary content was copied.

## Independent thrust and physical oracle

The independent witness reads actual native constraint rows and accumulated
normal/tangent impulses. It checks
`m_source*(delta_vY + 9.81*dt) - J_contact,Y = 600*dt`, with independently derived
`m_source = 705 - 0.1953125*sourceTicks/1e6`. It does not reuse the production
resource, acceleration or wrench helper to derive expected thrust. The row basis
uses the actual native normal without renormalization. One dynamic body and one
support constraint are checked.

The predeclared residual ceiling is 0.01 N s. Across all 1,200 intervals the maximum
was **0.000028595232464567744 N s**. A separate zero-wrench negative control on the
same native boundary produced **9.999615233368175 N s**, rejected by that oracle,
with canonical authority unchanged. Resource decrease alone is not the proof.

Debug and Release agree:

| Physical observation | Result |
|---|---:|
| Peak penetration | 0.00008743492921081675 m |
| Final-window horizontal drift | 0.00000015038193842160926 m |
| Final-window support height | 0.00007124804200930868 m |
| Final-window linear speed | 0.000005875414892227578 m/s |
| Final-window angular speed | 0.000002263770778435268 rad/s |
| Final-window orientation excursion | 0.00000008846353931362544 rad |
| Independently supported final intervals | 600/600 |
| Minimum native contact rows | 4 |
| Feature-set changes | 0 |

The existing 20 mm peak bound and precision-derived 61 micrometre settled bounds
were declared before results. Tests retain the angular surface-speed/displacement
conversion, independent transformed geometry and support persistence checks.

## Authority and successor properties

Assembly preparation is sealed to the next frontier. Missing, duplicate, consumed
or out-of-order preparation cannot enter the native step. Wrong target refusal
preserves a valid pending preparation for the correct target. Mass refresh now
requires the actual bound engine/source and owner phase, then the pending-frontier
checks. Existing expected revision/resource/command/profile/world checks remain.
The native handle is private, fixed for the retained world and not caller-selectable.

After resource consumption, the export now identifies successor mass, COM and full
tensor. Its COM kinematics are transported to the successor COM while retaining the
already-solved material-origin position/velocity and orientation. The canonical
publisher still uses the solved material-origin endpoint; no re-solve or reimport
is introduced. Tests compare every frontier's exported properties and material-O
state to canonical publication, plus the independent first-moment/tensor relation.

The stock episode ends at 20,000,000 ticks with fuel **28.4375 kg**, oxidizer
**42.65625 kg**, mass **701.09375 kg**, and COM x **1.0475061288165812 m**. It does
not exhaust. A separate isolated 630 kg dry-property refresh/export test covers the
numerical boundary and canonical nonmutation; it is **not** a claim of stock
exhaustion or dry canonical lifecycle qualification. No Stage 4 resource retuning.

## Determinism, allocation and storage

All matching frontier endpoint bits, resources and typed publication records match
at 30/60/150/240 Hz and the bounded backlog schedule. Moving-frame comparison passed
all 1,200 intervals. Original T0/frame epoch, retained body/shape/world generation,
400 intervals of 16,666 ticks, 800 of 16,667 ticks, 1,200 revisions/history records
and exact integer debt conservation are checked. No second authority appears.

Debug/Release warmed complete, no-work, backlog and publication-refusal/retry
windows report **0 B** with checked entry/exit. Deliberate control reports **152 B**.
Powered presentation submission also reports 0 B with its positive control.

Release owned contact CPU storage is **3,388,600 B**: managed construction upper
bound 2,913,464 plus native pool 475,136. History payload 1,881,600 and credit payload
76,816 are included. Debug upper bound is 3,389,176 B. Entire scene managed delta
4,431,416 plus native pool gives **4,906,552 B** CPU; mesh payload 1,359,096 gives
**6,265,648 B**, below 8 MiB. The unchanged Stage 1 mesh allocation witness and the
current prepared submission's 4,416 B give the same conservative **6,270,064 B**
assembly-owned total. Shared renderer/device/swapchain are separate: the actual
1280x720 plume presentation targets report **9,896,672 B**. They are not solver
storage and are not hidden in the CPU budget. No new target or mesh was introduced.

## Performance and the stop

Each fresh Release isolated process used 128 warm and 1,024 complete timed operations.
All collection deltas were [0,0,0]. These are characterization, not an integrated PASS.

| Process | Median ms | P95 ms | P99 ms | Max ms | Cold preparation ms | First operation ms |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 0.0900 | 0.0923 | 0.0937 | 0.1169 | 161.6714 | 55.3482 |
| 2 | 0.0904 | 0.0935 | 0.1019 | 0.1392 | 161.6721 | 55.9424 |
| 3 | 0.0913 | 0.0935 | 0.0947 | 0.1013 | 156.5045 | 55.2810 |

Worst sample indices are retained in `measurements.json`. There is no repeating
isolated warmed index; that does not explain live tails. Fixed 8 iterations/1
substep, native body ownership and exact interval arithmetic remain unchanged.
Service is limited to four intervals per call; rendering remains once per display.
No new locks or worker synchronization were introduced.

Two of the three predeclared 1280x720 live captures ran. Each rendered 4,000 frames
and completed the stock 1,200 publications without a physics/authority failure.

| Capture / timer | Median ms | P95 ms | P99 ms | Max ms |
|---|---:|---:|---:|---:|
| 1 callback interval | 5.5557 | 5.7121 | 5.9289 | **595.8980** |
| 1 live service | 0.0038 | 0.0796 | 0.1513 | 57.7075 |
| 1 native frame | 5.517 | 5.821 | 5.959 | 59.259 |
| 2 callback interval | 5.5569 | 5.7154 | 5.9315 | **1228.1696** |
| 2 live service | 0.0038 | 0.0812 | 0.1890 | 62.5022 |
| 2 native frame | 5.509 | 5.799 | 5.931 | 64.332 |

Scene cold preparation was 206.3044 / 184.6588 ms. Native average GPU work was
0.096 / 0.094 ms; average callback work 0.136 / 0.140 ms; average fence wait
4.959 / 4.967 ms. All quantities are reported at their actual boundaries.

**Cause of the 595.898/1228.1696 ms gaps: UNATTRIBUTED.** Callback elapsed includes
time between callbacks; `RunRenderer` starts its native timer after message
dispatch. Thus the maxima are not interchangeable. All 4,000 native samples fit
the 8,192-slot buffer. Existing aggregates do not identify which message, scheduler,
window/startup or other responsibility owns the missing time. A logged swapchain
recreation is not causal proof. Similar Stage 1 first-operation/service tails do
not explain these much larger gaps either.

After capture 1 independent review permitted only the remaining unchanged declared
captures to assess repetition, retaining the first. Capture 2 repeated the anomaly;
validation stopped immediately. Capture 3 is **NOT RUN**. Integrated acceptance is
**NOT QUALIFIED**, not a manufactured pass based on good medians. No new profiling,
renderer investigation or correction was attempted. Manual acceptance is **NOT
PERFORMED** for the new powered route; old Stage 1 acceptance is not substituted.

## Regression and review

Full solution Debug/Release builds: zero warnings/errors. Full Simulation:
**78/78 registered groups** in each configuration, confirmed by matching runner
labels (93 printed PASS lines include nested tests). Focused affected gates,
M14.17 graphics regression, assembly presentation, ReferenceFrames, Precision,
verified BEPU dependency and all 18 launcher tests pass Debug/Release. Full
Simulation also preserves the M14.18/M14.19/M14.20 and later protected groups.

Test-authoring history is retained: observation inside an owned phase initially
hit the correct reentrancy refusal; observing after exit corrected the test. The
first new impulse oracle assumed an exactly unit/up normal; actual FP32 normals
require normal and both tangent rows. Reading the actual native row formula fixed
that assumption with no change to the 0.01 N s bar or production physics.

The first isolated launcher layout was one directory too deep for its unchanged
five-parent repository lookup. The same binaries passed at the normal depth.
One Debug graphics invocation mistakenly entered unrelated GPU campaigns; it was
stopped when recognized, with no observed test failure and **no full Graphics PASS
claim**. Only the relevant graphics routes were subsequently run in both builds.

Independent read-only red team found the four static closure responsibilities
correctly addressed and no remaining material ownership/export defect. It confirmed
the bounded dry numerical test does not expand stock depletion scope. Integrated
performance and manual gates remain unpassed; static review cannot grant promotion.

## Identity, reproduction and cleanup

HEAD/main/origin/main/authoritative remote remain
`ab14e3e0f8b53bf5e8bf580a99aea3f2b6fd21f5`, branch
`codex/srv01-supported-contact-admission`. All 67 historical tag refs match preflight;
nothing staged. Current complete candidate seals are in `identity.json`; historical
Stage 2 seals and reports remain unchanged. No native renderer, shader, asset,
dependency, README, license, milestone or ref change occurred.

This pass changes only two production world files, the existing powered tests and
performance gate classification, one new oracle/closure test file, the bounded
sample route and its focused presentation tests. `identity.json` enumerates the
exact ten paths changed by this pass separately from the inherited candidate.

`reproduce.ps1 -Mode Build`, then `-Mode Focused` or `-Mode Regression` with
`-Configuration Debug`/`Release`, reproduces the relevant gates. `-Mode Performance`
is the three-process isolated characterization. `launch.ps1 -Characterize -Run N`
captures the live route; **do not resume this stopped campaign without Project
Control direction**. The no-argument launch waits at READY for a later manual
review. Scripts are reproduction instructions, not an authorization to continue.

Retained: plan, KSA/source/history map, concise measurements/validation records,
source/log identities, refusal/physical witnesses and scripts. No bulk raw log or
build tree is copied into evidence. `cleanup.json` and `cleanup.md` identify every
remaining/new disposable root, its exact count/bytes and copy/paste manual commands.
The previously blocked old scratch root was not retried.

**JUDGMENT: REVISE. STOP FOR PROJECT CONTROL.**
