# Population environment

ALBATRONIC007, Windows 11 Pro 10.0.26200, active local console session Tyler/1. .NET SDK10.0.303; Release managed/native build; native validation layer disabled; Visual Studio18 / Vulkan SDK1.4.357.0. Exact preflight in preflight.json. GPU selected by the native executable: AMD Radeon RX6800XT, Vulkan1.4; driver32.0.21045.5002. Other installed virtual-monitor/GPU adapters are recorded, not claimed to be in use by this renderer.

Actual client/swapchain extent **960 x540**, ordinary overlapped window, FIFO presentation. The original plan's1280x720 description was wrong; protocol-note.md preserves that deviation. All fresh logs must independently show the same extent; no run is replaced. Native Window uses the existing default dimensions; no NOVACORE_WINDOW_* overrides are set. Environment inspection found no NOVACORE/COMPlus/custom DOTNET runtime override; standard DOTNET_ROOT and VulkanSDK paths remain.

Each route uses the same isolated executable/assets, exact24sealed candidate sources and unchanged physics. Stock catalog FourHorn digest `ac23c15ae52adc5e8e836bd954d9084b10a2699cc01f0a6906a992724ea67fcf`,37visual mesh instances,23shared meshes. C uses the existing128-command free-flight behavior; A/B retain1200contact intervals. Each process starts cold and automatically opens, starts and closes its window after4000nativeframes. Preparation/replay/reporting are outside measured frame arrays. Fixed initial camera; no intentional input or scripted interaction. Desktop/background activity and OS/driver caches are not guaranteed identical or empty.

All nine use default logging. The earlier detailed captures included `--log=renderer,vulkan`; these fresh controls do not. Historical witnesses therefore are not claimed to have exactly identical environment/observer/logging exposure. This limitation is relevant to a nonreproducing result and is not a causal attribution.

The extra observer records after-title timestamps and copied authority into cold-preallocated arrays. No hot output, processCPU calls, GC forcing or allocation attribution. GC generation counts bracket the live capture coarsely, before post-render reference replay; native CPU/GPU averages are existing reports, not per-gap attribution. QPC frequency10,000,000Hz. Whole-process duration includes startup, post-run reporting and replay and is not display exposure. Per-capture first/last timestamps supply actual display exposure.

Source boundary: native RunRenderer pumps outer messages before frameBegin, performs one Update and one Draw, then appends one native frame sample. Managed callback services at most4publications and builds one submission. Rendering is not multiplied by backlog. A full after-title interval crosses adjacent native frames, not just the frame with the same index. The native overall wall-time average includes observer output and is excluded; ordered native samples remain untouched.
