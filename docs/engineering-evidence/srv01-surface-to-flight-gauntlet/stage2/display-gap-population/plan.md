# Predeclared display-gap population

Declared before building the temporary observer or running any capture, 2026-09-17.

Exactly nine fresh Release processes, fixed order **A1 B1 C1 B2 C2 A2 C3 A3 B3**. A = `srv01-powered-support`, B = `srv01-supported-contact`, C = `stock-assembly`. No D: the three required populations already supply powered, nonpowered-contact and free-flight controls. No replacement, adaptive count, or retry. Invalid captures remain recorded. Stop early only for the ticket's concrete-cause/material correctness stop rules; record unexecuted slots.

Each uses 4,000 native frames, 1280 x 720 windowed Vulkan, existing pacing, automatic start, fixed initial camera and no intentional input. No simultaneous build/test/capture. Same executable/assets for all. Fresh process is not a flushed OS/driver/shader cache. Background scheduling and desktop activity cannot be perfectly controlled.

Preserve A/B's 1,200 intervals / 20 seconds and C's banked 128 x 15,625-tick / 2-second main-gimbal-RCS behavior. C is banked behavior in the current candidate executable, not an untouched historical binary. Do not stretch C's physical episode. Its held endpoint is a shared-display control, separately labelled from active free flight.

Existing legacy display/service counters and native frame timer remain unchanged. Legacy display sampling ends with each episode. A minimal temporary observer adds a separate **full-capture after-title interval** series including held endpoints: 4,000 callbacks normally produce 3,999 intervals. It stores preallocated copied observations/accounting and performs no hot logging. Native reporting only dumps the existing ordered frame array after rendering. Build temporarily patched source, restore original bytes in guaranteed cleanup before captures. Observer overhead is acknowledged; no claim of binary identity with the original uninstrumented captures.

All callback/frame indices are zero-based. Interval ending at callback i spans after-title timestamps i-1 to i, covering the previous frame's suffix, outer host/message work and current callback prefix. It is **not** native frame i. Compare adjacent native work without asserting exact phase attribution. Preserve phase/frontier at both ends; first eight callbacks are descriptive cold-start, then active or held. No warm samples are discarded.

Diagnostic screening: >=250 ms is an extraordinary-event threshold, far above normal ~5.5 ms and observed ~50-65 ms cold operations, below both original 595.898/1228.1696 ms witnesses. It is not a permanent budget or waiver. Report all continuous distributions, maxima and eight largest intervals per capture, so conclusions do not depend solely on screening.

Nearest-rank median/P95/P99/max; pooled per-route samples also reported with active/held exposure. No independence/random-sampling assumptions or inferred probabilistic upper bound. Compare recurrence only within these nine captures. No recurrence means unresolved, not exonerated.

After rendering, replay identical recorded commands one canonical interval at a time and compare every sampled live endpoint's physical bits, exact stores, full mass/COM/inertia, revisions and history count. Record contact body/generation/shape continuity, independent transformed-corner sampled support, exact credit minus published ticks = debt, <=4 publications/callback and completion. Existing qualified all-interval physical tests remain authority; sampled geometry does not replace them. GC counts are two coarse episode/capture observations; CPU-around-gap and hot allocation attribution are unavailable. No profiler, new KSA work or optimization.

Prior failed captures and prior two diagnostic captures remain historical evidence in ../closure and ../display-gap-closure. Stage2 stays unqualified unless the current contract explicitly permits promotion; no self-waiver or manual acceptance during this population study. Stage3 closed; unbanked.
