# Independent read-only review

Two independently scoped agents inspected source; the lead alone edited candidate
files. Neither reviewer ran simulations, wrote source or changed KSA/Blender data.

## Geometry/source reviewer

Read the actual original NovaCore SRV visual recipe and banked assembly definition.
Identified explicit eight-box physical authorship over seven parts, including a
separate main flange. Confirmed these are conservative new physical proxies,
not existing visual or mass authority. Proposed independent scalar corners and
COM-in-footprint checks. Geometry first gate passed before contact stepping.

## Authority and final failure reviewer

Four source defects in the implementation draft were identified and corrected
before the final physical run:

1. Accepted gravity is -9.81 m/s²; the draft had used -9.80665.
2. Consumer guards must preserve null-authority refusal.
3. A post-step supported-domain failure must poison private continuation before
   staging/publishing the endpoint.
4. Contact abort needs its own consumer entry; the free-flight abort must refuse it.

The final stopped run was reviewed against source and the accepted design.
Drift0.00011947824532399198 m exceeds0.000061 m. No unit/arithmetic error or missing
moving-frame subtraction explains this stationary run. The metric adaptation is
explicit: horizontal material-O displacement from600 versus the older article's
3D COM displacement from601. Neither distinction permits an automatic PASS.

Peak and final-window height assertions passed before the drift exception. Speed,
angular and aggregate-accounting assertions after that exception did not execute.
Their printed values and completed per-interval checks are retained without
misrepresenting the aborted test as passed.

## Unfinished qualification / implementation review

- Orientation/normal-impulse witnesses are absent from the current physical harness.
- Private-only versus publish/ack physical equivalence is not yet measured.
- Host partitions/backlog, moving frame and all physical/configuration/resource/
  revision/overflow mutation rows remain incomplete.
- Native child representation and construction-failure cleanup need their full
  independent assertions, beyond source review and the scalar authored geometry test.
- A prechange free-flight runtime/2 byte fixture remains to be tested. Current
  source review finds unchanged DTO and omitted-null provenance, not a measured
  compatibility PASS.
- New focused selectors exist; full-suite integration/registration and regression
  campaign remain unfinished at this stop.
- Allocation/storage/performance and presentation/manual acceptance are unrun.
  Presentation-retained state must count in this ticket's combined8MiB budget.
- The draft must not be promoted or banked based on compilation/cheap checks.

**Judgment: REVISE. STOP upheld.** No causal physics correction was established,
implemented or benchmarked. The next responsibility remains a Project Control
decision on this failed physical gate, not powered support or departure.
