# One Florida support authority, multiple consumers

The prior graded-ground physics result and full-facility presentation are retained historical evidence. They do not grant the current slab route manual acceptance.

## Authored support

Classification A/B: use the existing bottom base and footing declaration. AuthoredFacilityGeometry.h defines the base x[-32,32], y[-24,24], z[0,1.5] and a same-footprint footing extending to -D. FloridaLaunchSite supplies the authenticated common root and D=6.835569277405739m. Their contiguous union is one rectangular support volume; no invented infinite ground or contact mesh extraction is involved.

Root altitude 21.970461536198854m, graded bottom 15.134892258793116m, top 23.470461536198854m. Site coordinates East/Up/-North give dimensions (64,8.335569277405739,48)m. The single box's native top is y=0. Material origin is 1.7m above that top, derived from the unchanged authored aft support face, not a visual offset. The COM permutation, mass, geometry, gravity, solver quality and contact policy remain unchanged.

FloridaSlabSupport is an immutable Core value owner with physical authority, root, footing depth, dimensions and top. FloridaLaunchSite prepares it from the existing authored survey and authenticated query. AssemblyFloridaSite authenticates the site and binds the box top; LocalContactWorld creates one retained BEPU Box. Terrain proves its grounding but supplies no contact constraint. Rendering uses the same descriptor and centre. Mesh 7 is an independently authored unit box using the existing base material.

Only the base/footing union is submitted. Meshes 3/4 (old full facility and separate footing) and 6 (qualification support proxy) are excluded from this route. The old full-facility shadow caster is also excluded. No removed arm/cube remains as a collider or caster; no new terrain shadow system is claimed.

## Shared construction

GUI: existing launcher Florida selection -> --scene=sol / --surface-site=florida-launch -> SampleOptions.UsesFloridaSlab -> existing SolarSystemScene -> StockAssemblyDevelopmentScene canonical Florida preparation.

Manual: --scene=srv01-florida-support -> the same StockAssemblyDevelopmentScene canonical preparation, with a deterministic J2000 presentation harness.

Both prepare the same site/slab/design and use AssemblyApplicationSession, retained contact, canonical host admission/publication and copied observations. The epoch is an explicit consumer input; equal epochs produce equal initial authority and endpoints.

The GUI remains a Solar scene: planet focus, overview/orbits, continuous zoom, surface camera, celestial progression and lighting retain their existing owners. Contact begins only after cold preparation; its current qualified responsibility is still bounded to 1,200 intervals. Completion holds the last copied **site-relative** physical endpoint while the Solar presentation may continue browsing. Solar display time is not the spacecraft canonical clock and does not authorize departure, warp evolution or future physical continuation. No programmatic 20-second application shutdown is installed in the GUI. The manual harness waits for Space and freezes celestial display at READY.

Future control, departure, launch stack, SAS, navball, time-warp physics and save/restore remain separate qualification responsibilities. No capability is added by the camera or by numerical equality.

## Independent witnesses

Initial authored corners are inside the finite rectangle with >23m edge margin. Four aft corners define a +/-0.26m support rectangle; COM projects to its interior. During 1,200 publications, all transformed corners retain >23m slab-edge margin; minimum native exported contact count is four. The settled geometric feature set does not change; this is not a claim that every internal warm-start manifold byte is identical.

J2000 qualification retains the independent matrix/product-derivative p/v/q/w oracle. The added actual-current-UTC consumer check uses an independent split-angle matrix **position** oracle and 1,200 physical intervals. It does not claim a new nonzero-epoch full p/v/q/w certification. An initial comparison against the old full-epoch rotation was a test-oracle mistake: its large-phase rounding boundary differs from the already admitted whole-second/local-angle contract. No production frame arithmetic or tolerance was changed.

The render regression checks material-origin positions, exact dimension transport, all eight slab corners under an independent basis, two camera locations, OFF exhaust, nonmutation and absence of obsolete meshes. Existing Solar tests remain unchanged and are run alongside the new shared-consumer test.
