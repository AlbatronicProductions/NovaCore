# Stage 5 — Florida physical/site integration: stopped

**REVISE — material settled-support failure. STOP FOR PROJECT CONTROL. UNBANKED.**

Stage 4 passed at its preserved checkpoint. The parent campaign then authorized evaluating this direct downstream responsibility. Current shared source contains an **unqualified Stage-5 draft**. Stage-4 full-suite/allocation results do not qualify that later tree. Stage 6 remains closed. No launch UI, Florida player route, manual acceptance, milestone or bank is claimed.

## Responsibility and promotion gate

Bind the same finite development-profile SRV-01 to an actual authoritative Florida graded-ground patch, initially engine-off, for 1200 ordinary intervals. Reuse the existing terrain-v5 physical provider, Florida grading identity, canonical assembly, private BEPU world and owner publication. Do not redesign the pad or use a rendering surface as collision authority.

Stage-4 mission/profile, focused physics, allocation, regression and three-process measurements completed before promotion. Independent reviewers allowed the objective site/frame responsibility to open without a new product decision. They required full frame transport and angular dynamics rather than silently freezing a rotating Earth. The chosen draft uses a site-fixed native frame; it was not yet fully qualified.

## Current source and KSA gate

Direct installation/history inspection is recorded in [ksa-site-chain.md](ksa-site-chain.md). It includes site placement, CCF/nonrotating frame conversion, physical terrain/static ownership, fictitious forces, retained solver state, ready/apply, and invalidation. Current-source method tokens and newer-history distinctions are preserved. No KSA assets or source were copied.

NovaCore's current physical provider supplies a trusted full-weight grading capability. The draft verifies terrain/source/generation/dataset/composition/facility identities and a complete 16 m patch. Its center is 48 m east: midway between the existing platform's 32 m half-width and the grading region's 64 m inner east bound. The patch spans 40..56 m, outside the authored platform. It is **graded ground**, not the pad top or a newly designed facility.

Terrain witness: body 6, source 2, version 5, physical generation 4, radius 6371008.8 m; global SHA `4600bc01767eb81404756af62c0ee87b4bc459b82de15dca6989df34fef76317`; regional SHA `c45c6d94e004e1a2927dc65d405a347b1800c619b22b2eb6b3543f3c445d3afe`; composition 86364352542676184; facility 5597594574148789740; query policy 1. Missing/stale data refuses. The existing plane altitude is 15.134892258793116 m; this is not a new height offset selected to make support pass.

## Draft implementation boundary

One immutable site binding carries source identity, exact 20-second domain, Earth orientation model, local origin/orientation and Earth gravitational parameter. Canonical assembly state is site-relative under an explicit site CCF carrier, itself under an Earth-centered nonrotating carrier. Copied observations transform to the Earth frame with full position, velocity, orientation and angular-rate transport. No duplicate assembly/resource owner is introduced.

The Earth orientation is evaluated through the current owner. Instantaneous angular rates/accelerations are derived from its exposed linear orientation model; the banked one-second finite-stencil angular observation is not relabelled an exact derivative. Site-only callbacks add central Earth gravity, centrifugal, Coriolis and Euler terms. The gyro correction distinguishes absolute body angular velocity from native relative angular velocity. Existing ordinary timestep, solver iterations/substeps and contact selector are unchanged. This draft models Earth-centered two-body gravity; it does not claim solar tidal acceleration or a global barycentric integration.

The development support factory is explicit and engine-off. Existing stock admission retains its guards. No mass-refresh, moving-site departure, full-flight or new propulsion model is qualified by this draft. The actual source files and SHA-256 values are enumerated in `identity.json`.

## Executions and decisive result

1. Debug Graphics-project compilation passed with zero warnings/errors.
2. The first cheap invocation used the free-flight servicing method on a contact-owned session. It refused `InvalidAuthority` before any private step (frontier zero). This was a test-call error, not a solver failure. The route was corrected to the existing contact service.
3. Recompilation passed with zero warnings/errors. One 1200-interval run completed. Its original output is retained verbatim inside `results.json`, including the now-withdrawn labels.
4. Comparing its final endpoint to inherited settled-support requirements exposed a material failure. No physics correction, repeated run, performance campaign or further qualification followed.

| Witness | Observed | Required / disposition |
|---|---:|---|
| Peak transient penetration | 0.019043728598313825 m | <= 0.020 m: PASS for this operand only |
| Final penetration | 0.014560651361285926 m | settled absolute height <= 0.000122 m: FAIL |
| Final linear speed | 0.13725863223254325 m/s | <= 0.000061 / 0.016667 m/s, approximately 0.00365993: FAIL |
| Final velocity | (0.13680320220602227, -0.0018795684388216859, -0.011012865703827182) m/s | Residual motion, not settled support |
| Final angular velocity | (-0.0002068077434545408, 0.006575534975447907, -0.08108537076623575) rad/s | Full settled-window angular maximum was not retained |
| Final tick / publications | 20000000 / 1200 | Exact completion/accounting witness |
| Stores / mass | unchanged at every checked interval | Engine off |
| StateRevision / history | 1200 / 1200 | One per committed interval |
| TimelineRevision / remaining debt | 0 / 0 ticks | Unchanged timeline, exact admitted duration |
| Native generation/body | unchanged | Retained world, no rebuild |

The proper final-600 contract comes from `AssemblyContactAdmissionTests.Physical`: feature-derived tolerance 0.000061 m; all 600 absolute support heights <= twice that; horizontal drift <= that tolerance; linear speed and angular surface speed <= tolerance/0.016667. The run's final endpoint alone disproves the height and speed requirements. It does not establish the exact count of compliant intervals, full-window drift, or a cause.

**Withdrawn preliminary labels:** `support=600/600` and `FLORIDA_SITE_CHEAP PASS`. The preliminary test incorrectly admitted minimum height from -20 mm through +0.122 mm, confusing the transient penetration ceiling with settled support. Its output is not erased or presented as a pass. The test now uses independent transformed-corner matrix arithmetic and the inherited height/drift/speed predicates. That corrected test has **not been rebuilt or rerun**. Production draft bytes did not change after the recorded contact run.

## Limited frame witnesses

Position round-trip maximum 4.0783538020105096e-10 m; velocity round-trip 3.0468225842716704e-14 m/s; independent centered position derivative error 7.995158878049328e-7 m/s; inertial acceleration reconstruction error below 2e-13 m/s² in the tested sample. These establish limited transform checks, not a completed rotating-contact physical qualification.

The sampled local acceleration was (-8.18366632701711e-5, -9.794044678203283, 0.014248756826371613) m/s². Its nonvertical component was not suppressed. No attribution proves that frame forces, high resource mass, contact selection, or another responsibility causes the residual motion.

## Qualification not reached

Release, complete failure/refusal matrix, host-partition comparisons, exact allocation, retained storage, performance, full regression, presentation, player/manual acceptance and Stage 6 were not reached. The physical stop precedes those costs. No renderer/assets/scene/launcher production changes were made for Stage 5.

Independent red team confirmed the physical stop, withdrawal of the incorrect labels and separation of Stage-4 identity. Full Stage-5 architecture acceptance was intentionally withheld. No correction is proposed as proven.

## Preservation and next decision

`pre-stage5-overlap.zip` retains exact predecessor bytes for all 11 overlapping paths. `identity.json` checks all 35 Stage-4 seals against current bytes or that archive and all 29 Stage-3 seals against current bytes or the two predecessor archives. Historical reports are unchanged. Current source is left as a clearly identified unqualified reproduction draft, not discarded or silently promoted.

Reproduction and cleanup: [reproduction.md](reproduction.md), [cleanup.md](cleanup.md). `closeout.py` performs only identity/result/inventory collection, never a simulation or deletion.

**Judgment: REVISE.** Project Control must scope any causal follow-up. No solver tuning, threshold change, another run, Stage-6 work, milestone or banking is implied. **STOP FOR PROJECT CONTROL.**
