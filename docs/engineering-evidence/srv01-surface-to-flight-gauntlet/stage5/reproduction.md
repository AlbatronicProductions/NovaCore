# Stage-5 stopped reproduction

No automatic rerun is authorized by this report. The retained final endpoint already disproves settled support. A further run or causal investigation needs the next Project Control scope.

The source identity in `identity.json` names the draft's 11 overlapping files and three new files. It also seals current Stage-4 and Stage-3 preservation locations. Base commit remains `ab14e3e0f8b53bf5e8bf580a99aea3f2b6fd21f5`; restore the qualified Stage-4 overlay as described in its reproduction instructions, then apply these Stage-5 draft inputs in an independent workspace. Do not overwrite the user's current tree.

The current test differs from the executed preliminary test **only as documented**: it restores inherited settled-height/drift/velocity predicates and independent matrix corner evaluation. It has not been rebuilt/run after that correction. The exact original stdout, its hash and execution caveat are in `results.json`. Do not use the original `CHEAP PASS` label as acceptance.

With the unchanged repository physical terrain datasets available, native prerequisites built as described in the Stage-4 instructions, the bounded diagnostic route is:

```powershell
dotnet build tests/NovaCore.Graphics.Tests -c Debug --artifacts-path build/srv01-stage5/artifacts -p:ContinuousIntegrationBuild=true -p:NativeBuildDirectory=srv01-stage4/native-debug --nologo -v:q
dotnet build/srv01-stage5/artifacts/bin/NovaCore.Graphics.Tests/debug/NovaCore.Graphics.Tests.dll --assembly-florida-site-cheap
```

The original execution reused `srv01-stage3/native-debug`. This is one headless physical/site route; it does not launch a window, qualify the whole Graphics suite, or implement Stage 6. The corrected test should fail at the inherited settled-support guard; this expected behavior is source-derived, not a new measured result.

Recheck terrain identity against `results.json`, not arbitrary downloaded/rebuilt replacement terrain. The authoritative provider requires global and regional data and the existing grading capability. Missing data is a refusal, not grounds for a flat fake surface.

`python docs/engineering-evidence/srv01-surface-to-flight-gauntlet/stage5/closeout.py` collects identities/results/cleanup inventory without simulation, building or deletion. It is a one-time closeout collector while the listed disposable logs still exist; after cleanup, the already-retained JSON is the historical evidence. Do not regenerate historical source seals from a changed candidate.
