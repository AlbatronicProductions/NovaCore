# Bounded performance revision — predeclared attribution

The active population is UNPOWERED STILL-FUELLED RETAINED-CONTACT CONTROL:
CenteredBaseline, Ignite=false, retained fuel 0.000013020833333333334 kg.
Historical output and stopped fingerprints are preserved in the parent evidence.

## Window before measuring

Complete(Contact) observes the copied canonical frontier; computes the next exact lattice
duration; submits one host credit; services debt; checks credit/status/count. It does not
read history, compare trajectories, observe constraint rows, or construct worlds.

| Order | Responsibility | Classification | Measurement bucket |
|---|---|---|---|
| 1 | Copied observation, integer next duration, two success checks | observation is production; duration/status wrapper qualification-only | A residual |
| 2 | Owner entry; source/clock/world checks; host accounting preparation/recheck/commit/ack | PRODUCTION-REQUIRED | A, D, I |
| 3 | Service eligibility, command drain and boundary closure | PRODUCTION-REQUIRED | A/B |
| 4 | Engine demand and exact finite-resource preparation; source/parent rechecks | PRODUCTION-REQUIRED | B |
| 5 | Physical proposal preflight; mapping exact powered fraction to six native values | PRODUCTION-REQUIRED | G/C |
| 6 | Same-body property update, world admission and source validation | PRODUCTION-REQUIRED | I/D |
| 7 | Retained BEPU Timestep, 8 iterations/1 substep | PRODUCTION-REQUIRED | E |
| 8 | Native endpoint validation/export, sealed receipt, support-domain check | PRODUCTION-REQUIRED | F/D |
| 9 | Joint successor preparation and final applicability, native endpoint-bit comparison | PRODUCTION-REQUIRED | G/D/B |
| 10 | Fixed canonical endpoint/resource/actuator/clock/debt/revision/history writes; lease retirement | PRODUCTION-REQUIRED | H |
| 11 | Fixed expected-state/private acknowledgement, copied result, next eligibility loop | PRODUCTION-REQUIRED | I/A |
| outside | Cold world/shapes/history and fixture preparation | OUTSIDE WINDOW | none |
| outside | Distribution sorting/reporting, native-bit/history equivalence check | TEST/QUALIFICATION-ONLY, OUTSIDE WINDOW | none |

## Static cost ownership

History is one fixed 4048-byte record append, O(1), not O(history length). Record/preview
copies are fixed-size. Command queues/engine-edge scans are bounded by existing capacities;
the measured steady control has no new edges. Canonical lookups are keyed single-body
lookups. Validation repeatedly recaptures paired state, clock and exact resource values;
parent-resource reads recursively revalidate engine and exact mass. Resource integers have
34 fixed limbs, ratio projection 35-word workspace and 54 quotient iterations. These are
bounded O(1) in episode length but potentially nontrivial constants. Solver work scales with
active bodies/contact constraints and configured iterations; this fixture has one body/pair.
No history hashing, LINQ/enumeration, cold preparation, or growing exact arithmetic was found
inside Complete. No cause is assigned from this audit alone.

## Bounded diagnostic campaign

One fresh Release attribution process initially: existing prime128, cold CenteredBaseline,
warm128, measured1024 complete operations. Preallocated per-phase counters and sample arrays;
temporary nested timestamp scopes partition the SAME operation, including residual work.
No logging, allocation, lock, arithmetic/solver/policy/order change inside the window.
Counter allocation/thread checks and GC snapshots outside timing. Report median/P95/P99/max,
same-sample top16 breakdowns, exact time partition sum, source restoration and paired trajectory.
An additional probe is permitted only if a specific unresolved narrow phase requires it;
no automatic full campaign. Once cause is assigned, remove instrumentation before correction.

A-I buckets are disjoint elapsed costs. Additional J (exact mass reconstruction), K (ratio),
L (powered source validation) identify nested responsibility; parent child matrices preserve
where these costs occur. Timestamp overhead/codegen perturbation is a limitation; production
identity and exact output comparisons are mandatory, and uninstrumented correction recheck
retains the original complete boundary. The raw failure max is not automatically attributed
from a later sample or a coincident phase. No profiler/JIT/GC tracing.

After at most one proven correction: affected focused contracts, exact allocation and
determinism, then three fresh Release control processes128/1024. Stop first ceiling failure;
no correction2. Only three passes authorize deferred populations and full qualification.
