# Production refusal and lifetime closeout

Existing Authority tests retain all canonical/private nonmutation comparisons, not merely
status checks: negative/zero/overflow/duplicate host samples; insufficient/exact debt;
wrong owner/reentrancy; foreign owner; fabricated/default/duplicate/consumed leases;
one outstanding endpoint; stale state/timeline/configuration/mass/forces/torques/resource;
foreign world/generation/frontier; event before/at target; capacity/revision overflow;
resource/engine retirement; retry publishes existing endpoint without another solve or
debit; physical and accounting committed/private-invalidated outcomes; export failure;
disposal and property-only retained manifold continuity.

The new FinalRefusalCloseout adds two explicit integrated witnesses:

- Change command revision after native staging, before publication. Source path:
  PublishPoweredFlightInOwnedPhase -> ReadFinitePropellantInOwnedPhase ->
  ReadSingleEngineActuationInOwnedPhase. Publication maps an unreadable parent to
  InvalidProposal before canonical writes. Canonical/private bytes remain unchanged.
- Supply a segmentation with wrong exact End=16667 for the first16666 interval to
  PreparePoweredInput under the existing owned phase. OutsideModel precedes native input
  installation. Canonical/private state remains unchanged.

Initial new test mistakenly expected StaleSource in the first case; it failed. Qualification
paused. Static source proved the existing mapping to InvalidProposal. One bounded TEST
expectation correction was made; no production behavior changed. The corrected exact status
and nonmutation tests passed. Original failure output remains accounted for in regression
evidence. This was not a weakening of a banked test or an allocation retry.

Wrong body: sealed receipt has no caller-supplied body handle. The world owns a private
readonly native BodyHandle, transferred once from its preparation. A genuine same-world
receipt cannot select a different body. Foreign-world/issuer/generation checks cover the
representable substitution route. This is a structural exclusion, not a fabricated claim
that a body-handle mutation was dynamically tested.

Existing lower-layer permanent tests independently cover stale command revisions, wrong
engine/resource intervals and foreign engine/resource authority. They remain in the full
Simulation runner. Ready lifetime tests retain deep standalone retry and expire source
readiness before the first canonical write. No new failure injection system was added.
