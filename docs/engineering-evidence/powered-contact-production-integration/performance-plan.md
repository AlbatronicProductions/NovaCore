# Predeclared integrated timing gate

Run only after focused correctness and separate exact-zero allocation pass. Three fresh
Release processes; stop on the first material failure, no retry-to-green. Runtime defaults,
no observer, profiler, no-GC timing wrapper, tiering override or artificial solver setting.

Each process primes the complete owner path with one genuine centered episode, 128
operations. Cold construction is measured separately. Dry-supported retained contact then
uses 128 warm plus 1,024 measured complete host-credit/step/export/joint-publication/
acknowledgement/copied-observation operations on one retained world.

Four genuine finite episodes (CenteredEndpoint, OffCom, NearUnloading, TinyEvent) each
report the first three complete interval costs individually, including full-powered,
endpoint/interior exhaustion and the first dry successor. No refill, frontier reset or
repeated cold-world population masquerades as powered production cadence. Event populations
are too small for per-process percentile claims; every maximum must satisfy .50 ms.
OffCom additionally continues on its same retained world for 128 warm and 1,024 measured
dry operations. Each retained population reports median/P95/P99/max, worst index, GC counts,
8/1 solver policy and one exclusive owner phase. Full-path ceilings remain
.05/.10/.25/.50 ms. No physical work or rendering per synthetic display frame is introduced.

The pure fixed-workspace mapper separately uses 128 warm/1,024 measurements over one genuine
copied event input; this is labelled pure preparation, never a complete-step population.
Its absolute distribution cannot grant a whole-engine FPS claim.

Carry forward historical diagnostic Boundary-B preparation P95 about .26 ms, max .30–.38 ms,
UNATTRIBUTED. The current run must report whether such tails recur. A repeating material
tail requires attribution and Project Control review, not automatic optimization.
