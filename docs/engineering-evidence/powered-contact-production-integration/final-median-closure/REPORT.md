# Final bounded median-closure gate

**STOP — REVISE / INSUFFICIENT PAYOFF.**

The implemented KSA-convergent ready/apply correction is preserved. No second correction,
additional attribution run, control retry, broad qualification or banking follows.
Production powered contact is **not qualified** against its existing performance contract.

## What happened

The first correction cleared its source/lifetime/gross .080 ms design gate, then passed
bounded correctness, refusal/retry, numerical equivalence and exact-zero allocation. Its
first fresh uninstrumented Release control failed median .0683 ms against .050 ms; all
other percentiles passed. Runs2/3 correctly did not execute.

Project Control then authorized one final bounded investigation, with >=.025 ms avoidable
or repositionable MEDIAN work required before another correction. Source inspection found
one plausible responsibility: consuming the existing sealed engine/resource previews in the
uninterrupted ready interval instead of recursively recapturing their sources.

Old attribution did not measure the corrected distribution. Exactly one diagnostic process
reused the retained timestamp observer with three extra category labels. It measured the
same complete operation and verified exact output equivalence. Nine temporarily instrumented
files were restored to exact pre-probe corrected bytes; two temporary sources were removed.

## Current authoritative identity

- HEAD/main/origin/main: `49057fecceb0f725d5f551ec40e2780971b0d81d`.
- Branch: `codex/powered-contact-production-integration`.
- M15.0: `4607d8c802006d5e1a01c595ab608cf53a4dab6b`, unchanged.
- All65 historical tags unchanged; no staged paths, commits, tags, pushes or banking.
- SDK10.0.303, runtime10.0.12. No tiering/PGO/GC/profiler configuration change.
- [Identity](identity.json) records19 current candidate paths and changes from the stopped17.
- [Restoration](restoration.json) proves the final probe preserved corrected source bytes.

## What remaining work is materially dominant?

Same population: **UNPOWERED STILL-FUELLED RETAINED-CONTACT CONTROL**,
CenteredBaseline, engine OFF, fuel .000013020833333333334 kg.
One fresh Release diagnostic: prime128, warm128, measured1024 complete operations.

| Whole measured family | Same-operation median ms | Interpretation |
|---|---:|---|
| All engine/resource preparation and readers | **.0234** | Largest relevant coherent family; includes indispensable work |
| All engine/resource preview readers alone | **.0084** | Generous upper bound on proposed ready-preview consumption correction |

Exclusive category medians below describe distribution, not additive median components:

| Work | Median ms |
|---|---:|
| Resource preparation, excluding separately timed engine reads | .0128 |
| Retained BEPU ordinary timestep | .0114 |
| Contact validation/preparation, excluding mapper/BEPU/export | .0102 |
| Transaction preparation/validation, excluding separately scoped children | .0084 |
| Remaining powered-source validation, excluding nested world comparisons | .0066 |
| Resource-preview readers, excluding engine reads | .0051 |
| Live prepared input mapper | .0038 |
| Engine-preview readers | .0033 |
| Host/service residual | .0032 |
| Command and engine preparation residual | .0022 |
| Export/seal | .0018 |
| Canonical fixed writes/history | .0004 |
| Acknowledgement/property update | .0002 |
| Source-mass reconstruction / exact ratios in this OFF control | 0 / 0 calls |

The .0183 ms uninstrumented median excess is not assigned artificially to one source line.
Required resource preparation and retained solver work are substantial. The proposed redundant
preview-consumption responsibility is much smaller than the deficit and the payoff bar.

## Why could some reader work be avoidable?

Six engine readers each recapture command/source and physical/configuration state. Four
resource readers invoke them and copy preview values. After engine/resource sealing, those
private previews and source facts remain stable within the admitted, continuously owned
interval. Some repeated captures can plausibly consume that proof.

This is a source/lifetime opportunity, not a finding that all reader instructions are wrong.
Actual struct copying is JIT-dependent; source assignments and out-discard sites are not
measured memcpy cost. Entire read scopes still include required active/seal/generation,
parent, interval and applicability checks. Separately entered preview/prepare/publication
and retry must remain deep. No correction is implemented because payoff fails first.

## Required outcome protecting any reuse

Preserve issuing owner/phase, active engine boundary and resource generation, exact parent
linkage, command closure/revision/sequence, selected consumer/frontier/interval, final
state/timeline/clock/resource applicability, genuine endpoint/world and physical-slot checks,
single use, no solver replay on retry, and distinct committed/private-invalidated outcomes.
Borrowed private facts must expire before canonical writes and preview clearing. Copied
observations/history remain values. Existing ready correction already meets its accepted
contract and remains unchanged.

No-change successor-mass recomputation is a separate candidate: exact NoDemand returns
successor=source and consumed=0. Its work is inside the resource-preparation bound and is
individually smaller. It is not bundled with preview-reader reuse, host-final checks or
postcommit-loop checks to manufacture a .025 ms result.

## Current KSA convergence

The actual `E:\Kitten Space Agency` installation remains build2026.9.10.5438. All four
previously inspected DLL/dependency hashes were freshly reverified unchanged. Current
source and actual Discord history evidence remain directly applicable; see
[the direct review](../lifecycle-cost-convergence/ksa-lifecycle.md).

Current source: GetNewProps/GetReadOnlyProps and GetNewStates/GetReadOnlyStates materialize
staged values once; UpdateRockets has dependency-aware no-change preparation; no active
nozzles means no consumption/mass recomputation. Ready/apply consumes staged results.
Classification remains **ADAPT**, with NovaCore-only exact resource/event and atomic
successor requirements retained. KSA OFF does not authorize skipping command edges, exact
NoDemand classification, canonical interval/debt/revisions or history. No timing is inferred
from KSA, and history is not substituted for current implementation.

## Absolute median payoff

Per-operation exclusive times are summed BEFORE percentile calculation. No summed medians,
per-call fractions or nested double count.

| Bound | Measured median ms | Required .025 | Shortfall |
|---|---:|---:|---:|
| Entire engine/resource family, even granting every required instruction free | .0234 | .0250 | .0016 |
| Entire preview-reader family, even granting all guards/copies free | .0084 | .0250 | .0166 |

Independent nested mean reconciliation checks the partition only; it is not the median gate:
whole family `(2.3200+14.2812+7.5020)/1024=.02353828125` ms;
readers `(7.5020+1.0989)/1024=.00839931640625` ms.
O-to-M is already included in O and never added twice.

Subtracting the measured family per operation gives a fictional zero-cost residual median
.0460 for the ENTIRE engine/resource family, or .0610 for all readers. The former would
remove indispensable work and still fails the .025 design-payoff bar; neither is an
implemented performance result. Instrumentation perturbs execution. Its .0694 median does
not replace the authoritative uninstrumented .0683. No guaranteed net saving is claimed.

**No single coherent, outcome-preserving correction has defensible >=.025 ms payoff.**
This is not a proof that every imaginable optimization is impossible. It is the authorized
bounded decision, and it requires stopping rather than expanding the search.

## Validation and first correction retained

The first correction's [build/test results](../lifecycle-cost-convergence/validation.md)
remain: affected Debug/Release builds and16 focused process gates PASS, all11 fixtures x3
frontiers exact direct/ready records and native bits, existing physics bounds, stale/refusal/
retry/terminal checks, finite-propellant and M15.0 regressions. Contact allocation five
windows0 B; M15.0 seven windows0 B; positive byte[128] control152 B.

| Uninstrumented Release control | Median | P95 | P99 | Max | Result |
|---|---:|---:|---:|---:|---|
| Run1 | .0683 | .0732 | .0963 | .1417 | FAIL median |
| Run2 | -- | -- | -- | -- | Not executed |
| Run3 | -- | -- | -- | -- | Not executed |

Ceilings .050/.100/.250/.500 ms unchanged. No final-correction control campaign was entered.
The final diagnostic reports0 B in every measured operation, GC0/0/0, thread2,1152 exact
history/endpoint comparisons and final native-bit equality. Partition sums reconcile exactly.
Its build passed with zero warnings/errors. No semantic failure occurred in the probe.

First correction retained storage upper bound5,401,088 B <=8 MiB, including history4,857,624 B;
cold setup2.4176 ms. No new production storage was added in this final investigation.
Full solution/suites, remaining powered qualification and visible/manual route remain deferred.
No production-qualified, renderer, FPS or gameplay claim is made.

## Independent verification

Cost reviewer independently reconciled per-operation unions and nested totals: PASS;
**REVISE / INSUFFICIENT PAYOFF**. Lifetime reviewer independently rejected bundling other
mutable-boundary or successor-property responsibilities into the reader claim: same stop.
Current KSA reviewer confirmed exact installation identity and responsibility equivalence,
without attributing unmeasured cost. No reviewer modified production or ran extra tests.

## Source, evidence, cleanup and next action

First correction changes six production paths: OrdinaryContactInputProjection,
PoweredContactPreparation, LocalContactWorld.Powered, LocalContactWorld.Publication,
SimulationTransactionEngine.PoweredFreeFlight and .Propellant. Test changes: Program,
PoweredContactTests.Qualification and new .Lifecycle. The final investigation changes NO
production or permanent-test bytes. Exact resource arithmetic, force/projection formulas,
BEPU timestep/8-iteration/1-substep policy, geometry, state/history commits and failure rules
remain intact. No Candidate-A, load correction, event-sized solve or source-mass change.

Retained: direct KSA/history, operation/lifetime/check matrices, first correction delta and
validation, current attribution summary, probe/reproduction instructions, hashes/restoration,
reviewer findings and exact cleanup inventory. Bulk build trees/raw logs are disposable;
see [cleanup](cleanup.md) and [reproduction](reproduce.md).

**Final judgment: REVISE / INSUFFICIENT PAYOFF.**
**PRODUCTION POWERED CONTACT — FOLLOW-UP REQUIRED.**
UNBANKED. STOP FOR PROJECT CONTROL. No correction #2, no further optimization or qualification.