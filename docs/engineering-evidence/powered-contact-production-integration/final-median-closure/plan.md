# Final bounded median-closure investigation

Baseline is the accepted first ready/apply correction, UNBANKED on
codex/powered-contact-production-integration. HEAD/main/origin/main remain
49057fecceb0f725d5f551ec40e2780971b0d81d. Its focused correctness/lifetime/refusal/retry,
numerical equivalence and exact-zero allocation passed. First uninstrumented Release control
median .0683 fails .050; P95 .0732, P99 .0963, max .1417 pass. No runs 2/3 occurred.

The final steer allows one new causal/cost decision and only one correction if a single
coherent responsibility provides >=.025 ms defensible avoidable/repositionable MEDIAN work.
No mean substitution, sum of medians, arbitrary bucket shares or optimization bundle.

## Source-first finding and measurement need

Current KSA identity was freshly rehashed unchanged. The directly inspected staged property
and module buffers, dependency-aware no-change preparation and ready/apply remain applicable.
The actual live-history reads recorded in ../lifecycle-cost-convergence/ksa-lifecycle.md
remain the history evidence. No fresh broad archaeology is needed.

The strongest candidate is consumption of existing sealed engine/resource previews within
the uninterrupted ready owner lifetime. Six engine readers still recapture source twice;
four resource readers recursively consume them. General/exposed preview and final stale
checks are valid and cannot be globally removed. Different responsibilities (host final
checks, no-change successor mass, solver, input projection) are not added to this candidate.

Old entire engine/resource bucket median was .0231 ms. It is already below .025 and includes
required work. However the implemented correction changed execution/codegen and the old
distribution is not a measurement of the corrected operation. One minimal current attribution
run will test dominance and the largest possible payoff of this family before further design.

## Predeclared diagnostic

Exactly ONE fresh Release process; no profiler or runtime/config changes. Same
UNPOWERED STILL-FUELLED RETAINED-CONTACT CONTROL, CenteredBaseline, OFF,
fuel .000013020833333333334 kg. Same prime128, warm128, measured1024 complete operations.
The existing observer is reused, adding only three category labels to separate engine reads,
resource preparation and resource reads from the old engine/resource category. Per-operation
parent-edge times are retained in memory for whole-family medians and then summarized.
No raw sample dump or extra process campaign.

Report complete/exclusive/inclusive measurements and exact operation-wise whole-family sums.
Both all-preview and ALL engine/resource time are upper bounds, not safe savings: real
preparation and final guards remain required. Required payoff must stand without their removal.
Counterfactual residual is computed per operation before taking its median, not by subtracting
independent medians. Instrumentation perturbation is a limitation; do not call its absolute
distribution a second uninstrumented performance qualification or retry-to-green.

One observer-disabled reference in that same process verifies all1152 history/endpoint records
and final native bits against observed execution. Same thread, exact partition sums, allocation
and GC changes are reported. No physics or callback change. Exact preimages are captured before
transactional instrumentation and restored before interpretation. Any technical/semantic probe
failure stops for review; no automatic rerun.

Scratch is only E:\NovaCore\build\powered-contact-final-median-closure. Preserve the older
performance-revision and lifecycle-correction trees. Do not retry their previous cleanup.
The retained probe.py derives from the existing observer; no broad decompile or source tree.
