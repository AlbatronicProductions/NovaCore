# Hosted consumption final qualification — dispatch blocked

Lead judgment: **ESCALATE TO PROJECT CONTROL**.

**VERIFIED BEPU BINARY CONSUMPTION — FOLLOW-UP REQUIRED**. UNBANKED.

## Baseline and candidate identity

Branch: `codex/bepu-local-contact-staging`.
HEAD/main/origin/main remain `3933d832b73511a8d08468b7b27a9b2820e2f8ee`.
No staged files. All previously recorded dependency artifact hashes match.
Seven build/project/test inputs match the final validated disposable export.
The original 501-file comparison still changes only Simulation's project file;
existing production and permanent test implementations are unchanged. No solver
implementation has begun. Runtime frame work added remains ZERO.

## Hosted CI run, environment, identities and clean checkout

**NOT STARTED.** No run ID/URL, tested hosted ref, runner image, hosted output
identity, elapsed workflow time or clean-checkout PASS exists for consumption.

Read-only GitHub REST inventory shows only remote branch `main`, at the baseline
above. Actions lists only workflow 356900973, `BEPU independent signature
qualification`, path `.github/workflows/bepu-signature-qualification.yml`.
The prepared `.github/workflows/verified-bepu.yml` is untracked locally, as are
the new dependency files and smoke test. HEAD contains no workflow files.

The prepared workflow checks out a repository ref; there is no published ref
containing this candidate. The current ticket explicitly forbids commit and push.
Consequently it cannot execute the requested candidate checkout under the granted
Git boundary. No unrelated workflow was dispatched, and no workaround upload,
commit, push, new workflow architecture or rescue loop was attempted.

This is a pre-dispatch availability/authorization blocker, not a hosted build
failure or an observed architecture contradiction. The earlier independent
signature qualification does not substitute for hosted consumption qualification.

Project Control must provide a hosted candidate ref or separately authorize
publishing a qualification ref and workflow. Main need not be promoted, but no
such publication is authorized in this ticket. Existing local passes remain valid;
none were rerun.

## Cleanup

**CLEANUP REQUIRES MANUAL PROJECT CONTROL EXECUTION**.

All three approved paths were resolved and inspected as generated validation
scratch inside the specified directory. No reparse points were found. Durable
reports, hashes and reproduction scripts remain outside them. Current counts:

| Directory suffix | Files | Bytes |
|---|---:|---:|
| 20260913-045054-162 | 499 | 59,133,941 |
| 20260913-045157-600 | 520 | 65,007,035 |
| 20260913-045232-566 | 848 | 90,516,270 |
| Total | 1,867 | 214,657,246 |

The newly authorized exact-path deletion was rejected before execution by
automatic approval review as "blocked by policy". No retry or alternate deletion
mechanism was used. All three paths still exist; disposed bytes/files: zero.

```powershell
Remove-Item -LiteralPath `
  'E:\NovaCore\build\bepu-binary-validation\20260913-045054-162', `
  'E:\NovaCore\build\bepu-binary-validation\20260913-045157-600', `
  'E:\NovaCore\build\bepu-binary-validation\20260913-045232-566' `
  -Recurse -Force
```

## Durable evidence and diff

The previous `verified-binary-consumption.md`, `verified-binary-results.json`,
production manifest, exact archives/DLLs, license and reproduction instructions
remain intact. `hosted-consumption-preflight.json` records current candidate
hash comparisons, remote inventory, scratch inventory and candidate paths.

Tracked candidate diff: `.gitattributes` +5; `NovaCore.sln` +6;
Simulation project +8/-1. Total: 3 files, 19 insertions, 1 deletion.
Untracked candidate areas remain the workflow, root targets, `external/bepu`,
dependency smoke test, reproduction scripts and engineering evidence. This pass
adds only two concise evidence files and an index link. No implementation changed.
Git diff check passes; nothing committed, pushed, merged, tagged or banked.

Stop for Project Control. Hosted CI remains the acceptance blocker; policy-blocked
scratch cleanup is independently pending manual execution.
