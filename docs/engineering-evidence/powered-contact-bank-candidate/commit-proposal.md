# Commit and future bank sequence — NOT EXECUTED

Subject to explicit Project Control milestone/content approval, proposed message:

```text
NovaCore M15.1: Bank finite-fuel retained powered contact

Join exact finite-resource and actual-actuator successors with ordinary retained
contact endpoints under NovaCore atomic publication. Preserve source-mass solving,
ready/apply authority, same-body acknowledgement and supported dry continuation.

Retain integrated Debug/Release, allocation, storage, determinism and manual
qualification, including historical performance misses and unattributed frame tails.
Record the Apache-2.0 attribution and subsequent whitespace-only identity migrations.

Reconcile current/public documentation and ownership/licensing status. Root-license
and companion drafts remain withheld; no departure or Florida integration is added.
```

Recommendation only; M15.1 has not been assigned. Exact membership is bank-paths.json.
Before any stage, Project Control chooses Path A/B, approves the milestone and
legal-file exclusions, completes cleanup, and reviews candidate-status labels for
the impending authorized bank. Qualified source bytes must match the successor
manifest; no silent comment/line-ending/token migration is permitted.

After that authorization only, the intended sequence is:

1. Recheck main/remote main equal `49057fecceb0f725d5f551ec40e2780971b0d81d` and all
   historical tags unchanged; stop if either advanced.
2. Stage only approved exact bank-paths.json members using literal arguments.
   Review `git diff --cached --name-only`, the complete staged diff and
   `git diff --cached --check`. The prospective alternate-index check recorded in
   whitespace-result.json does not replace this future real staged-content check.
3. Recheck source/test successor identity, legal withholding and documentation
   status. Stop on any mismatch or unexpected file. Use the exact message above
   only after milestone approval, then create the commit on the candidate branch.
4. Inspect `git show --stat HEAD`, the committed tree and parent; ensure no withheld
   legal draft, disposable output or unrelated work entered it.
5. Set the approved bank commit ID from `git rev-parse HEAD`. Create the annotated
   tag specified in tag-proposal.md at that exact commit, only when authorized.
6. Switch to main and fast-forward only to that verified bank commit. Never merge
   divergent main or move historical tags. Preserve the untracked withheld drafts.
7. Push main with `git -c push.followTags=false push origin main:main`; separately
   push only `refs/tags/m15.1-finite-fuel-retained-powered-contact` if authorized.
8. Verify `git ls-remote origin refs/heads/main` and the exact annotated tag plus
   peeled commit, then verify local refs/worktree and remaining withheld drafts.

No command in this future sequence was executed. There is no push-all-tags step.
The engineer performing the authorized bank must stop if review changes the
allowlist or candidate identity, rather than assuming this proposal grants approval.
