# Post-impact singleton coverage — predeclared qualification

Baseline: dc274639f670d122764bb0e8f3d0c95ae201c7a4, banked M14.15.
This candidate adds read-only coverage for the complete singleton definition
already admitted by the checked Florida chain. No publication or response at beta.

## Proof and limits (declared before qualification)

Use exact dyadic sigma partitions of [0,1], with t=alpha+sigma*(T-alpha).
Certified initial departure plus positive interval bounds establishes clear.
A clear prefix and strict positive/negative endpoint signs with uniformly
negative derivative establish the first approaching root. Tangency, domain loss,
ambiguous departure and exhaustion refuse. Numerical uncertainty is not slop.

Maximum 255 interval visits: a bounded proof budget equivalent to one complete
seven-level binary tree, also allowing deeper selective searches. Maximum depth
24 follows the existing root-refinement ceiling and keeps all dyadic coordinates
exactly representable. A left-first depth-first stack needs at most 25 intervals;
no heap work queue. Root refinement/comparison is limited to 24 steps. Limits
are work/refusal policy, not a promised resolution or contact tolerance.

Review independent trajectory equivalence, Earth rotation jets, grading guards,
departure and receipt ownership before executing tests. Analytical controls:
clear linear/quadratic departure; immediate penetration; approaching second root;
rotating inelastic reacquisition; multiple roots; grazing; near-alpha/near-T and
exact-T roots; domain escape; exhaustion; asymmetric/noncommuting spin; stale,
foreign and subset admission. All must prohibit false clear.

Timing: 32 warmups, 101 ordinary-runtime samples, sorted indices 50/95/99/100.
Separate checked no-GC allocation window of eight warmed calls and existing
positive control. Setup allocation is reported separately. No timing threshold
or whole-frame claim is invented. Report intervals and rotation/Earth evaluations.

Validation order: focused analytical/helper; full Debug/Release builds; three
fresh focused processes per configuration; one full Simulation per configuration;
ReferenceFrames/Precision; predecessor and real Florida/query/stale integration;
diff check. Stop on first material failure. If all representative real Florida
coverage refuses because of inherited numerical bounds, stop for Project Control.

Keep banked source files unchanged where possible; new additive production only.
Retain concise results and identity; do not touch closed diagnostics. No staging,
commit, tag, merge, push, bank or milestone number.
