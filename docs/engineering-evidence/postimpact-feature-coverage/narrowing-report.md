# Narrow post-impact terrain-contact clearance contract

**POST-IMPACT TERRAIN-CONTACT CLEARANCE CANDIDATE — READY FOR PROJECT CONTROL ACCEPTANCE**

**Lead judgment: PASS. Candidate UNBANKED.**

Proposed accomplishment: **Certify post-impact terrain-contact clearance for the admitted authored point**.
No milestone number. Stop for Project Control.

## Baseline and decision

Branch codex/postimpact-feature-coverage. HEAD/main/origin/main remain
dc274639f670d122764bb0e8f3d0c95ae201c7a4. Project Control selected NARROW CONTRACT
in “Narrow Production Contract to Qualified Clearance + Conservative Refusal.”

The source-feasibility assessment remains E — MIXED / INSUFFICIENT. It did not
prove physical impossibility or identify one unique upstream/coverage blocker.
Its exact source witnesses and calculations remain retained unchanged. No fourth
fixture, upstream change, new proof correction, tolerance, solver or Bepu work.

## KEEP / RETIRE map

| Type/member/responsibility | Disposition | Reason |
|---|---|---|
| FloridaPostImpactMotion, BodyFixedJet, CoverageDomain, rotation/translation | KEEP unchanged | Same M14.15 trajectory and supported terrain relation |
| Entire FloridaPostImpactGap, cached root anchor, interval intersection | KEEP byte-identical | Accepted Coast proof and conservative same-function inclusion |
| IPostImpactCoverageGap, CoverageGap, search nodes, monotonic/positive predicates | KEEP | Sound clearance and no-false-clear refusal |
| Strict crossing condition and chronological prefix/traversal | KEEP | Detect contact evidence before issuing any full-clear result |
| CoverageValues, CoverageEvidence, owner Check/TryCreate/Enclose | KEEP | Checked clearance identity and numerical reads without authority |
| NextEventCertified outcome | RETIRE | No qualified production beta integration |
| NextRoot constructor/TryCreate/Read/Refine/Compare | RETIRE | Beta-only capability and ordering/refinement authority |
| CoverageRootValues and CoverageEvidence.Owner accessor | RETIRE | Only beta APIs consumed them |
| CoverageSearchResult.RootSigma/RootDepth | RETIRE | No clearance proof or refusal consumer needs root identity |
| MaximumRefinements=24 and MaximumRootDepth=48 | RETIRE as unused | Beta-only APIs removed; spare work is not reassigned |
| Search MaximumVisits=255, MaximumDepth=24 | KEEP unchanged | Existing clearance work limits |

Contract version increases from 1 to 2 to identify the narrowed checked result.
No predecessor API or production file changed. Exactly four candidate files changed:
PostImpactCoverageSearch.cs, PostImpactCoverageProvider.cs,
PostImpactCoverageTests.cs, PostImpactCoverageProductionTests.cs.
The cost helper and candidate runner/project registrations remain unchanged.

## Final production contract

Outcomes are EventFreeThroughTarget, Unresolved, Unsupported and Stale.
Only EventFreeThroughTarget constructs CoverageEvidence. Refused/default values
cannot be read as coverage. No beta factory, root token, root read, refinement,
comparison or publication/execution capability remains in the candidate.

EventFreeThroughTarget proves no admitted terrain-contact event in (alpha,T]
for the COMPLETE singleton zero-radius authored-point geometry and supported
natural-terrain relation. It is not spacecraft-hull/all-event/multi-feature
clearance, support/rest, or publication permission.

The receipt remains immutable, derived and checked against the issuing provider,
the exact staged M14.15 source, alpha/target, full singleton feature, terrain/model/
frame provenance, source/timeline applicability and version. M14.15 still reports
Unknown event coverage itself. Coverage neither replaces its state nor publishes T.
Any future publication requires separate coverage classes and source preflight.

## Event-free theorem and refusal

No predicate that returns EventFreeThroughTarget was changed. The alpha-only
departure theorem, positive interval gap, endpoint-plus-monotonic positivity,
whole-interval domain/finite checks, left-first traversal and clear-prefix order
remain exactly as before.

The prior strict-crossing predicate remains:
prefix==node.Lower, positive left endpoint, negative right endpoint and a strictly
negative whole-interval derivative. Its only outcome change is now
Unresolved/StrictCrossingDetected with diagnostic work/prefix values.
It never creates a root receipt merely to encode refusal.

Exact target contact, grazing, immediate penetration, persistent gap zero,
ambiguous departure, close roots, grading escape and exhaustion remain NOT CLEAR.
RootUnresolved remains a refusal reason for unresolved potential contact; the
name is not a root capability.

The accepted Coast relation g(alpha+s) in s*(g'(alpha)+s*A/2), intersected with the
same raw interval, remains byte-identical with the terrain guards. No alpha sample,
epsilon, endpoint restart or force sharpening is introduced.

## Real production outcomes

| Case | Debug | Release | Visits | Depth | Gap enclosures |
|---|---|---|---:|---:|---:|
| Coast | EventFreeThroughTarget / None | Same | 63 | 5 | 63 |
| Force -0.2 m/s² | Unresolved / DepartureUnproved | Same | 25 | 24 | 25 |

Tests now demand these exact statuses/reasons/work counts independently, rather
than accepting any useful outcome across the pair. They also repeat Evaluate and
compare complete proof values, check default and foreign receipt rejection,
reject incompatible staged state and multi-feature subset admission, and compare
canonical translation/rotation/mass/revision/history/clock/debt/timeline snapshots.
M14.15 continuation equality is unchanged. No canonical mutation occurs.

The historical Force oracle (tiny departure, then early reacquisition) is retained
as physical explanation, not a production beta receipt or a changed acceptance test.

## Permanent analytical controls

| Control | Result |
|---|---|
| Linear and positive-curvature departure | EventFreeThroughTarget |
| Quadratic later crossing | Unresolved / StrictCrossingDetected |
| Two roots with clear final endpoint | Unresolved / StrictCrossingDetected |
| Separating COM with rotating feature | Unresolved / StrictCrossingDetected |
| Inelastic rotating-feature reacquisition | Unresolved / StrictCrossingDetected |
| Near-alpha contact | Unresolved / DepartureUnproved |
| Near-target crossing | Unresolved / StrictCrossingDetected |
| Event exactly at T, grazing, uncertain close roots | Unresolved / RootUnresolved |
| Immediate penetration, persistent contact, ambiguous departure | Unresolved / DepartureUnproved |
| Interior grading escape | Unresolved / GradingDomain |
| Explicit cumulative visit exhaustion | Unresolved / WorkLimit, 255 visits, depth 8, 495 enclosures |

Exact polynomial roots and rational pre/post-contact signs remain independent
oracles. Independent alternating-series rational Taylor bounds retain the rotating
contact sign witnesses. Removed tests only asserted retired root brackets/authority;
their missed-contact responsibility is migrated to exact refusal/no-false-clear
checks and a clear prefix before the first polynomial contact.

The new visit-exhaustion control uses the same exact quadratic with root .9 and
deliberately wider sound inclusions for intervals wider than 1/256. It reaches the
existing cap before contact. It is an analytical numerical control, not a new real
production fixture or proof-budget change.

## Validation and determinism

| Gate | Debug | Release |
|---|---|---|
| Full solution build | PASS, 0 warnings/errors | PASS, 0 warnings/errors |
| Focused analytical coverage, fresh processes | 3/3 PASS | 3/3 PASS |
| Full Simulation | 47/47 PASS | 47/47 PASS |
| ReferenceFrames | 11/11 PASS | 11/11 PASS |
| Precision | PASS | PASS |
| M14.2–M14.15 focused predecessor selectors | 14/14 PASS | 14/14 PASS |
| Real Coast/Force | PASS | PASS |
| Surface query / stale snapshot | 2/2 PASS | 2/2 PASS |
| Candidate cost/allocation paths | PASS | PASS |

All six focused outputs are identical, including status/reason, prefix bits and
work counts. Real Florida numerical witnesses agree across configurations.
Surface query fingerprint in both:
3e08f86757588127099cbb100d3829b73d5e5f29b1de8d0f22ec83de28534912.

One implementation compile error used a nonexistent test interval Width member;
it was corrected to Upper-Lower before execution. Final builds pass. No failed
runtime test was retried, and no new allocation attribution was performed.
Git diff --check and cached check PASS (existing line-ending notices only).

## Allocation and bounded performance

32 warmup calls, 101 ordinary-runtime timing samples, then 8 equivalent calls
under the existing checked no-GC allocation boundary. All 11 reusable paths
allocate exactly zero in both configurations. Entry/exit succeed; positive
byte[128] controls detect 152 bytes in both analytical and real cost processes.

Owner admission allocates 122,240 bytes over 8 calls = 15,280 bytes per setup,
unchanged from prior qualification. It is not declared allocation-free.
Delegates/fixtures/sample arrays and output formatting are outside measurement.

Release CPU measurements, microseconds per call; nearest-rank percentiles:

| Path | Median µs | P95 µs | P99 µs | Maximum µs | Bytes/call |
|---|---:|---:|---:|---:|---:|
| analytical clear certification | 0.1 | 0.2 | 0.2 | 0.2 | 0 |
| analytical crossing refusal | 1.9 | 1.9 | 2 | 2 | 0 |
| depth exhaustion | 2 | 2 | 2 | 2 | 0 |
| visit exhaustion | 93.7 | 99.5 | 107.2 | 128.4 | 0 |
| domain refusal | 1.3 | 1.3 | 1.3 | 1.3 | 0 |
| coverage owner admission | 238.9 | 265.4 | 271.9 | 292.3 | 15280 setup |
| reusable interval | 142.5 | 164.5 | 170.5 | 216 | 0 |
| real Coast clear certification | 1208 | 1335.8 | 1989 | 2013.2 | 0 |
| checked coverage read | 2.8 | 2.9 | 2.9 | 2.9 | 0 |
| foreign authority refusal | 0.2 | 0.3 | 0.3 | 0.3 | 0 |
| invalid numerical interval refusal | 2.9 | 2.9 | 27.2 | 60.9 | 0 |
| real Force departure refusal | 468.6 | 532.5 | 573.7 | 624.4 | 0 |

The full Debug/Release records are in narrowing-validation.json. Timing is
ordinary-runtime and reflects tier/runtime variation; no causal speedup claim is
made from differences against older sessions. These are not whole-frame,
sustained-gameplay, fleet-scaling or high-warp guarantees.

Search uses 600 bytes of node-stack payload, excluding caller/runtime frames.
Measured value sizes: CoverageSearchResult 32 bytes, CoverageEvidence 40 bytes,
CoverageValues 152 bytes. No next-root value exists.

Source-derived per valid real gap call: one torque-free rotational enclosure,
one Earth/body-fixed trajectory evaluation, one terrain-domain check, and zero
terrain point queries. Thus Coast search uses 63 each; Force uses 25 each.
Owner initialization performs two trajectory/rotation/Earth evaluations.
These operation counts follow the evaluated call path and proof.Evaluations;
they are not hardware performance counters. Upstream checked-read preflight is
included in measured calls. Invalid intervals/stale sources exit earlier.

Analytical crossing: 8 visits/depth 5/10 enclosures; depth exhaustion: 25/24/25;
grading refusal: 27/24/27; visit exhaustion: 255/8/495. Analytical models perform
no actual Earth or rotation evaluation; trigonometric fixtures are test oracles.

## Independent red team

PASS. The reviewer checked:
- no new path to false clearance;
- unchanged strict-crossing and clearance predicates;
- retained rotating-feature detection;
- complete singleton admission rather than subset coverage;
- no authority broadening or hidden publication;
- no required clearance helper removed;
- exact anchor and entire motion file unchanged;
- no solver/Bepu scope or budget widening.

The strongest risk was accidentally deleting crossing detection with root
authority. Static inspection and permanent polynomial/rotating refusal controls
both cover that distinction. Default/foreign/incompatible receipt reads are
explicitly rejected. Independent source review does not replace the executed gates.

BEPU GATE NOT REACHED.

## Reproduction and evidence

Use the commands in README.md and narrowing-validation.json. For each Debug/Release
build, run focused coverage three times in fresh processes, the full Simulation,
ReferenceFrames and Precision binaries, and each --<flag>-only predecessor selector
listed in the JSON. Graphics --case selections are the real coverage, cost, physical
surface-point query and stale-snapshot registrations. Normal runtime; no profiler.

Retain the source-feasibility assessment and exact witnesses; the narrowing
before/after identity, current contract/report, compact validation and cost records.
No raw stdout, new fixture search, diagnostic source copy or separate build tree
was retained by this narrowing. Normal bin/obj outputs remain rebuildable.

Prior reviewed scratch remains 274 files / 7,905,537 bytes:
- E:\NovaCore\.codex\coverage-comparison-control: 28 files / 441,530 bytes.
- E:\NovaCore\.codex\coverage-causal: 244 files / 7,418,130 bytes.
- E:\NovaCore\.codex\source-feasibility: 2 files / 45,877 bytes.

Deletion was previously rejected by automatic approval review as blocked by policy.
No retry/workaround in this ticket. New diagnostic scratch: zero. Removed scratch:
zero. After review, the exact manual cleanup remains:

```powershell
Remove-Item -LiteralPath 'E:\NovaCore\.codex\coverage-comparison-control', 'E:\NovaCore\.codex\coverage-causal', 'E:\NovaCore\.codex\source-feasibility' -Recurse -Force
```

## Git boundary and future work

Final identity checks all 241 banked production files and 55 tags unchanged.
FloridaPostImpactMotion and the exact FloridaPostImpactGap anchor match their
accepted fingerprints. Only the four authorized candidate code/test files
changed, plus this concise evidence. Nothing staged, committed, tagged, merged,
pushed or banked.

Future beta issuance remains separate: it must qualify a robust real source path
and re-establish checked authority/read/identity/refinement/ordering and positive
integration. No dormant beta authority is left in the narrowed candidate.
Canonical publication remains unimplemented.

**PASS — return UNBANKED for Project Control acceptance.**
