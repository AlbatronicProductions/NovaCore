# Post-impact terrain-contact clearance — UNBANKED

Proposed accomplishment: **Certify post-impact terrain-contact clearance for the admitted authored point**.

Project Control selected NARROW CONTRACT. The candidate now issues checked
`EventFreeThroughTarget` evidence or returns conservative refusal. It does not
issue an authoritative next-event root. No milestone is assigned.

- Baseline HEAD/main/origin/main: `dc274639f670d122764bb0e8f3d0c95ae201c7a4`.
- Branch: `codex/postimpact-feature-coverage`.
- [Current narrowing report](narrowing-report.md).
- [Current validation and costs](narrowing-validation.json).
- [Pre-narrowing identity](narrowing-before.json) and [final identity](narrowing-identity.json).

## Checked contract

For the COMPLETE singleton zero-radius authored point and the supported natural
terrain relation, clearance proves no admitted terrain-contact event in (alpha,T].
This is not whole-hull, all-event or multi-feature clearance. It is not support,
rest, publication permission or event execution permission.

Outcomes: EventFreeThroughTarget, Unresolved, Unsupported, Stale.
Only EventFreeThroughTarget creates a checked immutable receipt, bound to its
issuing provider, checked M14.15 trajectory, alpha, target, complete geometry,
feature, terrain/model/frame identity, source/timeline applicability and contract
version. Numerical enclosure success alone cannot mint this receipt.

The same M14.15 translation and torque-free rotation are consumed without choosing
alpha, replacing endpoint bits, recomputing response or mutating canonical state.
The exact accepted root-correlated gap intersection and terrain guards remain intact.

## Clearance and refusal

Search remains chronological, with at most 255 visits, depth 24 and 600 bytes of
node-stack payload. Only the inherited-alpha node uses the qualified departure
theorem. Later nodes require positive gap or a valid monotonic positivity proof.
A strict approaching crossing now returns Unresolved/StrictCrossingDetected.
Tangency, possible contact at T, ambiguous departure, domain loss and exhausted
work refuse; no possible-contact case creates root authority.

Beta-only receipt/read/refine/compare APIs and root payload are retired. Their
24-step refinement and 48-total-root-depth limits have no remaining consumer;
they were removed, not reassigned to clearance search. Certificate version is 2.

## Current qualification

- Full solution Debug/Release: PASS, zero warnings/errors.
- Full Simulation: 47/47 groups in both configurations.
- Three fresh focused processes per configuration: PASS, identical proof records.
- ReferenceFrames: 11/11 per configuration; Precision PASS.
- Fourteen focused predecessor selections per configuration: PASS.
- Real Florida Coast: EventFreeThroughTarget/None, 63 visits, depth 5, 63 enclosures.
- Real Florida Force -0.2 m/s²: Unresolved/DepartureUnproved, 25 visits, depth 24.
- Surface query and stale-snapshot regressions: PASS in both configurations.
- Eleven reusable cost/allocation paths: exactly zero bytes in each configuration.
- Shared positive controls detect byte[128] as 152 counter bytes.
- Independent authority/no-false-clear review: PASS.

The analytical controls retain exact polynomial or independent rational Taylor
contact oracles. Former root-issuance expectations now demand refusal. They cover
rotating reacquisition, clear endpoints surrounding contact, multiple/close roots,
tangency, target/near-alpha boundaries, persistent contact, penetration, grading
escape and explicit 255-visit exhaustion. They do not manufacture real receipts.

## Reproduction

Build `NovaCore.sln` in Debug and Release. With normal runtime settings:

```powershell
dotnet tests/NovaCore.Simulation.Tests/bin/Debug/net10.0/NovaCore.Simulation.Tests.dll --postimpact-coverage-only
dotnet tests/NovaCore.Graphics.Tests/bin/Debug/net10.0/NovaCore.Graphics.Tests.dll '--case=Post-impact Florida terrain-contact coverage'
dotnet tests/NovaCore.Simulation.Tests/bin/Release/net10.0/NovaCore.Simulation.Tests.dll --postimpact-coverage-cost
dotnet tests/NovaCore.Graphics.Tests/bin/Release/net10.0/NovaCore.Graphics.Tests.dll '--case=Post-impact Florida coverage costs'
```

Repeat focused qualification three times in fresh processes per configuration.
Run full Simulation, ReferenceFrames, Precision and the focused selectors listed
in narrowing-validation.json. The cost helper separates normal-runtime timing
from checked allocation measurement; setup allocation is not a reusable-path claim.

## Continuing evidence

The [source-feasibility assessment](source-feasibility-report.md) explains the
decision: E — MIXED / INSUFFICIENT. It resolved the two original source failures
without proving a robust positive-beta fixture through the full predecessor chain.
It did not establish physical impossibility or a unique upstream/coverage blocker.
Beta/root issuance is FUTURE work requiring its own authority, checked read,
identity, refinement, ordering and positive real integration qualification.

The [causal report](causal-report.md), exact Coast/Force inputs and interval records,
and natural/physical oracle scripts retain the proof of the accepted Coast
correction and conservative Force outcome. The source-feasibility diagnostic
source and finite scalar study reproduce the bounded assessment; no fourth
production fixture was attempted.

Earlier plan/validation/comparison/positive-beta artifacts describe superseded
candidate states. They are historical evidence, not current production APIs or
current qualification recipes. In particular the old comparison extractor refers
to retired NextRoot code and does not run against the narrowed source. No runtime
or permanent test depends on these historical files.

## Boundaries

BEPU GATE NOT REACHED. No collision framework, friction, manifolds, support/rest,
persistent constraints, response execution or canonical publication is added.
The candidate remains UNBANKED for Project Control acceptance.

Prior reviewed scratch remains policy-blocked; no deletion retry or workaround.
Exact paths, counts and the manual cleanup command are in narrowing-report.md.
