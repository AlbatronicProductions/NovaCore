# Candidate registration / module / first-use discriminator

**D — CANDIDATE MODULE / PROCESS-LAYOUT ASSOCIATION WITHOUT OWNER**

Candidate causality: **NOT ESTABLISHED**. Lead judgment:
**ESCALATE TO PROJECT CONTROL**.

**PAIRED PRIVATE PROPAGATION CANDIDATE — FOLLOW-UP REQUIRED**

The normal registration row is unnecessary for the observed SAS witness: C-3
allocated one 12,336-byte object with that row removed and the candidate group
never executed. C still contains the unused existing selector and candidate
assemblies. This is an association, not proof of an allocating production owner
or of complete absence of candidate reachability. The three marked C runs did not
localize a preceding first-use transition. The authorized process budget is exhausted.

## Baseline / candidate identity

Branch `codex/root-linked-private-propagation`; HEAD/main/origin/main and
`m14.14-private-root-postimpact-state^{}`:
`f64dc07f23a0a765b9b07dd49b895a8f3cb5ebfe`.
Annotated tag object: `412084a0acda2fa96c4be166e1f9f29020ffc577`.
SDK 10.0.303; every child reports .NET 10.0.12. Same relative runner path from
each disposable checkout root; no selector arguments.

Before/after: all **315 source/project** and **six canonical binary** hashes match;
all **54 tag refs/objects/targets** match. Sorted path + SHA256 source manifest
(LF join, no final LF):
`599CCDF9B4ADA20B964100B4DD43C5230C83000E54EDC389BF07A92108A77B92`.
Individual candidate identities remain in [identity.json](identity.json).
The [ledger](registration-discriminator.json) retains binary/source identities,
observer identity, timestamps, PID/thread identities, stdout hashes and witnesses.

## Static reachability map

Normal Program registration, final tuple/Action row
-> `PrivateCanonicalPropagationTests.Run`
-> Translation / Majorants / Rotation / NoncommutingAttitude / RealizationAndRefusals
-> `PrivatePropagationMath`, enclosure/receipt types, existing Florida arithmetic.

The test body uses BigInteger oracle arithmetic, local arrays/closures and
reporting when called. It is last, after SAS; no source call into that body occurs
before SAS on the normal route. The separate Graphics linked helper is not a
Simulation startup dependency. No new shared mutable static state or earlier
test-helper consumer was found.

Read-only PE metadata inspection of retained canonical binaries found no
`.cctor` or nonliteral static fields on the new production types, candidate test
class, Florida provider/Proof/private receipt, or either `<Module>`. Compiler
generated Program delegate caches exist. Registration changes the startup
tuple/delegate collection; creating its Action cannot run a nonexistent
candidate static constructor. It can affect resolution/layout/scheduling.

Existing `CertifiedPostImpactVelocityMath.RuntimeVersionSupported =
CheckRuntimeVersion()` reads Environment.Version. That initializer is banked
and unchanged. Candidate Evaluate can consume it when called; no link from its
first use to this SAS witness was established. Existing FloridaBound.Pi is also
banked. No candidate-owned immutable initializer was discovered.

Source anchors: Program.cs selector at 18 and normal registration at 102;
PrivateCanonicalPropagationTests.Run at 14; PrivateCanonicalPropagation.cs
math/receipt definitions; CertifiedPostImpactVelocityMath.cs runtime check at 20.
These are inspection facts, not allocation-stack attribution.

## Variants and comparison quality

| Variant | Definition | Budget |
|---|---|---:|
| A | Banked M14.14, 45 normal groups, no candidate source; common observer | 3 |
| B | Complete candidate source, 46 normal groups; common observer | 3 |
| C | B with only final candidate registration row removed; both candidate files and unused selector remain | 5 |
| D | Not constructed: no candidate initializer to isolate and no reviewed honest inert permanent replacement | 0 |
| CM | C plus passive assembly-presence snapshots before registration and immediately before SAS | 3 |

A/B/C were separately built before the matrix, once each, Release with zero
warnings/errors. C source differs from B only by that one registration row;
legacy ordering, measured workloads, warmups, no-GC boundaries and thresholds
are unchanged. Physical line endings differ for some banked checkout files;
normalized source comparisons contain only the candidate additions/wiring.

Different diagnostic executables necessarily have different IL/layout/build
identities; no PE identity is claimed between A/B/C. CM was built once only
after C completed. Its three production dependency DLL hashes equal C's.
No rebuild occurred between runs of a variant; no retries or acceptance runs.

## Observer validity and limitations

Reused qualified global permanent-gate observer:
`E:/NovaCore/.codex/sas-allocation-classification/observer/profiler.dll`,
SHA256 `8B86C6B14BFF5E35FCE4873C17A9B3A0620C9BB30BDD60A211CA60216A755E31`.

Its existing CLR registration is child-process-only and identical in all arms.
No other profiler/tiering/PGO/GC environment changes. Same observer adapter and
five disposable test-file hook plan. It observes actual ObjectAllocated callbacks
for the owning OS thread, bounded stack/context records, and per-thread counters.
All reached completed regions, including the failing gates, entered/exited no-GC
successfully. No scope abort/overflow invalidated a witness.

**71/71 deliberate controls qualified**: each reported 152 counter bytes,
one object and 152 object bytes. Managed thread was 2; OS thread and context
identity agree within each witness. Counts per process are in the ledger.

The native callback scope is slightly wider than the allocation-counter window.
Existing small callbacks can therefore be observed with a zero counter:
speed-presets 3 objects/172 bytes; body-catalog 1 object/108 bytes. They are not
additional counter-gate failures. Reporting occurs after each measured window
and may perturb subsequent compilation/scheduling; these are attribution runs,
not acceptance or timing measurements.

## Executed run matrix

Groups are pass / fail / unreached. SAS object column is count / bytes.
All reached SAS and failing speed gates had no-GC entry/exit PASS.

| Run | SAS counter | SAS object | Registered? | Candidate executed? | Groups P/F/U | First failure | Exit |
|---|---:|---|---|---|---|---|---:|
| A-1 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |
| B-1 | 0 | 0 / 0 | YES | YES | 46/0/0 | none | 0 |
| C-1 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |
| A-2 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |
| B-2 | 0 | 0 / 0 | YES | YES | 46/0/0 | none | 0 |
| C-2 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |
| A-3 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |
| B-3 | UNREACHED | UNREACHED | YES | NO | 3/1/42 | Solar UTC and speed presets | -532462766 |
| C-3 | 12336 | 1 / 12336 | NO | NO | 31/1/13 | SAS sign/frame continuity proof | -532462766 |
| C-4 | UNREACHED | UNREACHED | NO | NO | 3/1/41 | Solar UTC and speed presets | -532462766 |
| C-5 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |
| CM-1 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |
| CM-2 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |
| CM-3 | 0 | 0 / 0 | NO | NO | 45/0/0 | none | 0 |

A: 3/3 clean. B: 2/3 clean, one speed failure. C: 3/5 clean, one SAS failure
and one speed failure. CM: 3/3 clean. Total 14 processes, 11 pass, 3 fail.
The full candidate's SAS was zero twice and unreached once in this matrix.
Its earlier 3/5 real-object SAS failures remain a separate
[banked differential](banked-global-differential.md); they are not reruns here.

## SAS witness and arithmetic

C-3, PID 41852, OS thread 13664: 12,336 counter bytes exactly match one
12,336-byte managed object. No candidate group executed or was registered.
Resolved frames include SAS proof evaluation and Program.Main; no candidate
propagation frame was resolved. Object type and allocating owner: **UNAVAILABLE**.

| Accounting field | Before | After |
|---|---:|---:|
| Cumulative ordinary allocation bytes | 10,471,064 | 10,483,400 |
| Cumulative UOH bytes | 46,622,840 | 46,622,840 |
| Unused context bytes | 272 | 272 |

Ordinary cumulative increase = counter increase = observed object bytes = 12,336.
Allocation-context address remained the same. Pointer/limit advanced 12,360;
the extra 24 is not assigned an invented object or ownership.
This is actual managed allocation, not the earlier retirement-only 8,160 effect.

B-3 and C-4 failed speed-presets at 6,192 counter bytes. Each wider callback
scope contains four objects/6,364 bytes: one unresolved 6,192-byte object plus
three System.String objects of 72/36/64 bytes. Those same three small callbacks
also appear in zero-counter scopes. Unused context decreases by exactly 6,192.
This does not establish that speed and SAS share an allocating owner.

## Registration / module / first-use relationship

C-3 disproves the normal candidate registration row and candidate test execution
as necessary conditions for this observed SAS failure. It does not exonerate
all candidate references: C intentionally retains the unused selector and all
candidate code. It also cannot distinguish test-assembly content from production
assembly content or process layout. The banked control remains clean within its
bounded matrix; there is no candidate-owned stack/initializer chain.

CM adds only GetAssemblies snapshots/reporting outside allocation windows, without
typeof(candidate), GetTypes, forced initialization, candidate calls or load-event
subscriptions. All three processes already contain NovaCore.Simulation and
NovaCore.Simulation.Tests before registration and before SAS. SAS is zero, with
zero observed objects, in all three. The snapshot cannot date an earlier load
or identify JIT/type first use. No transition was localized; no runtime-first-use
classification is justified. These passing marked runs are not a fix.

## Verification and payoff

Strongest objection: C retains other reachability and differs in IL/layout;
the observer and markers can change scheduling. It survives only the bounded
claim **module/process-layout association without owner**. Neither candidate
causality nor candidate innocence is proven.

The second objection is the wider native scope. SAS's counter/object/cumulative
agreement and unchanged unused context support actual allocation. Speed's extra
172 bytes are explicitly separated. No hidden allocation tolerance was added.

**NOT ESTABLISHED** candidate causality. No production correction, registration
change or measurement migration is justified. Stop at Project Control for payoff
and disposition; do not automatically extend diagnostics or resume acceptance.
M14 isolated contact remains a bounded specialization; no general solver work,
Bepu convergence decision, milestone assignment or architecture change.

## Restoration, scratch and Git

Canonical candidate was never instrumented. Final comparison confirms 315/315
source/project and 6/6 binary hashes unchanged, all refs/tags unchanged, no staged
paths, same initial Git status. `git diff --check` passed with line-ending warnings.

Restored 29 marked-C source/bin/obj preimages and the five existing instrumented
source files in each A/B/C checkout. All preexisting source/project hashes match
A 196/196, B 198/198, C 198/198. This is not complete directory cleanup:
the added GlobalGateAllocationObserver.cs files remain in A/B/C, and
CandidateModuleSnapshot.cs remains in C, together with disposable builds.

**DISPOSABLE SCRATCH RETAINED DUE TOOLING/POLICY RESTRICTION**.
A removal command was rejected before execution as "blocked by policy."
Zero files were deleted; no retry or alternate destructive method was used.

New scratch: 1,045 files / 71,353,272 bytes at
`E:/NovaCore/.codex/private-propagation-registration-discriminator`.
Prior reviewed roots: private-propagation-real-runner-observer,
private-propagation-global-gate-observer, private-propagation-banked-differential:
157 files / 16,031,753 bytes combined.
Total reviewed remaining: **1,202 files / 87,385,025 bytes**.
Three detached experiment worktrees remain. The reused qualified SAS observer is
outside this cleanup scope. Cleanup retention is a tooling restriction, not a
new technical blocker or permission to rerun.

Only this report, compact ledger, reproduction recipe and this package's README
are added/updated for the task. All prior evidence other than the README index
is byte-identical. Incremental budget 80 KiB; combined package budget 304 KiB.
Raw stdout/build trees remain disposable, not permanent repository evidence.

## Reproduction (not authorization to execute)

Use [registration-discriminator-reproduce.ps1](registration-discriminator-reproduce.ps1)
with PowerShell 7 and this exact canonical identity, after a new bounded Project
Control decision and permitted cleanup of existing scratch. The recipe refuses
overwrite/retries; it recovers the common observer hook plan from the retained
global-gate recipe and validates the retained native observer hash.

1. `-Phase Prepare`: detached A/B/C, complete candidate copy, exactly one C row
   omission, common observer hooks with backups and source identities.
2. `-Phase Build`: build A/B/C once, before any process. Inspect preflight identities.
3. `-Phase Run -State A -Run 1`, then B1,C1,A2,B2,C2,A3,B3,C3,C4,C5.
   Each command launches one fresh child. After each, review GLOBAL_GATE/NATIVE/
   CONTEXT/OBJECT records, positive controls, group status and runtime identity.
   No retry. Stop for invalid scope or an out-of-scope contract/environment change.
4. Only if C reproduces with qualified evidence, `-Phase PrepareMarkers
   -QualifiedCFailureReviewed`, then `-Phase BuildMarkers`, then
   `-Phase Run -State CM -Run 1`, 2, 3. This is one extra dimension.
5. `-Phase Restore` restores backed-up contents non-destructively. Extra
   diagnostic files/worktrees remain for separately permitted cleanup.

Causality depends on observed outcomes, not reproducing a preferred count.
No post-restoration execution. Inspect native observer source provenance through
the retained global-gate report. The native observer must detect the deliberate
byte[128] as 1 object/152 bytes and its matching counter; otherwise invalidate
the process. Successful controls do not eliminate all observer perturbation.

**STOP FOR PROJECT CONTROL.**
