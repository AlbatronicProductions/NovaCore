# Banked-versus-candidate global allocation-observer differential

**B — CANDIDATE-ASSOCIATED REAL MANAGED ALLOCATION PHENOMENON**

Candidate causality: **NOT ESTABLISHED**.

Lead judgment: **ESCALATE TO PROJECT CONTROL**.

Candidate: **PAIRED PRIVATE PROPAGATION CANDIDATE — FOLLOW-UP REQUIRED**.

## Identity and comparison contract

Banked M14.14, HEAD, main and origin/main remained
`f64dc07f23a0a765b9b07dd49b895a8f3cb5ebfe`.
Canonical branch: `codex/root-linked-private-propagation`, unbanked.
Tag `m14.14-private-root-postimpact-state` and all historical tags stayed unchanged.

Two detached sparse worktrees used the same Core/EphemerisFormat/Simulation/test
closure. Baseline was clean banked code. Candidate copied its exact canonical
198-file source/project closure. The 196-file baseline differs only by the new
production file, new test file and Program's two candidate registration lines.
Initial checkout line-ending expansion affected 119 candidate files; copying
canonical bytes resolved candidate identity, and LF-normalized comparison proved
there were no additional source semantics differences.

Each state's full normal registration array was preserved: banked 45 groups,
candidate 46, with candidate propagation last. No application selectors or
arguments were passed. Working directory was each checkout's root.

One Release Simulation build per state passed, each with zero warnings/errors.
No build occurred during the ten-process matrix. SDK 10.0.303; runtime .NET 10.0.12.
All 315 canonical source/project fingerprints and six retained binaries remained
preserved. Exact hashes, dependency closure, source manifest digests, process
timestamps and output hashes are in [the data ledger](banked-global-differential.json).
Different checkout builds naturally have distinct PE identities; canonical
accepted binaries were neither rebuilt nor replaced.

## Observer equivalence and positive controls

The exact retained native observer SHA-256 is
`8B86C6B14BFF5E35FCE4873C17A9B3A0620C9BB30BDD60A211CA60216A755E31`.
Both adapters are
`95FB6C1B12C11DD314E1C0E7BD11986DD6BF07EE5FAFFDD83051DEB62A161B97`.
The same five temporary source hook sets wrapped every normal-runner permanent
zero-required counter and existing positive control. Four instrumented files are
byte-identical across states; Program differs only in its existing candidate
registration. No workload, warmup, threshold, timing boundary or checked no-GC
contract changed. Child-only CORECLR registration was identical. No tiering, PGO
or global GC setting changed.

A full process executes 45 zero-required windows and six existing positive controls.
The three SAS failures reached 33 zero-required windows and three controls, including
the identical diagnostic catch's existing positive control after the stopped group.
All **51 control observations** qualified: byte[128], counter 152, one 152-byte
managed object, correct owner thread, hard no-GC entry/exit PASS.
No observer scope aborted or exhausted its bounded buffer.

Observer scope begins just before the original opening counter and ends just after
its closing counter. Thus callbacks outside the narrower measured interval are
possible. Every process reported the same callback-only observations at speed-presets
(3 objects / 172 bytes) and body-catalog (1 / 108), while both counters stayed zero.
Their location relative to the counter reads is not established by this probe.
They are explicitly **not** baseline nonzero-gate witnesses. Baseline PASS does not
mean zero objects across the entire wider observer scope.

This is an attribution run. Callback/stack/metadata work can perturb runtime
compilation and scheduling; it does not replace ordinary acceptance measurements.

## Paired normal-runner matrix

Predeclared maximum: five fresh processes per state, no retries. Candidate first
qualified its observer before a potentially decisive baseline run, permitting an
immediate stop on a valid baseline witness after both arms had qualified.
Actual order is below. Groups are pass / fail / unreached.

| Sequence | State/run | First nonzero required gate | Counter B | Corresponding objects / B | Candidate group executed | Groups | Result |
|---|---|---|---:|---|---|---|---|
| 1 | candidate 1 | SAS sign/frame continuity proof | 12336 | 1 / 12,336 | no | 31/1/14 | FAIL |
| 2 | banked 1 | none | 0 | 0 / 0 at counter-failing gates | absent | 45/0/0 | PASS |
| 3 | candidate 2 | none | 0 | 0 / 0 at counter-failing gates | yes | 46/0/0 | PASS |
| 4 | banked 2 | none | 0 | 0 / 0 at counter-failing gates | absent | 45/0/0 | PASS |
| 5 | candidate 3 | none | 0 | 0 / 0 at counter-failing gates | yes | 46/0/0 | PASS |
| 6 | banked 3 | none | 0 | 0 / 0 at counter-failing gates | absent | 45/0/0 | PASS |
| 7 | candidate 4 | SAS sign/frame continuity proof | 12336 | 1 / 12,336 | no | 31/1/14 | FAIL |
| 8 | banked 4 | none | 0 | 0 / 0 at counter-failing gates | absent | 45/0/0 | PASS |
| 9 | candidate 5 | SAS sign/frame continuity proof | 12336 | 1 / 12,336 | no | 31/1/14 | FAIL |
| 10 | banked 5 | none | 0 | 0 / 0 at counter-failing gates | absent | 45/0/0 | PASS |

Every reached checked region entered/exited successfully; every run had valid
positive controls. PASS process exit = 0. The three candidate failures exited
-532462766 with the original exact-zero assertion, and stopped before later groups.
No additional process was run after the declared maximum.

## Baseline and candidate witnesses

**Baseline:** no nonzero permanent-gate counter/object witness in five 45/45 passes.
This bounded negative result does not establish that baseline failure is impossible.

**Candidate:** runs 1, 4 and 5 failed SAS sign/frame continuity proof, ordinal 32:
31 groups passed, one failed, 14 were unreached. Each recorded one exact
ObjectAllocated callback for 12,336 bytes, matching the 12,336-byte counter.
No-GC entry/exit passed. Managed thread was 2; OS threads were 16140, 32596, 30176.
Runs 2 and 3 passed 46/46.

All three callback stacks resolved the SAS proof method and Program.Main, followed
by an unresolved native frame. Object type: UNAVAILABLE. Allocating owner: UNKNOWN.
A nearby JIT notification does not establish the allocating responsibility.
No additional type, native-helper, JIT or object investigation was performed.

## Object/counter arithmetic and candidate execution order

For candidate runs 1 and 4, cumulative context bytes changed
10,474,152 → 10,486,488; for run 5, 10,471,128 → 10,483,464.
All increases are 12,336. UOH bytes stayed 46,622,840. Unused context stayed
3,264 / 3,264 / 272 respectively. Context storage and measured thread stayed the same.
One 12,336-byte object therefore matches the counter and cumulative-byte increases.
Pointer and limit each advanced 12,360; the extra 24 pointer bytes are not assigned
as another object.

The candidate's propagation group (46) had **not executed** before any SAS failure.
No candidate propagation code appears in the captured resolved stack. This excludes
that group's preceding execution as the immediate explanation, but not a
candidate-associated module, registration, layout or first-use runtime effect.

## Historical relationship and verification

Keep attitude 12,336, isolated attitude 6,192, speed-presets 6,192 and rigid-lookup
12,336 as separate witnesses with unresolved relationships. The prior SAS
12,336-byte exact-object witness and the current SAS witnesses have matching
observable signatures; common allocating ownership is still unknown.

The SAS real-object evidence is **different** from the earlier retirement-only
8,160-byte mechanism. No relationship among the other amounts follows from size.

Strongest challenge: five clean baseline processes cannot exclude a rarer baseline
failure; observer perturbation and executable layout remain alternative explanations
to candidate production allocation. The bounded association classification survives.
A claim that candidate production caused the allocation does not.

## PAYOFF and stop

The next Project Control decision is whether one bounded candidate registration,
module or first-use trigger split now earns its cost. This ticket does not perform
that split, change tests or production, relax zero, investigate object ownership,
or resume acceptance. ReferenceFrames, Precision, focused predecessor regressions
and remaining integration gates stay paused.

## Restoration, retention and reproduction

Both temporary hook sets were restored byte-for-byte; all 196 baseline and 198
candidate source/project files matched their pre-instrumentation identities.
Temporary adapters were removed. Canonical verification matched all 315 source/project
fingerprints and all six retained binaries; refs/tags and staged state stayed unchanged.
Git diff --check passed (line-ending warnings only).

Both detached worktrees were removed: 616 files / 40,562,953 bytes. New diagnostic
output before retirement was 677 files / 43,766,882 bytes. The remaining new scratch
is 61 files / 3,203,929 bytes; prior reviewed scratch is 96 / 12,827,824 bytes.
Total disposable remaining: **157 files / 16,031,753 bytes** across the three exact
paths in the data ledger. No copied worktree remains. The old qualified observer
dependency stays outside the cleanup scope.

**DISPOSABLE SCRATCH RETAINED DUE TOOLING/POLICY RESTRICTION.** The one authorized
consolidated deletion command was rejected before execution, stating "blocked by
policy." No retry, permission change, rename or alternative mechanism was used.
This is not an engineering blocker.

Retain this report, the compact data ledger and [reproduction recipe](banked-global-differential-reproduce.ps1).
The recipe reuses the previous retained observer/hook plan rather than copying a
profiler or SDK. New phase budget: 64 KiB; combined candidate package budget: 224 KiB.
Bulk callback logs, rebuilt binaries, worktrees and restoration backups are disposable.

Reproduction requires fresh Project Control authorization, the exact preserved
candidate and the retained qualified profiler. The recipe's Prepare phase verifies
the canonical source manifest, creates both detached checkouts and instruments them.
Build runs once per state. Run uses no application arguments and rejects duplicate
state/run labels. Inspect each result before proceeding; a baseline nonzero
counter plus corresponding object with qualified controls is the early-stop condition.
Maximum sequence is candidate 1, banked 1, through candidate 5, banked 5.
Restore verifies source hashes before retirement. Do not rerun this completed
matrix merely to seek a different result.

No staging, commit, tag, merge, push or banking was performed.
