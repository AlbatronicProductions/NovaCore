# Paired private canonical propagation candidate

**PAIRED PRIVATE PROPAGATION CANDIDATE — READY FOR PROJECT CONTROL ACCEPTANCE**

Latest: [final normal acceptance](final-acceptance.md) passed five fresh Release
processes, 46/46 groups each, with exact-zero gates unchanged. Remaining declared
ReferenceFrames, Precision, focused predecessor and Florida/query/stale gates passed
in Debug/Release. Independent final review passed. Candidate remains unbanked.
Historical/intermittent candidate-associated managed allocation witnesses were not
reproduced in this final 5/5 bar. Production causality remains NOT ESTABLISHED;
no cause or defect is claimed fixed. Project Control's residual-uncertainty
disposition applies; further forensics is closed for current candidate acceptance.

Prior: the [registration/module discriminator](registration-discriminator.md)
completed A 3/3 clean, B 2/3 clean (one speed failure), C 3/5 clean (one SAS and
one speed failure), and three clean marked-C processes. C retained all candidate
code with only the normal registration row removed; its SAS witness remained
one 12,336-byte object. No first-use transition or allocating owner localized.
**D — CANDIDATE MODULE / PROCESS-LAYOUT ASSOCIATION WITHOUT OWNER**;
candidate causality **NOT ESTABLISHED**. Canonical fingerprints match.
Acceptance remains paused. Scratch deletion was policy-blocked; backed-up
contents were restored, while added diagnostic files/worktrees remain disposable.

Prior: the [banked/candidate global differential](banked-global-differential.md)
completed five fresh Release processes per state. Banked M14.14 passed 5/5;
candidate passed 2/5 and recorded three SAS failures, each one 12,336-byte
managed object matching its counter. **B — CANDIDATE-ASSOCIATED REAL MANAGED
ALLOCATION PHENOMENON**; candidate causality **NOT ESTABLISHED**. Observer
controls qualified in both states. Acceptance remains paused; no correction.

Prior: the [global permanent-gate observation](global-gate-observer.md) stopped
on run 2 at `sas-proof`: 12,336 counter bytes matched one 12,336-byte object.
Positive controls and scope were valid. Object type/allocating owner remain
unresolved: **H — MIXED / INSUFFICIENT**, candidate causality NOT ESTABLISHED.
Run 3 was not executed. Instrumentation is restored; acceptance remains paused.

Prior: the [bounded real-runner observation](real-runner-observer.md) exhausted
three Release processes: 46/46 PASS, failure at unobserved `rigid-lookup` (12,336
bytes), then 46/46 PASS. Both named target gates measured zero in every run;
positive controls were visible. **H — MIXED / INSUFFICIENT**; candidate causality
NOT ESTABLISHED. Temporary source/build instrumentation is restored. Acceptance
remains paused; no additional observer or correction is authorized.

Prior: the [normal Release acceptance bar](normal-release-acceptance.md) stopped
on run 2 at `speed-presets` (6,192 bytes; entry/exit PASS). Run 1 passed all 46
groups. Runs 3–5 and remaining acceptance were not executed. Cause is unclassified.
The original implementation and differential records below remain preserved.

Original implementation-stage judgment: **ESCALATE TO PROJECT CONTROL**. The new responsibility passed its
focused analytical and real Florida gates, but full Release validation failed an
existing allocation contract. Validation stopped immediately. No subsequent
runtime tests, retries, profiling, measurement migration or production correction
were attempted in that original step. Later authorized follow-ups are linked above.
The candidate remains unbanked.

Follow-up: the separately authorized [bounded attitude differential](attitude-differential.md)
completed with mixed evidence: the original 12,336 did not recur, two isolated
candidate runs reported 6,192, and every prefix run passed. Cause remains unknown;
acceptance has not resumed. This section's original validation record is unchanged.

## Baseline and scope

Started clean `main`; HEAD/main/origin/main remain
`f64dc07f23a0a765b9b07dd49b895a8f3cb5ebfe`, banked M14.14. Implementation predecessor:
`64a4173e59d15ad34f0ac3819bc6aa60addeb559`. Branch:
`codex/root-linked-private-propagation`. All fourteen banked tag objects/targets
match the before-edit inventory in [identity.json](identity.json).

One new Simulation production file implements checked M14.14 receipt consumption,
linked root-to-SourceEnd continuation, and an immutable paired private receipt.
All 233 banked production C# files remain unchanged. Two new test files and three
registration/project edits add coverage; existing workloads stay unchanged.
[Current contract and derivation](../../private-canonical-propagation.md) defines
the math, ownership, arithmetic and scope. No canonical mutation, coverage,
publication, event-local execution or solver work is included.

## Decisive validation stop

| Gate | Debug | Release |
|---|---|---|
| Full solution build | PASS, 0 warnings/errors | PASS, 0 warnings/errors |
| New focused analytical propagation | PASS | PASS |
| New real Florida coast + force propagation | PASS | PASS |
| Complete endpoint/certificate replay | Identical to Release | Identical to Debug |
| Full Simulation | 46 passed, 0 failed | 27 passed, 1 failed, 18 unreached |
| Separate ReferenceFrames | NOT RUN after stop | NOT RUN after stop |
| Separate Precision | NOT RUN after stop | NOT RUN after stop |
| Separate focused M14.6–M14.14 regressions | NOT RUN after stop | NOT RUN after stop |
| `git diff --check` | PASS, line-ending warnings only | Same working tree |

Full Debug already included the registered M14.6–M14.14 groups. The Florida test
uses the real M14.9–M14.14 chain in both configurations. These results do not
silently replace the remaining explicit acceptance gates.

The sole full Release attempt reported:

```text
ORDINARY_ALLOCATION gate=attitude-integration bytes=12336 entry=PASS exit=PASS
Unhandled exception. System.InvalidOperationException: attitude-integration: expected zero managed allocation, actual=12336
at Program.<<Main>$>g__SpacecraftAttitudeIntegrationTests|0_7() ... Program.cs:line 300
at Program.<Main>$(String[] args) ... Program.cs:line 104
```

This is an **exact-zero allocation-gate failure, cause unclassified**. The
assertion is independent, follows the original 100,000-iteration loop and uses
the existing checked no-GC helper. Entry/exit passed. Objects, allocating call
stack and runtime accounting attribution are unavailable. No baseline rerun or
recurrence claim was made.

The complete failing method matches banked HEAD after line-ending normalization;
its normalized SHA256 is recorded in identity.json. The Simulation runner adds
only one selector and one final group. The new group was unreached when Release
failed. These facts do not exclude indirect assembly/code-generation/process-state
effects and do not prove a pre-existing defect or a CLR cause. The previous known
allocation investigations are not reused as causal attribution.

Initial development compilation corrected two new test call signatures before
focused runtime qualification. No production defect or numerical failure arose
from those compile corrections. No tolerances or method orders were changed
after observing runtime results.

## Numerical witnesses

The real fixtures retain source interval [0, 1] seconds, asymmetric principal
inertia (2, 3, 4), 8 kg mass, nonzero off-centre post-impact spin, real Earth
terrain and 1,000 ticks of host debt before admission. Both successful stages
retain **EVENT COVERAGE UNKNOWN**, exact source/target/initial-velocity identity,
and complete canonical nonmutation.

Requests were fixed before Florida execution: 0.002 m component position,
2e-6 m/s component velocity, 2e-6 rad/s component spin, 4e-6 sign-invariant
unit-quaternion chordal attitude error. Two millimetres covers roughly 64 ULPs
at 1 AU; the velocity/spin/attitude budgets account for the retained sub-10-us
root enclosure and physical rates. These are numerical bounds, not contact
clearance tolerances.

| Fixture | Linked D enclosure (s) | Max position error (m) | Max velocity error (m/s) | Max spin error (rad/s) | Attitude chordal error |
|---|---|---|---|---|---|
| Coast | [0.49663543701171836, 0.4966430664062505] | 6.103515625000001e-5 | 3.637978807091714e-12 | 2.4406343647476184e-8 | 5.074279406046512e-7 |
| Force | [0.5199813842773433, 0.5199890136718756] | 6.103515625000001e-5 | 6.312147888820619e-7 | 2.9424510883391267e-8 | 5.577355888403054e-7 |

Coast spin/attitude truncation radii are 9.053197967557677e-14 and
7.249765320079106e-12. Force radii are 5.292754184507648e-13 and
3.860362610003607e-11. The retained root enclosure feeds polynomial intervals;
truncation and final representation are separately explicit. Root uncertainty
and outward arithmetic coexist in those intervals: no disjoint error-budget
attribution is claimed.

[replay-witnesses.json](replay-witnesses.json) retains complete outputs, initial
state/source expressions, certificates, requests and signed Int64 bit arrays.
It is copied verbatim from successful output, without parsing 64-bit integers
through binary64 JSON numbers. Entire witness strings matched Debug/Release,
independently rebuilt provider chains and repeated checked reads. Two witnesses
are retained once rather than duplicated by configuration.

Permanent analytical coverage includes exact rational quadratic extrema and
non-rational translation; orbital cancellation; nonzero force/epoch; zero,
principal and spherical spin; asymmetric evolving spin; exact rational closed
majorants; sign-equivalent/noncommuting attitudes; independent square-root bounds
for final-bit quaternion meaning; nonfinite/resolution/work-limit refusals; roots
near the endpoint and unproved ordering; and a recontact counterexample.

The degree-eleven Taylor expansion has a uniform twelfth-derivative remainder
derived from conserved energy and unit-quaternion norm. It does not rely on RK4
convergence as certification. Endpoint bits undergo the existing normalization
and sign policy before their exact-normalized physical meaning is certified.
Arithmetic version 1 uses the existing qualified runtime guard. Foreign FP mode
or unsupported runtime fault injection was not performed.

## Bounded measurements

Predeclared: 32 warmups; 101 ordinary-runtime Stopwatch samples; separate checked
eight-call allocation windows. Sorted indices 50/95/99/100 give median/P95/P99/max.
No timing interval contains no-GC entry/exit. The independent existing byte[128]
positive control detected 152 counter bytes in each configuration.

Release measurements, microseconds:

| Path | Median | P95 | P99 | Max | Allocation / 8 calls |
|---|---:|---:|---:|---:|---:|
| Existing-chain source preparation | 1756.8 | 3503.3 | 3531.1 | 5150.4 | 13376 B |
| Translation | 0.3 | 0.3 | 0.3 | 0.4 | 0 B |
| Rotation | 17.2 | 18.2 | 22.7 | 22.7 | 0 B |
| Paired staged construction | 30.5 | 31.4 | 33.7 | 34.9 | 0 B |
| Checked consumption | 22.1 | 23.5 | 25.7 | 26.1 | 0 B |
| Numerical refusal | 30.6 | 31.6 | 34.3 | 34.5 | 0 B |
| Stale refusal | 1.9 | 2.0 | 2.0 | 2.0 | 0 B |

[validation.json](validation.json) retains the complete Debug and Release cost
tables, validation ledger and failure witness. Source preparation starts with an
existing Case and loaded terrain query; it rebuilds M14.9–M14.14, allocating
1,672 B/call. This is separated from the reusable paths. All measured calls
completed; entry/exit passed; reusable paths measured exact zero in both builds.

Degree/order are fixed at 11/12: 66 coefficient pairs plus 78 majorant pairs,
zero substeps, refinements or retries. Pair counts are not operation counts.
Summed explicit stack-array payload is 1,760 B, excluding locals, alignment and
call frames. Receipt/value/endpoint layout sizes are 5,840/1,112/544 B. No new
provider heap object/cache is created by staging. Existing provider references
remain retained by the value receipt. No whole-frame or high-warp scalability
claim follows; Release checked-read timing exceeding Debug is retained honestly.

## Independent review and architectural reference

Three read-only reviews passed the scoped implementation:

- Numerical review: root correlation, Euler/quaternion signs, uniform remainder,
  final normalization and deterministic arithmetic. It requested a noncommuting
  quaternion-order control; the added exact-rational test and rereview passed.
- Ownership review: checked M14.14 consumption, target/source applicability,
  mutation-free failure, unknown coverage and no publication/interval consumption.
  Its requested explicit root-after-target refusal test was added before final
  full builds and passed the focused gates.
- Cost/boundary review: separate timing/allocation, fixed work, receipt ownership
  and KSA staging. Stack and pair-count claims were narrowed to actual evidence.

Current installed KSA remains version
`2026.9.7.5402+487c3f340de24c6a81037120b6d1129c045c5400`; installed/source hashes are
in identity.json and match the banked source provenance. Read-only references:
`VehicleUpdateData`, `VehicleUpdateState`, `VehicleUpdateTask` and `PhysicsBubble`.
Adopt complete prepared game state, private integration, paired staged output and
separate later application. Adapt provider-owned exact-root certification.
Intentionally preserve exact identity/certified transport rather than rounded
root time. No proprietary code was copied. The earlier
[banked provenance](../private-postimpact-state/README.md) remains relevant; no
new broad engineering-history campaign was required. No Bepu/solver gate was
crossed.

These reviews do not waive the failed full Release gate. Remaining responsibilities
are failure classification and the unrun acceptance gates, followed separately by
future event coverage/admission/publication decisions under Project Control.

## Reproduction and retention

From `E:/NovaCore`, preserve this unbanked candidate and verify identity.json before
execution. The following commands document completed runs; they are not an
instruction to rerun the failed gate without a separately bounded decision.

```powershell
dotnet build NovaCore.sln -c Debug --no-restore -v minimal
dotnet build NovaCore.sln -c Release --no-restore -v minimal
dotnet tests/NovaCore.Simulation.Tests/bin/Debug/net10.0/NovaCore.Simulation.Tests.dll --private-propagation-only
dotnet tests/NovaCore.Simulation.Tests/bin/Release/net10.0/NovaCore.Simulation.Tests.dll --private-propagation-only
dotnet tests/NovaCore.Graphics.Tests/bin/Debug/net10.0/NovaCore.Graphics.Tests.dll '--case=Private Florida paired canonical propagation'
dotnet tests/NovaCore.Graphics.Tests/bin/Release/net10.0/NovaCore.Graphics.Tests.dll '--case=Private Florida paired canonical propagation'
dotnet tests/NovaCore.Simulation.Tests/bin/Debug/net10.0/NovaCore.Simulation.Tests.dll
dotnet tests/NovaCore.Simulation.Tests/bin/Release/net10.0/NovaCore.Simulation.Tests.dll
git diff --check
```

Full-suite commands run in fresh processes, Debug then Release, stopping on first
material failure. Do not assert recurrence from this single failed Release run.
The next acceptance steps would be ReferenceFrames, Precision and separate
focused M14.6–M14.14 gates; they remain unexecuted after the stop.

Original-package budget: **64 KiB**; with the later differential and normal
acceptance records, the earlier combined budget was **80 KiB**. This separately
authorized observer report/data/reproduction recipe increases the package budget
to **112 KiB**, retaining a new unresolved witness and reproducible scope limitations.
The final authorized global coverage adds a report, compact decisive data and
reproduction recipe; that combined package budget was **160 KiB**. The separately
authorized banked/candidate differential retains compact identities, witnesses,
the ten-run matrix and reproduction, with a **64 KiB** incremental budget and
**224 KiB** combined budget. Its cleanup outcome is in the differential ledger.
The registration/module discriminator adds concise static reachability, identities,
fourteen process results, decisive witnesses, marker limits and reproduction,
with an **80 KiB** incremental budget and **304 KiB** combined package budget.
Its retained disposable scratch is recorded separately from permanent evidence.
Final acceptance adds a compact ledger, report, reproduction commands and independent
review under a **48 KiB** incremental budget (**352 KiB** combined). Earlier
validation/observer reports remain historical records, not current acceptance status.
The original four files retain contracts/results,
source and binary hashes, decisive numerical bit witnesses and reproduction.
Raw stdout/build logs stay out of the repository. No disposable comparison tree,
observer, profiler, runtime dump or numerical experiment file was created in the
original implementation step. The later probe's cleanup is recorded in its report. No
files were deleted; disposable evidence remaining for this ticket is zero.
Standard ignored bin/obj deployment was rebuilt and not duplicated into evidence.
No staging, committing, banking, pushing or tag changes occurred.
